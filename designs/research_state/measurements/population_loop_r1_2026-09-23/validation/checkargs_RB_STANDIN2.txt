checked 131 flags from --argv
  parser                         : the PINNED commit 6eb9c776's — --pin-commit in the argv
  ✅ argv validated against PINNED parser @6eb9c776 via the pinned build_parser() [mode=build_parser, 627 options, 1.5s]
  accepted by the PINNED parser  : 129
  launcher-owned (not forwarded) : 2
  unrecognized                   : 0
  resolved against the FORK PARENT: /home/goodlad/dev/gen3ai/models/ai_v13_17_fold_k1/model_config.json
      56 unset flag(s) INHERITED from it — the checks below read the EFFECTIVE config, not the argv
  [ForkLR] ✓ this fork names its own dose (--fork-lr 0.00025 --fork-lr-freeze)
  refused combinations           : 1  ✗ WOULD FAIL IN resolve_config
      --distill-target action requires --distill-coef > 0 — the target form is a property of the distill term; without the term there is nothing to shape
        · --distill-target 'action' (INHERITED from the parent's recorded config)
        · --distill-coef 0.0 (from the argv)

  These are main.train.combination_checks — the value-conditional refusals the
  launch path prints verbatim, read from the SAME list resolve_config refuses on
  (a rule that lives only inside resolve_config is invisible here: C1 2026-09-01,
  G5 2026-09-06). A value marked INHERITED was never typed: it came from the parent
  checkpoint's model_config.json, which is what a fork resumes. A finding marked
  ADVISORY depends on something only the launch has — it may not fire there.

  ARCH SURFACE vs designs/production_config.json  [production_config@360f8378dd90]
    ✓ every ARCH-surface key matches the production mirror (39 of 51 registry toggles are the ARCH surface; 9 critic readouts + 3 non-structural rows are excluded by their own declaration)
  ✓ this command still launches (its own commit's parser accepts every flag)
