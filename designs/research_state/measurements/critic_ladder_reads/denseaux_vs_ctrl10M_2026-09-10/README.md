# Critic ladder read — arm 9 `ai_v12_20_ladder_denseaux` (`--win-prob-dense-aux 1.0`, KataGo-style dense auxiliary targets, pin 46ca68ef) vs all three controls at pinned 10M (live cycles, quota-matched, the wider-draw floor, tool v5)

`vs_ctrl10M/` (registered pair), `vs_ctrl10M_b/`, `vs_ctrl10M_c/`. Offline 400/800 reads follow (queued). Verdict: ledger 2026-09-10 · *READ · critic ladder arm 9*.

**Offline 400- and 800-game reads vs all three controls (2026-09-11):** `vs_<control>/critic_read_hp400.md/json` (tool v5; a v6 re-read is queued) and `critic_read_hp800_v6.md/json` (tool v6 — the turns-4–10 class AUC, the late spread rows, the optimal-spread decomposition and the crossing step; floors: hp400/hp800 floor files, the v6 rows vs zero — the two-draw v6 floor is 0.022 on class AUC t4–10). Nothing on any registered row against any control at either size. Ledger 2026-09-11 · *arm 9 on the OFFLINE frames*.
