# Critic ladder read — arm 11 `ai_v12_22_ladder_lambda095` (`--win-prob-lambda 0.95`, the DOSE point between 1.0 and 0.9; seed 1002, pin 28ece02a) on the OFFLINE 400/800-game frames vs all three controls

`vs_<control>/critic_read_hp400.md/json` and `critic_read_hp800.md/json` (tool v6; the v6 rows read vs zero — the floor files in use predate v6). The registered bar (ledger 2026-09-10 *CLARIFICATION · the lambda095 bar*): `cond.spread_ratio.t1_3` ARM − CONTROL with the CI below zero, against all three controls on both draws — PASS only if all six hold. Result: 3 of 6 hold (vs `ctrl10M` both draws, vs `ctrl10M_c` at 400); the three against the replicate controls' lower values include zero — FAIL at the registered standard, with the POINT below every control on both draws. Verdict and the 1.0 / 0.95 / 0.9 dose curve: ledger 2026-09-11 · *READ · arm 11*.
