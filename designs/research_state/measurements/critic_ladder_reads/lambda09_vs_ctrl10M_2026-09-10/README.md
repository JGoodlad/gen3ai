# Critic ladder read — arm 8 `ai_v12_19_ladder_lambda09` (`--win-prob-lambda 0.9`, pin 28ece02a) vs all three controls at pinned 10M (live cycles, quota-matched, the wider-draw floor applied)

`vs_ctrl10M/` (the registered pair; the control's default quota gives an 8/12 matched frame — ~200 battles — so its decoder rows are wide), `vs_ctrl10M_b/`, `vs_ctrl10M_c/` (both at 40/40/10, matched at 40/37). Verdict and the offline confirmation plan: ledger 2026-09-10 · *READ · critic ladder arm 8*.

**`critic_read_v4.md/json` beside each (2026-09-10)** — the same three pairs re-read by tool **v4**, which adds the **(A)/(B)** rows that say whether an own-team decode is CONDITIONING or SUBSTITUTION: within-own-team and within-team-STRATUM Murphy resolution, the between-team spread ratio, `own_team_r2.late` and the PROVISIONAL `t1 − late` contrast. **Every pre-existing delta is bit-identical to `critic_read.md`** — the new rows are computed from the trace tree, so no cached readout moved. The new rows carry NO floor in `../replicate_floor_10M.json` yet (they read *vs ZERO — NO FLOOR*); the two floor pairs' own `critic_read_v4` files supply the first draws, and they are large: the between-team spread's run-to-run |Δ| is **0.154–0.185**, which swallows every arm delta on that row.

**Offline 400- and 800-game reads (2026-09-10):** `critic_read_hp400.md/json`, `critic_read_hp800.md/json` (both sides offline-generated, sentinels 3, floors from the offline floor pairs on tool v4). The live-frame own-team effect does NOT replicate on them; the between-opponent spread ratio is DOWN past the floor on both. Ledger 2026-09-10 · *arm 8 on the offline frames*.
**`critic_read_v5.md/json` beside each (2026-09-10)** — the same three pairs re-read by tool **v5**, which adds the **CALIBRATION SLOPE** (the Cox 1958 recalibration pair: the outcome regressed on `logit(V)`; **`b > 1` = UNDER-dispersed, i.e. SHRUNK toward the base rate**), its WITHIN-STRATUM and COMMON-SUPPORT companions, and the per-side LEVER-ARM table (`sd(V)` / `sd(logit V)` / support / clipped share) without which a wider interval on the shrunk side reads as a null. **Every pre-existing delta is bit-identical to `critic_read_v4.md`.** Arm 8 reads slope **1.579** pooled / **1.426** at turn 1–3 against controls at 1.240–1.441 / 0.761–0.979 — the highest of all eight 10M runs on both rows, and the only arm with a positive POOLED delta — but the replicate floor (0.201 / 0.217, from the two `…_FLOOR*/critic_read_v5.json` draws) is of the same order as the deltas: the arm's slope LEVEL is well above 1 where the controls sit at ~1 early (the OVERSHOOT branch of the restated (C)), while the DELTA is **UNREADABLE at 100 games** with the direction uniform (12/12 positive). 🚨 These are LIVE frames; the decisive read is the 400/800-game offline pair, where the surviving rows' floors are 3–8× tighter. Design note §2e; ledger 2026-09-10 · *INSTRUMENT + READ · the CALIBRATION SLOPE is BUILT*.
