# THE REPLICATE FLOOR — `ai_v12_15_ladder_ctrl10M_b` (seed 1001, pin 377a5aa1, quota 40/40/10) vs `ai_v12_11_ladder_ctrl10M` (seed 42, pin f3502568, default quota), both pinned to `step_10000032`

`python -m main.ops.critic_read ai_v12_15_ladder_ctrl10M_b --control ai_v12_11_ladder_ctrl10M --step 10000032 --out <job tmp>/read_floor`, tool v3 (quota-matched: the arm side subsampled to caps 8/12/3 over 21 seeds for the frame-sensitive rows), 2026-09-09 ~11:40 PT. Two identically-configured runs differing by the seed (which does not reach the battle stream — ledger 2026-09-09 VERDICT) and the verified-neutral span. **This is total run-to-run variance, ONE draw of a difference**: `../replicate_floor_10M.json` holds `|Δ|` per row as the floor magnitude, which can only DEMOTE a detection to WITHIN FLOOR, never make one (ledger 2026-09-09 CORRECTION + SCOPE). Verdict and the relabelled arms: ledger 2026-09-09 · *THE FLOOR*.

**400-game OFFLINE read (2026-09-09):** `critic_read_hp400.md/json` — both sides 400-game offline-generated cycles (`main.ops.eval_trace_gen`, seed 20260909, 12 opponents, full capture); NOT comparable to the 100-game rows above (different population); ledger 2026-09-09 · *THE FLOOR AT 400 GAMES* and *THE ARMS AT 400 GAMES*.

**800-game OFFLINE read (2026-09-10):** `critic_read_hp800.md/json` — both sides 800-game offline cycles (seed 20260910); its own floor `../replicate_floor_10M_hp800.json`; ledger 2026-09-10 · *THE ARMS AT 800 GAMES*.

**`critic_read_v4.md/json` (2026-09-10):** the same pair re-read by tool **v4** for the FIRST FLOOR DRAW of the (A)/(B) rows. Pre-existing deltas bit-identical. `|Δ|` on the new rows: within-team resolution **0.0071**, within-stratum **0.0146**, between-team spread ratio **0.1848** (raw 0.1740), `own_team_r2.late` **0.0320**, `t1 − late` **0.0380** (PROVISIONAL — that row can never be given a floor from a control pair; both sides read ≈ 0 at turn 1).

**`critic_read_v5.md/json` (2026-09-10):** the same pair re-read by tool **v5** for the FIRST FLOOR DRAW of the CALIBRATION-SLOPE rows (draw 1). Pre-existing deltas bit-identical. `|Δ|` on the new rows — slope pooled **0.1281**, slope turn 1–3 **0.0412**, slope common-support **0.0196**, slope within-stratum **0.1624** — and `../replicate_floor_10M.json` now carries the WIDER of the two draws per key. 🚨 Every one of this file's 63 pre-existing floor entries recomputed BIT-FOR-BIT on v5, which is the check that the new rows changed nothing else.

**`critic_read_hp400_v5.md/json` + `critic_read_hp800_v5.md/json` (2026-09-10):** the CALIBRATION-SLOPE floor draws on the offline frames, added to `../replicate_floor_10M_hp{400,800}.json`. 🚨 **This row's floor is RUN-LEVEL, not frame-level:** these two identically-configured runs differ on the pooled slope by **+0.191 [+0.088, +0.296] at 4,772 battles a side and +0.254 [+0.175, +0.331] at 9,563** — both CIs clear of ZERO, and the magnitude GREW with eval size instead of shrinking. More eval does not remove it, so the slope cannot be read as an arm-vs-one-control delta at any size; it needs a replicate ARM. This is what turned arm 8's (C) read from a detection against `ctrl10M` into NOT SUPPORTED.
