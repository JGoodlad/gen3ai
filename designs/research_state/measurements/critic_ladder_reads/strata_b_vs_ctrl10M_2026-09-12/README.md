# `strata_b` vs the three controls — the registered confirmatory read (2026-09-12)

**Verdict: MISS on all six pair-draws — `strata` NOT CONFIRMED.** Ledger 2026-09-12 · *VERDICT · `strata_b` FAILS the registered confirmatory read*.

Arm `ai_v12_24_ladder_strata_b@10000032` (`--win-prob-strata-weight 1.0`, `--seed 1002`, pin `f871e79f` — the same pin and argv as `ai_v12_17_ladder_strata`, which ran at the default seed). Two offline draws, generated by `hp_strata_b.sh` (log beside it): `hp400/` = 400 games × 12 opponents, seed 20260909; `hp800/` = 800 games, seed 20260910. Each read is `main.ops.critic_read` v6 against the control's trace tree from the SAME draw. `hp800/` uses `hp800_floor_v6.json` (the two-control-pair floors, class AUC t4–10 = 0.0219); `hp400/` uses `hp400_floor.json`, which predates v6 and carries no class-AUC rows, so its class-AUC lines are labelled "vs ZERO — NO FLOOR" and are descriptive only.

| draw | control | `cond.opp_class_auc.t4_10` arm | control | Δ [CI] | label |
|---|---|---|---|---|---|
| hp800 | `ctrl10M` | 0.6792 | 0.7086 | **−0.0294 [−0.0444, −0.0147]** | NOT DETECTED (clears zero, not the 0.0219 floor) |
| hp800 | `ctrl10M_b` | 0.6731 | 0.7069 | **−0.0338 [−0.0493, −0.0191]** | NOT DETECTED (same) |
| hp800 | `ctrl10M_c` | 0.6804 | 0.6878 | **−0.0075 [−0.0225, +0.0073]** | WITHIN FLOOR |
| hp400 | `ctrl10M` | 0.6798 | 0.7163 | −0.0364 [−0.0576, −0.0149] | (no floor) |
| hp400 | `ctrl10M_b` | 0.6798 | 0.7121 | −0.0323 [−0.0539, −0.0126] | (no floor) |
| hp400 | `ctrl10M_c` | 0.6803 | 0.7041 | −0.0239 [−0.0447, −0.0030] | (no floor) |

`strata` itself read 0.7501 / 0.7590 / 0.7599 on its three draws (`strata_vs_ctrl10M_2026-09-09/`). Gate rows at hp800: `gate.resolution.all` +0.0098 / +0.0100 / +0.0094 (CIs clear zero against every control); `gate.resolution.bot` +0.0143 / +0.0107 / +0.0068 (DETECTED / n.d. / within floor). Self-play crossing 4,128,768 on both sides of every pair — no mismatch.

**Hazard seen in these files:** the ARM column's bracket on the `cond.opp_class_auc.*` rows is not always the arm's own CI (e.g. `+0.6798 [+0.7158, +0.7169]`); the point and the Δ CI are right. Backlog row 2026-09-12.

Each `vs_*/` holds `critic_read.md`, `critic_read.json`, and `cond_readout.json` where the tool wrote one.
