# Critic ladder read — arm 5 `ai_v12_14_ladder_truevalue` (`--value-true-team`, the PRIVILEGED critic, pin d11386dc) vs all three controls at pinned 10M (live cycles, quota-matched, the wider-draw floor applied)

The ceiling probe: the value path sees the opponent's TRUE team at training and eval; the policy never does. `vs_ctrl10M/` (registered pair), `vs_ctrl10M_b/`, `vs_ctrl10M_c/`. Offline 400/800-game reads follow (queued on the generator). Verdict: ledger 2026-09-10 · *READ · critic ladder arm 5 (privileged)*.
