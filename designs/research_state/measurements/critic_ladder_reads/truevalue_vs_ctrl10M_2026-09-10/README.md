# Critic ladder read — arm 5 `ai_v12_14_ladder_truevalue` (`--value-true-team`, the PRIVILEGED critic, pin d11386dc) vs all three controls at pinned 10M (live cycles, quota-matched, the wider-draw floor applied)

The ceiling probe: the value path sees the opponent's TRUE team at training and eval; the policy never does. `vs_ctrl10M/` (registered pair), `vs_ctrl10M_b/`, `vs_ctrl10M_c/`.  Verdict: ledger 2026-09-10 · *READ · critic ladder arm 5 (privileged)*.

**Offline 400- and 800-game reads (2026-09-10):** `critic_read_hp400.md/json`, `critic_read_hp800.md/json` — both sides offline-generated (sentinels 3, all battles traced), quota-matched at 399/173/4 and 797/354/8, floors from `../replicate_floor_10M_hp400.json` / `_hp800.json`, tool v5. Bot resolution +0.000 / +0.003 WITHIN FLOOR; the between-opponent spread ratio and the own-team decode both DOWN inside the floor; the privileged head +0.08–0.09 more optimistic against its own continuation on every draw (run-level row) and worse calibrated (ECE all +0.041 clear of the eval-draw floor at 800, pool ECE +0.11) — both POST-HOC leans, never registered, and no second truevalue draw exists to promote them (ledger *CORRECTION to arm 5's offline read*). The ceiling probe is closed. Ledger 2026-09-10 · *READ · arm 5 on the OFFLINE frames*.
