# `vf15_b` vs the three controls — the registered confirmatory read (2026-09-12)

**Verdict: PASS on all six pair-draws — `vf15`'s DOWNWARD detection on `cond.opp_class_auc.t4_10` is CONFIRMED at run level.** Ledger 2026-09-12 · *VERDICT · `vf15_b` PASSES*.

Arm `ai_v12_25_ladder_vf15_b@10000032` (`--vf-coef 1.5`, `--seed 1001`, pin `f3502568` — `vf15`'s argv verbatim plus the pin, seed and run name; `vf15` ran unpinned on the same commit at the default seed). Two offline 800-game draws generated by `hp_vf15_b.sh` (log beside it): `hp800/` seed 20260910 with `hp800_floor_v6.json` (two control pairs, class AUC t4–10 floor 0.0219); `hp800b/` seed 20260911 with **`hp800b_floor_v6.json`, built here** from the two hp800b control pairs (`ctrl10M_c`'s hp800b tree was generated for this read; the pair reads are in `hp800b/floor_pairs/`) by the same max|Δ| rule — class AUC t4–10 floor **0.0245** (the earlier single-pair estimate used by `repr_class_decode_*` was 0.031). The registered bar (ledger 2026-09-12 · *VERDICT · strata_b FAILS*): the Δ CI clears −0.022 DOWNWARD at both draws against all three controls.

| draw | control | arm | control | Δ [CI] | label |
|---|---|---|---|---|---|
| hp800 | `ctrl10M` | 0.6385 | 0.7137 | **−0.0752 [−0.0907, −0.0606]** | DETECTED |
| hp800 | `ctrl10M_b` | 0.6385 | 0.7060 | **−0.0675 [−0.0827, −0.0521]** | DETECTED |
| hp800 | `ctrl10M_c` | 0.6385 | 0.6914 | **−0.0529 [−0.0682, −0.0374]** | DETECTED |
| hp800b | `ctrl10M` | 0.6254 | 0.7125 | **−0.0871 [−0.1022, −0.0714]** | DETECTED |
| hp800b | `ctrl10M_b` | 0.6254 | 0.6913 | **−0.0658 [−0.0814, −0.0505]** | DETECTED |
| hp800b | `ctrl10M_c` | 0.6254 | 0.6889 | **−0.0635 [−0.0788, −0.0470]** | DETECTED |

`vf15` itself read 0.6278 / 0.620 on the same two draws (−0.082 / −0.080 vs `ctrl10M`); the replicate's point (mean −0.069) is well above half of the original's, so no inflation shape. Gate rows: `gate.resolution.all` within floor on every pair (−0.003 to +0.000); `gate.resolution.bot` +0.002 to +0.011, none clearing its floor — **V's outcome resolution is unchanged while its opponent-class discrimination falls.** `t1_3` −0.009 to −0.032 (three of six DETECTED against the 0.009–0.015 floors). Self-play crossing 4,128,768 on both sides of every pair.

Each `vs_*/` holds `critic_read.md`, `critic_read.json` and `cond_readout.json` where the tool wrote one. The ARM column's bracket on the class-AUC rows is not the arm's own CI (backlog row 2026-09-12); the point and the Δ CI are right.
