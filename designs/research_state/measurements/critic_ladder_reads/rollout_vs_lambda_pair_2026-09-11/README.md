# Critic ladder read — arm 10 `ai_v12_23_ladder_rollout` (`--win-prob-rollout-target 0.0012 --win-prob-rollout-r 8 --win-prob-rollout-mode replace --win-prob-rollout-weight 64`, on arm 8's λ-0.9 argv, seed 1003, pin 40bf23d9) on the OFFLINE 400/800-game frames vs the λ-0.9 PAIR (primary comparator) and `ctrl10M` (reference)

`vs_lambda09/`, `vs_lambda09_b/`, `vs_ctrl10M/` × `critic_read_hp400.md/json` (floor `hp400_floor.json`) and `critic_read_hp800.md/json` (floor `hp800_floor_v6.json` — carries the v6 two-draw floors: class AUC t4–10 0.022). Tool v6, quota-matched, sentinels 3. Registration: ledger 2026-09-11 · *VERDICT + REGISTRATION · arm 10* (primary rows `cond.opp_class_auc.t4_10`, `gate.resolution.bot`, `gate.resolution.all`; the late spread rows reported; void if `rollout_mass_weighted` left 5–15 % — it read 0.07086 at all 98 updates). Verdict: ledger 2026-09-11 · *READ · arm 10*. The arm crossed into self-play at 2,162,688 (matched to `lambda09`, mismatched to `lambda09_b` and `ctrl10M` — printed on those pairs).
