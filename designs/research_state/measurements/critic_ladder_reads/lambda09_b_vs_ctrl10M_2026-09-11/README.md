# Critic ladder read — `ai_v12_21_ladder_lambda09_b` (arm 8's RUN-LEVEL REPLICATE, `--win-prob-lambda 0.9`, seed 1002, pin 28ece02a) on the OFFLINE 400/800-game frames vs all three controls — the PRE-REGISTERED test of arm 8's post-hoc overall-resolution gain

`vs_<control>/critic_read_hp400.md/json` (tool v5) and `critic_read_hp800.md/json` (tool v6 — carries the turns-4–10 class AUC, the late spread rows, the optimal-spread decomposition and the crossing step; the v6 rows read vs zero because the floor files in use predate v6). Both sides offline-generated, sentinels 3, quota-matched. The registered test (ledger 2026-09-10 *post hoc; pre-registered for lambda09_b*): `gate.resolution.all` DETECTED only if the CI clears zero at both draws against all three controls. Result: NOT DETECTED at all six — same sign as arm 8, about half the size. Verdict: ledger 2026-09-11 · *READ · the λ-0.9 replicate's pre-registered test*.
