$ python -m main.checkargs --argv "$(cat argv_hidose.txt)"   [run 2026-09-21 from the MAIN checkout, CPU-only]
checked 131 flags from --argv
  parser                         : the PINNED commit 6eb9c776's — --pin-commit in the argv
  ✅ argv validated against PINNED parser @6eb9c776 via a best-effort STATIC scan of the pinned add_argument() calls (NOT authoritative) [mode=ast_scan, 279 options, 4.5s]
      (why not the real parser: the pinned tree's main.train_rl_agent will not import here (BaselineError: no baseline registry at '/tmp/pinned-argv-6eb9c776-uaxkhob_/designs/baselines.json' — this file is committed; a checkout without it is broken, not merely unconfigured.); running the pinned entry point reached no parse_args() (BaselineError: no baseline registry at '/tmp/pinned-argv-6eb9c776-uaxkhob_/designs/baselines.json' — this file is committed; a checkout without it is broken, not merely unconfigured.))
  accepted by the PINNED parser  : 129
  launcher-owned (not forwarded) : 2
  unrecognized                   : 0
  resolved against the FORK PARENT: /home/goodlad/dev/gen3ai/models/ai_v13_12_plateau/model_config.json
      56 unset flag(s) INHERITED from it — the checks below read the EFFECTIVE config, not the argv

  ARCH SURFACE vs designs/production_config.json  [production_config@360f8378dd90]
    ✓ every ARCH-surface key matches the production mirror (39 of 51 registry toggles are the ARCH surface; 9 critic readouts + 3 non-structural rows are excluded by their own declaration)
  ✓ this command still launches (its own commit's parser accepts every flag)

$ python -m main.checkargs models/ai_v13_13_exploit5_offense   [the TEMPLATE, for the ARCH-surface comparison]
checked 129 flags from models/ai_v13_13_exploit5_offense
  parser                         : the PINNED commit 6eb9c776's — --pin-commit in the argv
  ✅ argv validated against PINNED parser @6eb9c776 via a best-effort STATIC scan of the pinned add_argument() calls (NOT authoritative) [mode=ast_scan, 279 options, 4.4s]
      (why not the real parser: the pinned tree's main.train_rl_agent will not import here (BaselineError: no baseline registry at '/tmp/pinned-argv-6eb9c776-0jnxwgmp/designs/baselines.json' — this file is committed; a checkout without it is broken, not merely unconfigured.); running the pinned entry point reached no parse_args() (BaselineError: no baseline registry at '/tmp/pinned-argv-6eb9c776-0jnxwgmp/designs/baselines.json' — this file is committed; a checkout without it is broken, not merely unconfigured.))
  accepted by the PINNED parser  : 127
  launcher-owned (not forwarded) : 2
  unrecognized                   : 0
  resolved against the FORK PARENT: /home/goodlad/dev/gen3ai/models/ai_v13_12_plateau/model_config.json
      56 unset flag(s) INHERITED from it — the checks below read the EFFECTIVE config, not the argv

  ARCH SURFACE vs designs/production_config.json  [production_config@360f8378dd90]
    ✓ every ARCH-surface key matches the production mirror (39 of 51 registry toggles are the ARCH surface; 9 critic readouts + 3 non-structural rows are excluded by their own declaration)
  ✓ this command still launches (its own commit's parser accepts every flag)
