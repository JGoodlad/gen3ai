---
name: project_current_run
description: "The live training generation, its config delta, and the standing per-run flag policies."
metadata: 
  node_type: memory
  type: project
  originSessionId: dfaec083-a419-45f2-9aaf-889c7704c967
  modified: 2026-08-16T13:46:56.936Z
---

**GEN-17 VERDICT (2026-08-21): BOTH GATES PASS — PFSP CONVICTED TWICE, INDEPENDENTLY.**
`ai_v9_21_gen17_pfspoff_0820` (single-variable PFSP-off, fresh weights, launchdiff-proven): PRIMARY
vs gen-15 **−8.82 [−17.3, −0.30] NON_INFERIOR**; SECONDARY direct vs gen-16 **+26.48 [+17.9,
+35.0]** — conviction condition met with room. Cross-check: implied (via gen-15) +32.75 vs direct
+26.48 = **6.3 ELO agreement** across two independent 6,400-game experiments — the arena
instrument self-validated. **Conviction written narrowly: "pure PFSP over a homogeneous
fresh-lineage self-pool costs ~26–33 ELO"** (ai_v8_14 ran pfsp 2.5 productively over a diverse
anchored pool + distill KL — ecology matters). Executed dispositions: substrate EXONERATED
(gen-16's −41.6 was PFSP, not the mechanics content) · **gen-17 = the exploiter-gate base** ·
PFSP returns only MIXED (uniform floor) with its own gate — natural vehicle = the flywheel
(teachers-in-pool restores the v8_14 ecology). Honest limit: gen-17 is non-inferior NOT superior
to gen-15 (CI excludes 0 on the negative side) — the substrate's POSITIVE ladder value is
unproven; that burden now sits where designed: the exploiter gates. Bait-arm banked: OA2 at **86%
of α's strength on loop steps** (1,526× the diluted global read — dilution now a 2-for-2
instrument lesson with c1) yet 0 flips vs the 0.97 commitment ⇒ credit-assignment magnitude
problem; B3 is THE quantity; loops-on-TD-arms is the decisive queued test (λ>0 ⇒ B3 falls,
pre-registered). Also queued: per-bot heal read + B1–B4 reproduction, switch_branch census,
EV-coherence audit on the 23 loop decisions, rung-2 gates own line.

**GEN-16 VERDICT (2026-08-20): INFERIOR, direct-confirmed — Δ −41.57 CI [−50.2, −33.0]** (6,400
direct cross-arch arena games, two-process websocket harness over a 9XXX port; every gen-16 node
below every gen-15 node). The mechanics generation DECOMPOSED: B1/B2 bait gains = the eff-fix
MEMORY effect (gap-stratified: in-window re-clicks fell 5×, out-of-window flat — f05764e's
behavioral receipt within one generation); first-bait avoidance did NOT arrive (OA2 thin: β
influence ×4.3 but 56× weaker than α; switch_branch carries argmax margins, not mass); α→critic
severance causally confirmed (dv 0.158→0.0000); α/β quality jumped (switch top-1 0.48→0.82, β
species 4×) = bot-weight + live eff. **Prime suspect for the −42: PFSP's CAPABILITY variant**
(frame-bias variant DEAD — per-bot sag concentrated on heuristic −11/aggressive_v2 −8, not
uniform). ⚠️ HISTORY CORRECTION (2026-08-20, checked at metadata not prose): opponent-PFSP was
NOT unprecedented — **ai_v8_14_distill3 ran `--pfsp-scale 2.5` + `--team-pfsp onesided` +
`--stable-opponent-pfsp` explicitly** (the +69 flywheel PoC). "Never production-enabled" is true
of the ai_v9 LINEAGE only; `pfsp_hardest_win_rate`'s absence pre-gen-16 dates the METRIC, not the
feature. The load-bearing difference: v8_14's PFSP ran over a DIVERSE ANCHORED pool (stable
opponents + teachers) under a distill KL anchor — every PFSP pathology had an opposing force;
gen-16 ran it over a homogeneous fresh-lineage self-pool with none. A gen-17 conviction therefore
reads "pure PFSP over a homogeneous self-pool", and PFSP's return vehicle is the FLYWHEEL itself
(teachers-in-pool restores the v8_14 ecology) + a uniform floor. **GEN-17 = single-variable PFSP-OFF re-run of the gen-16 config, fresh weights** (same
arch v97 — but never a warm start, attribution needs clean init). METHOD WINS BANKED: bot-mediated
§1 VALIDATED as fallback (|direct − mediated| = 10.8 < 15 bar — no standing caveat on past
contrasts); BT gate VALIDATED against cycle contamination (merged-graph excess width 6.2 ELO
p=0.39, 0 cross-lineage cycles); ⚠️ cross-only graphs are K(4,4) BIPARTITE = zero triangles, curl
undefined — Hodge cross reads need the MERGED graph. Bot matchup matrix COMPLETE (360k games,
10k/pair, ±3.5 ELO edges; bot metagame near-transitive: 0 significant cycles, excess 13.8 ELO).
Still owed before the package: bait-conditioned injection arm · loops-on-TD-arms (B3 falling
pre-registered) · switch_branch coordinate census · rung-2 gates (own line).

**GEN-15 §1 VERDICT (2026-08-19): SUCCESS — the v8-reward restoration recovered gen-14's
regression and more.** `ai_v9_18_gen15_v8rewards_0818` vs gen-14, tail-4 matched-12 paired refit:
**Δ +47.71, CI [+29.5, +65.9]** (bootstrap agrees). Net vs gen-13: +9.42 CI [−9.2, +28.1] — a
statistical TIE, orientation-only. ⚠️ **The −38.3 gen-14 dip is now UNEXPLAINED at the mechanism
level and operationally CLOSED**: gen-13 and gen-14 trained the SAME v9 reward, so the restoration
cannot be what gen-14 lacked — with gen-15 ≈ gen-13 net, the parsimonious read is run variance
and/or frame-deletion cost ≈ reward gain, inseparable without the 6M forks, and not worth buying
since gen-15 is the base either way. §2 mechanism: config-confirmed only (NO per-term reward
logging exists — logger queued for gen-16); reward magnitudes are NOT comparable across the
v9→v8 boundary (mean eval reward 17.3→13.0 by construction). Stall watch: cap-rate 0.73% vs
0.22%, Fisher p=0.104 n.s., games FASTER on average (46.7 vs 69.9 turns) with a fatter tail —
bimodal, watch in gen-16, do NOT act yet. §3/§4 running. **Gen-15 = the eval reference; gen-16 =
fresh weights at v96+ (route wave + LIVE eff columns via f05764e), substrate-ON mechanics gen.**
Sentinel-sweep context (ledger 2026-08-19): β NOT stateless (n=1 display artifact, retracted);
losses = critic surprise (median P(win) 0.83 on the top-50 losing decisions); bait/loop class is
a tempo leak in won games; OA2/switch_branch enablement CAUSALLY DEMONSTRATED (α/β injection probe, ledger
2026-08-19): forcing α/β to 100% moves NOTHING on bait decisions (0 flips/40 arms, β bit-exact
zero) while the positive control moves P(explosion) 41 points — the arrival channel does not
exist pre-OA2; also α→critic is strong (V moves up to 1.24) while α→policy is ~1e-2 nats.

**STATE 2026-08-17 late — gen-14 RUNNING, substrate BUILT-BUT-OFF, forks ARMED.**
gen-14 (`ai_v9_16_gen14_framedel_v91_0817`, launcher pid 1944261) is healthy at ~4.5M/25M,
~581 fps cumulative, 0 crashes; **ETA ~09:20 PT**, not the 05:00 first estimated. The substrate
(v93/v94/v95) landed on main during the run but is **entirely OFF** and does not touch it —
`ARCH_SIGNATURE` is still `gen3_event_semantics_v1`, obs 2501. **TD-aux 3-arm fork runner is armed**
(`tmp/launch_td_aux_forks.sh`, waiter pid 1981330): on gen-14's exit it verifies checkpoint /
≥24M steps / arch signature, then runs control / λ=1.0 / λ=3.0 **sequentially**, 3M steps each,
`--n-envs 16`, `--sync-to-main`. Enablement plan: [[project_substrate_before_flywheel]].

**§7 SUCCESSOR EXECUTED + the C3 line CLOSED (2026-08-17).** Coverage is REFUTED as the constraint:
stall trajectories are **3.0% of training decisions vs 0.21% of matched sentinel eval losses** (~14x
OVER-exposed), so the flywheel's thermostat has nothing to fix. The delivery fix already worked —
positive-V at a timeout loss's final decision went **93% (13/14) pre-clock → 22% (2/9)**,
P=3.0e-07, so `gen3_deadline_clock_v1` discharged the blindness. The residual (22% vs 5.1%) is
p=0.076 at n=9 and **no single run supplies n≈30 at current trace retention** — meta-analyse the
per-run DIFFERENCE across generations instead. **Both mechanisms for the stall over-confidence are
now dead** (missing input, then off-distribution) while the phenomenon stands: reopen only with a
NEW pre-registered mechanism.

**⚠️ Lever T1 (`--compile-opponents` net value) was opened and KILLED the same day — the flag is
net +40%, not a wash.** The scary "~31% of wall-clock in pool-promotion recompiles" was a
mis-attribution: that +1095 s event was the ONE-TIME self-play activation (pool seeded from empty →
all 48 workers load a 41 MB checkpoint AND pay their process's FIRST compile). The recurring
promotion cost is **+77 s (~2.7%)**. Caches all work. See [[project_throughput_compile]].

**ROUTE WAVE SHIPPED (2026-08-18, `230be94`): v96 `gen3_critic_route_wave_v1`** — the seven
licensed deletions executed (−540,786 params, −20.7%); **vf_combined IS value_pooled** (the
v89/M2 orphaned-branch class unrepresentable); ARCH_SIGNATURE bumped, MIGRATION_FLOOR 96,
**gen-16 = fresh weights** (gen-15 is eval reference, never warm start). hidden_opp PI half +
policy-side p_KO pin-tested alive. The `concat` audit arm had silently measured the seed
readout for 3 generations (position-bound instrument re-pointed at the slot's new occupant —
ledger method lesson: bind instruments to NAMED subjects). Reward defaults = v8 composition
(`43673ed`, census 1T+7PBRS+1BIAS recorded in every metadata.json).

**🚨 TWO DEFECTS (2026-08-18) + RE-BASELINE:** (1) the reward composition drifted SILENTLY at
the v8→v9 boundary — all 20 v8 runs `--all-shaping-pbrs` ON (1T+8PBRS+1BIAS, near-invariant),
EVERY v9 run OFF (28 additive BIAS terms) — unrecorded, training-only, invisible to gates; all
v9 reward/critic findings are REGIME-SCOPED. (2) fork run dirs start with an EMPTY snapshot
pool (directory-derived) → the TD-aux arms trained vs BOTS (salvage = bots-regime mechanism
evidence only; mechanism healthy, value_loss −25%). **GEN-15 = re-baseline on v8 rewards**
(--all-shaping-pbrs, draw −35, gen-14 command otherwise verbatim + licensed hygiene deletions),
primary gate vs gen-14 (near-single-variable). TD rung-2 re-runs with the pool pre-seeded (full
12) off the new base. Guards to build: the LAUNCH-DIFF gate + the empty-pool fork warning
(ledger method 2026-08-18, commit 4379a5f).

**GEN-14 VERDICT (2026-08-18): INFERIOR** — dense tie-break Δ −38.30 CI [−55.0, −21.6]
(400 games/pair, point estimate STRENGTHENED with 4× data, paired refit + bootstrap agree).
**Frames EXONERATED** (event_seats 2.77→3.58, +29.3%, all_off stable ⇒ NO revert — the
deletion's substitution bet confirmed). The −38 lives in the other v91 bundle members; the
**GIGO accidental-signal probe is pre-registered** (`designs/research_state/gigo_signal_probe.md`:
the magnitude bug was accidentally the window's only residual-attrition signal; convicted ⇒
residual-damage event row rider for gen-15, arithmetic stays honest). §3: intent_value_reduce +
value_clock DELETE (no appeal, fell further at 2×). threat KEEPS (1.07). r ALIVE 3.2× bar.
hidden_opp: dV 0 but flips 39.6% — pi half KEEPS, measured. Gen-15 PARKED pending the probe.
TD-aux forks running (gates independent of the verdict — within-fork A/Bs).

**GEN-14 ERA (2026-08-17):** the frame deletion SHIPPED (`bcdd868`, `gen3_frame_deletion_v1`: obs 3529→2437, config 90, ARCH_SIGNATURE bumped, MIGRATION_FLOOR 90) with the dependence-vs-coverage standing rule ratified (dV licenses dependence, NOT per-fact coverage — ledger method section; cant_reason closed, faint-cause+item-consumed ruled CLOSE-at-next-bump, refused-switch-target ACCEPTED loss). Gen-14 launching: fresh weights, DROP value_intent+p_KO route, tie-breaks intent_value_reduce+value_clock ON for re-audit, riders stay conditional (decision 5 undischarged). **v91 `gen3_event_semantics_v1` (2026-08-17): gen-14 RESTARTED on it** — the magnitude-GIGO fix (dead `from` guard; residuals summed into MOVE magnitude since v81 — gen-13 trained on it in full), the from_clause key-class fix, + faint_cause_id/item_transition columns pulled forward (obs 2437→2501). Gen-14 = a FOUR-change bundle, runbook AMENDED (attribution explicitly weakened); the §4 licence numbers carry the corrupted-column caveat (ratio defensible, absolutes unrecomputable). TD-aux forks feed gen-15 — ⚠️ fork base must move to a GEN-14 checkpoint (gen-13 final is behind the signature wall) and the knob needs rebase+renumber onto v91. §7 verdict: FAIL with liveness proven (ledger C6) — delivery line exhausted; next = stall training-distribution probe + TD-aux estimator.

**GEN-13 COMPLETE** (`ai_v9_15_gen13_hb_events_stack_0817`, launched 2026-08-17, fresh-init at v89
`1fa4733`, 25M steps, NON_INFERIOR vs gen-12 on the dense ladder). Delta from gen-12: `--history-events` (v81 H-B) · `h` graduated + `r`
added · `--item-belief` (v83) · `--intent-threshold` (v84) · `--intent-conditional` (v85) ·
`--damage-matrices both` · `--op-drop-renders`/`--op-believed-lean` (v86) ·
`--value-entity-pool-full` (v82) · `--value-clock`/`--value-intent` (v87). **First run where the
five value routes actually reach the critic** (v89 fixed M2). End-of-run battery is
pre-registered: `designs/research_state/gen13_endofrun_runbook.md` — §0 ELO artifact rule, §2
route liveness/|dV|, §7 the critic-calibration gate (blind-loss / stall-PIT confident-blind
fraction; FAIL branch pre-committed to input-coverage or distribution, NOT routes/search/
tail-weighting), §8 the opponent-PP observability probe (ledger C3).

**GEN-12 COMPLETE** (`ai_v9_14_gen12_h_entitypool_shaping_0816`, launched 2026-08-16, v80).
Delta from gen-11, three flags: `--belief-grad-mode shaping` (reverting label_only),
`--edge-bias-families …,h` (v79 H-A pair-history), `--value-entity-pool` (v80 — later found DEAD
under `value_from_dist`, ledger M2; fixed by v89 for gen-13).

**Standing flag policy:** `--win-prob-mode shaping --win-prob-coef 0.05` on every run from gen-11
onward (owner: "win prob head enabled from here on out for diagnostics"). 0.05 not 1.0 — it buys
parity with every belief aux; measured `grad/win_prob_share` 0.6–0.9%, calibrated (pred 0.673 vs
label 0.674).

**GEN-11 RESULT (settled).** ELO **TIED** with gen-10: matched n=12, final 24M 2079.4 vs 2079.3,
Δ +0.1, CI [−28.7, +28.9]; last-4 mean −1.0. Non-inferiority passed. But `belief/species_acc`
**regressed 7–10 pts, persistently from 4M on** (0.78 → 0.71), so the pre-registered rule
("non-inferior AND calibration flat-or-up") did NOT flip the default — hence gen-12's revert.
Confounded: gen-11 changed belief-grad AND win-prob together. **RESOLVED 2026-08-16 by gen-12** (one-flag revert, win_prob held at 0.05): **label_only
CONVICTED, win_prob EXONERATED.** Window means of `belief/species_acc` — gen-12 tracks gen-10 to
within −0.03 and is level by 10-11M (+0.002), sitting **8–9 pts above gen-11** for four consecutive
windows. Read WINDOW MEANS, never single points: per-point noise is ~5 pts, the same size as the
effect.

**The surprise, and where the explanation stands.** Cutting route C (PPO→belief head) made the
belief head *worse at its own label* — confirmed causally by gen-12. The MECHANISM is still open.

⚠️ **RETRACTED:** I first explained it as PPO acting as a damper, citing
`grad/species_belief_share` 0.060 → 0.163 and `norm_shared` 0.20 → 0.52. **Those were the LAST
LOGGED scalar, not a matched step** — the same endpoint-artifact error as the ELO drift. At matched
steps the share difference is small, noisy, and *negative* for most of training (4M: 0.093 vs 0.036;
24M: 0.042 vs 0.062). Do not rebuild that story.

What survives: the aligned-teacher reading — the `DamageOperator` is a differentiable expert, so
route C carried a physics-grounded signal correlated with the label, and removing it removed a
second teacher. Unproven. Also unexcluded: the two runs have different policies ⇒ different state
distributions ⇒ species-prediction difficulty is not held fixed.

**Step-budget fix CONFIRMED in production:** gen-11 stopped at 25,067,520 / 25,000,000 (0.27% =
one partial rollout at 48×2048). Gen-9 and gen-10 both ran to ~26M and needed manual killing.

Related: [[feedback_elo_reading_rules]] (ladder.json, at run END, matched snapshot COUNT),
[[project_fresh_generation_equivariance]], [[feedback_strength_first_principles]].

**2026-09-06 13:12 — NEW ERA RUN: `ai_v12_01_winprob_critic`** (win-prob-only critic, config v109 mode, pin e798c13a,
75M steps ≈ 4 GPU-days, 4096×K16). Owner pivot: critic + debt first, flywheel resumes on a mature parent. Read via
`python -m main.critic_gate`. Training Run owns it. All G2/G3 flywheel argvs held on disk unlaunched.
**TB BASELINES (owner Q 09-06):** the v9 long baseline = `ai_v9_29_rev1_0823` (fresh, 25.0M steps, ladder 2098, ancestor
of R2ACTION and every fold, the arm's famine comparator); gen17 (22.9M, 2034) secondary; v8 line = the ai_v8 chain
ending in ai_v8_14 (277→292M); live = ai_v12_01_winprob_critic. Curated TB dir: tb_curated/ (main.tb_curate).
