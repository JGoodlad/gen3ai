---
name: project_spread_belief_supervision
description: 2026-06-18 spread-belief was UNSUPERVISED (loss never wired) → stuck at usage-mean prior → over-estimated the largest EV; BUILT the supervision loss
metadata: 
  node_type: memory
  type: project
  originSessionId: a6b67bc4-6b85-495f-93e8-837a94b09851
---

> **Archived 2026-09-08** — SUPERSEDED by project_nature_ev_belief_built (generative nature/EV head replaced the point estimate). Preserved verbatim; nothing below is current.

DIAGNOSIS (explains the user's "belief stats off — always over-estimate the largest EV" + partly "recent runs nearly identical"): the `SpreadBelief` head (v25, predicts the opp's hidden derived stats {atk,def,spa,spd,spe}, consumed by the DamageOperator for damage + outspeed) was **UNSUPERVISED** — the dedicated loss was designed but NEVER WIRED (`spread_belief_coef` recorded but no loss consumed it; grep-confirmed nothing read `last_spread_belief` for a loss). So its only gradient was the weak/unaligned indirect op-damage path → `believed = mean + delta·std` sat at the Smogon usage-MEAN prior. The usage-mean over-weights the modal (max-invested) stat → systematically OVER-ESTIMATES the largest-EV stat for any off-modal opp → the op mis-prices damage/outspeed against the MODAL opponent ("thought it'd outspeed → died"). NOT an order/index bug (stat order verified consistent) — an unsupervised-head-pinned-at-a-biased-prior issue.

THE FIX (BUILT 2026-06-18, worktree, NOT shipped — awaiting /gen3ai-ship; gen3_unified_spread_belief_v1, NO version bump — loss-only, no forward/weight/obs-vector change): wired the supervision the design intended. LABEL: training-only Dict keys `belief_spread[6,5]` (the TRUE derived stats of each REVEALED opp mon, matched BY SPECIES against agent2's own team's computed `mon.stats` — privileged ground truth Gen3 hides; `belief_labels.build_known_spread_labels`) + `belief_spread_mask[6]`, emitted by `gen3_env._spread_labels` (INDEPENDENT of the species/move belief path → `--spread-belief` works standalone). LOSS: `instrumented_ppo._spread_belief_loss` = `spread_belief_coef·smooth_l1((believed−true)/100)` over revealed slots; grad believed→stat_head→trunk (joins the aux grad probe); leak-safe (believed is the model's OWN output; the label rides a training-only key read only by the loss). METRICS `belief/spread_{mae,largest_bias→0,n_slots}` — `largest_bias` is the DIRECT measure of the over-estimate, now supervised toward 0. To use: add `--spread-belief-coef 0.05` (requires `--spread-belief`).

VERIFIED: 2861 unit (9 new: loss masking/scale/grad/largest-bias/off + the SPREAD_STAT_ORDER==op _SB_ GIGO pin + label builder) + belief_labels_fuzz extended (1662 live decisions, 9159 revealed slots: belief_spread == real mon.stats, believed/pad zero=no-leak, OFF env no spread keys) + smoke (roundtrip PASSED, loss runs, belief/spread_mae≈19) + 5-lens adversarial review workflow (37 raised → 2 confirmed LOW, verdict SOUND/ship-ready: no GIGO/leak; clamp(min=1) pinch inert; added the coef=0-default off-invariant regression test). HONESTY GATE: it now LEARNS the spread but UNMEASURED if it lifts win-rate → the A/B is ai_v6_11+coef vs without. Pairs with [[project_op_move_order_bugclass]] (the other thing that made recent runs inexpressive). Follows [[feedback_gigo_order_bugs_asap]].

SHIPPED + RAN (ai_v6_11_typed_hp_0619, `--spread-belief-coef 0.05`, reviewed @26.4M 2026-06-20, adversarially verified). Supervision WORKS for OVERALL fit — `spread_mae 37→17.5`, `spread_loss 0.135→0.030` both monotonically falling. **BUT `spread_largest_bias` STALLED at ~−13 — and a 2026-06-20 adversarially-verified workflow proved that ~−13 is the IRREDUCIBLE FLOOR for this head class, NOT under-fitting.** The metric (instrumented_ppo.py:499-501) is NOT an order statistic max(believed)−max(true); it is the signed `believed[argmax_TRUE] − true[argmax_TRUE]` mean over slots (the bias ON the genuinely-largest stat). `believed = prior_mean + delta·prior_std`, a per-stat POINT estimate. Conditioning on the realized-largest stat = a winner's-curse/Jensen selection effect: the stat that wins argmax is selected high, so any central-tendency (smooth_l1→median) point estimate sits systematically below it. A PERFECT per-stat point head scores **−13.9** on this exact metric (re-derived from build_opp_spread_prior) ≈ the observed −13 → only ~1pt headroom, NOT ~5. NATURES are ~HALF the floor (~−6.7 of −13.9): ±10% boost on ~55% of sets' peak stat (+~30 pts), inflates peak-stat std ×1.5, right-skews it; the head can't commit to "boosted" without seeing the hidden nature → hedges low. Natures-OFF floor still ~−8 (EV concentration + argmax selection); natures roughly DOUBLE it. spread_mae stays low (~17) because averaging over 5 stats (4 low-variance un-invested) dilutes the peak-stat error (~19–21) ~1.5×.
**CORRECTION to the prior lever (higher coef / largest-stat-weighted loss = WRONG, won't help — head's at the floor; and driving largest_bias→0 on a point estimate is the WRONG target, it'd force over-prediction → worse MAE/calibration).** The metric is a DIAGNOSTIC of the point-estimate limitation. The ONLY fix the evidence supports: make the spread belief DISTRIBUTIONAL / CATEGORICAL (predict the (nature, EV-investment) modes or a per-stat distribution) so the DamageOperator MARGINALIZES E[damage]/E[outspeed] over the spread uncertainty instead of consuming one de-biased stat. Not blocking — run healthy, +290 ELO over the no-coef predecessor; supervision still helps OVERALL fit (mae 37→17.5). This is the one new-head metric that is at-floor, by design, not broken.
