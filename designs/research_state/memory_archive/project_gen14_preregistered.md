---
name: project_gen14_preregistered
description: "PRE-REGISTERED gen-14: the TurnDelta frame deletion + unconditionalizing gen-13's proven riders. Edit only with new evidence, and say so."
metadata: 
  node_type: memory
  type: project
  originSessionId: 1046b1d6-7bc6-429f-9028-1d78fdc77677
  modified: 2026-08-17T00:00:45.415Z
---

**PRE-REGISTERED 2026-08-16, before gen-13 launches.** Written ahead of the numbers on purpose —
the concat-deletion precedent. **Edit only with NEW EVIDENCE, and say explicitly that you did.**
Gen-13's plan: [[project_gen13_launch_plan]].

Gated on gen-13's own end-of-run battery — same command, `--ref` = gen-13:
```bash
python -m main.endofrun models/<gen13-run> --ref models/ai_v9_14_gen12_h_entitypool_shaping_0816
```

## 1. THE FRAME DELETION — the headline, and the reason gen-14 exists

The **7x159 TurnDelta lag frames + the prev-turn action mask leave the obs (−1124 dims)** — IF the
H-B event seats carried (usage audit at or above the bar). This is the "history on entities"
end-state marker.

**Retrain-class, and NOTHING else behavioral rides that generation.** This is the one NON-zero-init
deletion in the whole plan, which is exactly why it could not share a generation with gen-13's H-B
enable: if the enable and the deletion land together, a regression is unattributable between them
(`design_history_entity.md` row-2).

## 2. Unconditionalize gen-13's proven riders, and delete their legacy branches

Each ONLY if gen-13 adopted it AND the battery shows no regression attributable to it:
- **`--op-drop-renders`** becomes the only path → delete the render flat-append code + shrink decode.
- **`--op-believed-lean`** → delete the de-timid else-branch.
- **`--item-belief`** → delete the static `SPECIES_CB_PRIOR` branch.

## 3. Trim the dead consequence-edge families

Drop **c1 / c3 / c5 / x** from the family string, then delete their kernels. Add **c2 / c4** only
if the G3 verdict says the CELLS carry the mechanics.

## 4. Re-read d3's ablation arm AFTER believed-lean

`design_opponent_intent` §7a(3): **a channel carrying DISTORTED content also reads low.** So d3's
low reading may have been the de-timid fiction, not irrelevance — believed-lean may REVIVE it,
which would mean **KEEP d3**, not delete it. Do not condemn d3 on the pre-lean number.

Related: [[project_gen13_launch_plan]], [[project_current_run]], [[feedback_elo_reading_rules]].
