---
name: project-battle-reconstruction
description: "Battle reconstruction + falsifier ALL SHIPPED: Phase-0 (0f30c2e) + Phase-1 `falsify` (2236b76) 2026-06-11; Phase-2 `falsify-scan` + Phase-3 `calibration` probe + sharded-eval discovery fix (2497b9d) 2026-06-12 — run-level loss-crater map: aleatoric/unattributed/proven-policy BRACKET (critic_headroom = UPPER BOUND, not a measurement)"
metadata: 
  node_type: memory
  type: project
  originSessionId: b7d3c1d7-0aa2-4875-9183-6e9491acdea9
---

> **Archived 2026-09-08** — reconstruction + falsifier SHIPPED; src/main/prober/CLAUDE.md is the always-current doc of record. Preserved verbatim; nothing below is current.

**Phase-0 reconstruction layer SHIPPED to main 2026-06-11 (`0f30c2e`)**; the live ai_v5_6 run picks it up at its next 3h launcher restart (`--sync-to-main`). Round-trip proof: **1124/1124 decisions bit-identical incl. eval's concurrent mode (concurrency=3)**. The probe-agnostic foundation for counterfactual analysis (luck-vs-mistake falsifier, distributional-critic validation, policy what-ifs):

- **Capture:** `local_sim_bridge.js` emits `__RECON__` (resolved seed + both teams + `inputLog` + RAW command log) before `__END__`. Raw commands matter: the sim's `inputLog` drops REFUSED choices (the `[Unavailable choice]` maybe-trapped probe), but their |error|/re-request round is part of the agent's protocol — raw replay regenerates it. Joined to eval forensic traces by battle tag (`utils/bridge/reconstruction.py` bounded registry; ordering: `__RECON__` arrives AFTER the |win|-chunk trace write) → sibling `*_reconstruction.json`. `BridgeSession` keeps a single-slot `last_recon` only (registry × 64 env workers = memory). `states.npz` gained an `actions` array.
- **Replay/re-roll:** `replay_driver.js` (batch JSON-over-stdio). `replay_battle` = byte-identical per-side streams **modulo `|t:|` wall-clock lines** (in poke-env `MESSAGES_TO_IGNORE`, obs-invisible — the one nondeterminism found). `reroll_turn(record, t, seeds, p1_action/p2_action ∈ recorded|random|explicit, followup)` swaps `battle.prng` at start of turn t (~10–30 ms/seed, prefix rebuild; serializeBattle clone = deferred 2-3× optimization).
- **Obs wall:** `agents/training/obs_materializer.py` consumes ONLY per-side chunks, mirrors EvalRLPlayer cadence (stall→embed→zero-mask defer→advance with npz actions). **Round-trip proof: 463/463 decisions bit-identical** (`obs_roundtrip_fuzz_test.py`); one extra row past the last action is valid = the V(s′) row a re-roll consumer needs.
- **Team decode (6683df9):** review tooling reads teams ONLY via `record.team_details(side)` / `decode_packed_team` — the one decode home (poke-env parser + sim ALIAS resolution; a pool export can say `wisp`, canonical is `willowisp` — raw parse_packed_team does NOT resolve aliases and silently breaks id joins). Validated vs the sim's `Teams.unpack` over the ENTIRE pool (719 teams/4314 mons, `packed_team_decode_integration_test.py`). Replay/re-roll never decode (verbatim strings → true EVs/IVs/HP-types in counterfactuals).

**Why:** answers "was that loss-crater dice or a reducible mistake" requires re-rolling recorded turns and scoring counterfactual states with the real one-sided obs — the saved obs vector can't do it (trackers not invertible, counterfactual states never happened). Hard requirement honored structurally: omniscient record never touches the obs pipeline.

**Phase 1 SHIPPED 2026-06-11 (`2236b76`):** `python -m main.prober.query falsify <battle> [--inv N|--worst K] [--seeds 40] [--alts 3]` (`src/main/prober/falsifier.py` + `ProbeSession.falsify`). Per anchored move decision: (1) LUCK = fix-both re-roll under N fresh seeds, realized line via the special `"original"` seed (no PRNG swap + exact recorded follow-ups) → `luck_percentile`; (2) MISTAKE = top-k alts by saved logits under the SAME seeds (paired/common-random-numbers → tight SE), idx→sim-choice via `obs_materializer(map_actions_at=i)` (the real mapper); metric = omniscient material margin (alive diff + hp-frac diff — analysis side of the wall); verdicts LUCK/MISTAKE/MIXED/NEUTRAL (adv>0.5 material AND >1.96·SE; pctl≤0.20). Refused alts (maybe-trapped) detected from `[Unavailable choice]` in one-sided suffix, excluded. Driver gained `"original"` seed + recorded-QUEUE source (refused-then-corrected replays faithfully); routing invariant: one 'move' request per side per turn → source, 'switch' follow-ups → followup policy. Validated: 14 unit + 2 integration tests, deterministic on re-run; random-policy demo correctly reads MISTAKE (hydropump +0.89±0.11 over switch). ~5-10s/decision @40 seeds. Design: `designs/ai_v5/design_falsifier_phase1.md`.

**Phase-2/3 + discovery fix SHIPPED to main 2026-06-12 (`2497b9d`); 2240 unit tests green on the rebased tree.** Phase-2 run-level `falsify-scan`:** `python -m main.prober.query falsify-scan <run> [--opponent --limit 20 --worst 2 --seeds 32 --concurrency N]` + `ProbeSession.falsify_scan`. Walks every loss with a `*_reconstruction.json`, runs `falsify_battle` on its worst δ-craters, aggregates verdicts **weighted by |anchor δ|** (new `falsifier.anchor_deltas` map; `select_anchors` ranks over it; `falsify_battle` annotates each decision `anchor_delta`). Bounded `ThreadPoolExecutor` across battles (default 1 — Node-spawn contends w/ live training). 117 prober unit + 3 bridge integration tests; worktree needed `git submodule update --init` THEN the dist/node_modules symlinks (order matters — symlinks-first blocks the submodule clone).

**KEY LESSON (adversarial review caught my over-claim — honesty-over-flattery in action):** the aleatoric/epistemic split is only PARTIALLY identifiable from a model-free, material-margin, single-turn, MEAN-only re-roll. **MISTAKE (`policy_reducible`) is the ONLY proven leg.** LUCK (`aleatoric`) is provable as dice-on-the-chosen-line but reducible ONLY via a risk-SENSITIVE policy (not built) + a lower-variance alt the mean-search can't even see. NEUTRAL is **`unattributed`** (renamed from my wrong "critic_mean") — "not unlucky + no better action found by a shallow sweep" is NOT "critic mean wrong"; equally a genuinely-lost position the critic valued correctly. So `gate.critic_headroom_upper_bound` = LUCK+NEUTRAL is an **UPPER BOUND, biased upward by construction** (weaker search → mass into LUCK/NEUTRAL), shipped with a `caveats` list + both `weighted_shares` & `count_shares`. Don't let a forensic gate's headline drive a build decision without its caveats.

**Phase-3 `calibration` probe BUILT + RAN + SHIPPED (2026-06-12, `2497b9d`).** `prober.query calibration <run> [--concurrency 8 --step N]` + `ProbeSession.calibration`. Splits falsify_scan's `unattributed` into `critic_overvalued` (epistemic) vs `lost_position` via recorded V(s) vs realized return G(s)=Σγ^k r (model-free — recorded V; falsify pass at concurrency 8). Pure helpers `_discounted_returns`/`_reliability_curve`/`_reliability_gap_at`/`_calibration_stats`. **ALSO fixed a real pre-existing discovery bug**: `_FNAME_RE` didn't match the sharded eval naming `<outcome>_s<shard>_<idx>` → the WHOLE prober (scan/triage/falsify) was blind to every recent run (outcome parsed `?`); fixed + shard folds into `index`.

**KEY FINDING (first real run, ai_v5_11 @30M, conc 8): the cheap model-free calibration is SELECTION-CONFOUNDED beyond usefulness, and the tool now self-diagnoses it.** Naive "V−G on losses" is biased + by construction (losses ARE the below-V tail). Measured: bias_on_wins −18 / bias_on_losses +35 / captured win-frac 0.42 — the TEXTBOOK CALIBRATED-critic signature (E[V−G]≈P(win)·(−18)+P(loss)·(+35)≈0 at true WR~66%, and the model beats bots well above that). So the reliability curve over the QUOTA-captured (loss-heavy) sample is not a valid baseline; the "~34% critic-mean-reducible" is mostly the capture-quota artifact, NOT real over-valuation. The critic is likely WELL-CALIBRATED. → output surfaces bias_on_wins/bias_on_losses/captured_win_fraction so the confound is visible in-data; critic_mean_reducible_upper_bound flagged LOOSE.

**How to apply:** the model-free reliability proxy is insufficient → the trustworthy resolution is the **gold-standard re-roll → POLICY-ROLLOUT to terminal → return-distribution PIT** (selection-free by construction; needs a NEW mid-game rollout primitive — reconstruct to turn t then drive the BattleStream with the model+bot to terminal, ~Phase-4) — which is ALSO the distributional-critic's own validator (predicted quantiles vs replayed reality). Cheaper interim: reweight the reliability curve to the true per-opponent win rate (needs the true WR threaded in). Phase-0 design: `designs/ai_v5/design_battle_reconstruction.md`. Pairs with [[project_local_sim_bridge]]; motivated by [[project_plateau_diagnosis_2026_06_09]]; gates [[project_model_frontier_roadmap]]'s distributional-critic lever (which now looks LESS urgent — the critic may be calibrated; the bigger crater levers are policy [28% PROVEN mistakes] + aleatoric).
