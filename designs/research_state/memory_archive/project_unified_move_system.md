---
name: project_unified_move_system
description: "2026-06-16 unified move system BUILT (config v24, gen3_unified_move_system_v1) — NOT run/shipped; honesty gate + unification backlog + 2 named rough edges"
metadata: 
  node_type: memory
  type: project
  originSessionId: 53090569-5f88-4eb3-b190-637a81b6e8e6
---

> **Archived 2026-09-08** — v24/v25 build record; the move system is production and stated in designs/ARCHITECTURE.md. Preserved verbatim; nothing below is current.

BUILT 2026-06-16 (worktree branch, config v24 `gen3_unified_move_system_v1`, NOT shipped, NOT run). Three stages on the [[project_gpu_damage_op]] backbone:
1. **Data** — extracted per-move secondary effects `{chance,status,volatile,boosts}` + priority/drain/recoil into `gen3_moves.json` (reverses the old "secondary status is incidental" decision) + the `gen3_data.moves` facade (`secondary_chance()`). GPU-side only → obs golden byte-identical.
2. **Op effects** — `DamageOperator` effect block enriched with per-status SECONDARY probabilities: incoming (opp threat, accuracy-folded, ×Serene Grace) + per-OUR-move outgoing (×our Serene Grace, ×opp Shield Dust). NO speed coupling on flinch (owner: leave to attention). Intrinsic to `--damage-op` (no flag); incoming_dim 78→88, outgoing 17→57. Serene Grace is a REAL lever (gen3 Jirachi ~25% OU; gen4 Jirachi is Ubers).
3. **Move latent + grading** — `MoveLatentEncoder` (context-free `MLP(move_emb⊕type_emb⊕MOVE_ATTR)`) into the move network (`--move-latent`, structural); latent_table is the Stage-3 grading target. `_move_belief_latent_loss` (`--move-belief-latent-coef`) cosine-grades the predicted move distribution's expected latent vs the true moveset's mean latent so **Rock Slide ≈ HP Rock** (near-move cosine >0.9 vs far <0.2). Leak-safe. Umbrella `--unified-moves {off,incoming,both}`.

Verified: 2641 unit + prober 190 + full-stack bridge smoke roundtrip PASSED. Adversarial review (13 agents, 4 lenses, verify pass): 3/9 confirmed, all FIXED — (1) MAJOR `build_move_attr` missing the HP num-237 skip → row 237 got hiddenpowerwater's attrs order-dependently (now all-zero, matching `build_move_type_idx`); (2) VICReg floors MAGNITUDE not ANGULAR collapse for the cosine objective — real guard is the move-net task-anchoring, NOT VICReg (doc-corrected; verify proved full collapse isn't a reachable attractor → minor); (3) `snapshot_test` didn't pin the new `arch_toggles` keys (added + a subset-vs-current_model_version guard). v24 ALSO back-filled the v23 `arch_toggles_from_model` gap (damage_outgoing/move_candidate_floor missing → would FATAL a `--unified-damage both` self-play run).

**HONESTY GATE (the recurring lesson):** it LEARNS but UNMEASURED if it HELPS the policy → fresh-run A/B (belief precision↑ AND surprise-secondary/wrong-effect crater share↓ via `falsify-scan` AND wr non-regress). Risk = learnable-but-inconsequential (the incoming-belief precedent). Run via `--unified-moves incoming` (swap for the prior `--unified-damage incoming`).

**User feedback 2026-06-16:** model now plays with "more human-style intuition" (replays). Two named rough edges → v2 op follow-ups (in the design doc backlog): (a) **Seismic Toss into Ghosts** — fixed-damage moves (BP 0) aren't priced + their Normal-type immunity isn't surfaced → model fixed-damage in the op (level-HP, type-gated to 0 vs immune); (b) **speed+status** — `p_outspeed` ignores paralysis (gen3 ×0.25 speed) + boosts; the op now KNOWS which moves paralyze (new secondary data) but doesn't consume the result.

**UNIFICATION BACKLOG** (redundancies this work exposed; in `designs/ai_v6/design_unified_move_system.md`): (1) `MOVE_EFFECT_FLAGS` ⊂ `MOVE_ATTR` (cheap DRY — add 1 primary_status col, drop the buffer); (2) static move facts TRIPLE-encoded (obs move slot + reactive move-effect block + MOVE_ATTR) — source once via the latent, keep only dynamic fields in obs (big, A/B-gated, bottleneck risk); (3) CPU incoming-damage block already A/B-redundant with the op (`--mask-incoming-damage-obs`) → delete on A/B confirm; (4) two `p_outspeed` calcs + the speed↔status disconnect. Design: `designs/ai_v6/design_unified_move_system.md`.

**v25 SPREAD/SPEED BELIEF + disable-redundant flag BUILT 2026-06-16 (config 25, gen3_unified_spread_belief_v1, NOT run/shipped):** the user's "do the same setup for speed (priors+math+adjust)" + "give me a flag that disables all redundant logic." THIRD belief leg (moves✓ species✓ STATS✗). (a) Data/facade ALREADY existed (`gen3_spread_priors.json` + `priors.spreads()`/`stat_distribution`). (b) `build_opp_spread_prior[n_species,5,2]` usage (mean,std) for {atk,def,spa,spd,spe} (Aerodactyl 392 spe, Blissey 50 atk/306 spd — far better than the flat constant). (c) `SpreadBelief` mirrors MoveBelief: believed=prior_mean+delta·prior_std (zero-init head→cold==prior), reinjected into revealed opp tokens, stashed last_spread_belief[B,6,5]. Predicts DERIVED stats not EVs (op stays additive). (d) DamageOperator forward+_outgoing_block consume believed atk/spa/def/spd/spe at opp_active_local, REPLACING the hand-coded 252/×1.1+neutral-0-EV constants (None→legacy, byte-identical) — the op stops reading hand-coded constants. (e) `--unified-obs` master flag: 3 ObsUnpack forward-behavior masks (incoming-damage + active-move-scalars[needs damage_outgoing] + move-effects) zero now-subsumed obs regions, clone-once, reward/PBRS untouched. v25 versioning fully threaded (4 fields, check_compatible, migration, arch_toggles to 4 sites); OFF byte-identical. Verified 2647 unit + spread_belief_test(6) + full-stack `--unified-moves both --spread-belief --unified-obs` smoke roundtrip PASSED. Adversarial review running. EhSPEED ANGLE: offensive/defensive stats get RL gradient via the op damage rolls; speed gets WEAK gradient via p_outspeed → the move-order SUPERVISION (--spread-belief-coef, flag wired, loss STAGED) is its accelerant (TurnView.we_moved_first = leak-safe inequality label, mask clean turns for priority/para). STAGED (designed not built): the speed supervision loss; MOVE_EFFECT_FLAGS→MOVE_ATTR; fixed-damage type-immunity (Seismic Toss/Ghost). Run: pure-unified = `--unified-moves both --spread-belief --unified-obs`.
