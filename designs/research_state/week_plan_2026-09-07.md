# WEEK PLAN — 2026-09-07 → 2026-09-13: validate the value network AS win probability

**Status: DRAFT for the owner's feedback (written 2026-09-06 22:00 by the orchestrator).** Once
accepted it is the week's registration: the arms, the decisions, their bars and their order. Bars
are registered here BEFORE the data; a bar changed after its data is a ledger entry, not an edit.

**The goal, in the owner's words (2026-09-06):** *understand the value network as win prob — do we
need PBRS, do we need to bootstrap with a frozen win-prob value head; right now: run a model further
and validate the win-prob head.* Long term: *a mature model, free of tech debt, understood to
benefit from both search and teacher distillation, to try to reproduce v8's gift.*

**The prior, stated:** SPARSE is the recipe of record (owner). The bootstrap must be *massively*
faster in wall time to displace it; ties go to SPARSE.

---

## 0. The week in one table

| when (PT) | arm / read | decision it feeds |
|---|---|---|
| Sun 20:27 → ~Tue | **ARM A** `ai_v12_02_winprob_critic` (SPARSE, 75M) — running | D1, D2 |
| Mon ~02:30 (10M snapshot) | **D1 — famine kill?** ladder vs `famine_comparator` at matched snapshot count, floor 38 Elo, AND bots not rising | continue A, or launch the registered gen-era-head FROZEN-φ successor |
| Mon evening → Tue 08:00 | tech-debt burn-down to 95% of weekly quota (CPU/agents only; the arm is untouched) | — |
| Tue (A at 75M) | **D2 — is the win-prob critic a good critic, and is sparse sufficient?** `main.critic_gate` with the G5 controls | the recipe of record; licenses the era flip (owner) and ARM B |
| Tue evening | **ARM B** — FRESH run, same argv as A plus `--win-prob-pbrs-frozen <A's final head>` | D3 |
| Wed–Thu (B at 10M–16M) | **D3 — is the bootstrap massively faster?** B vs A at matched snapshot count | PBRS/bootstrap needed or not; B stops early if not |
| Wed–Thu (CPU, beside B) | **D4 — does the win-prob model gain from SEARCH?** `main.search_dividend` on A's final checkpoint | the "benefits from search" half of the long-term goal |
| Thu–Sat (GPU after B) | **D5 — does the win-prob model absorb a TEACHER without robbing?** a short fold on A + a matched continuation control, ×2 | the "benefits from distillation" half; the v8-gift question on the new critic |
| Sat–Sun | write-up: `UNDERSTANDING.md` §4 rewritten from the week's verdicts; next week's goal proposed | — |

GPU budget: A 33 h + B 10–33 h + D5 ~12 h ≈ 55–78 h of the week's 168, leaving room for the kill
branch (another 33 h) or a replicate. D4 is CPU-only and runs beside B at low priority.

---

## 1. The decisions, with their bars

### D1 — Does terminal-only reward STARVE? (Mon ~02:30, the 10M snapshot)
Registered (ledger *READ AMENDMENT* 1 and 2). Kill ⇔ A trails `famine_comparator` by more than
**38 Elo** at matched snapshot count (n = 4) AND `win_rate_vs_bots` has not risen over ≥3 cycles.
The 5M smoke ran early and every input resolves. Kill ⇒ `ai_v12_03_winprob_frozenphi` as
registered (gen-era head; the self-head option does not exist on this branch). **Expectation:** no
kill — the stripped arm learned on this signal, and A's first bot cell sits inside its envelope.

### D2 — Is the win-prob critic a GOOD critic, and is sparse SUFFICIENT? (Tue, A at 75M)
One command: `python -m main.critic_gate ai_v12_02_winprob_critic --parent v9_fold_parent
--famine-comparator famine_comparator --control <the three G5 arms>`. Two questions, two bars:
- **Critic quality (G1, primary):** the head's RESOLUTION strictly beats the matched-stratum
  baseline from `winprob_critic_baseline_2026-09-06/`; G2/G3 per-stratum non-inferiority; G4 skill
  > 0; G7 stall/episode-length kill not fired.
- **Sufficiency ("is sparse within the bar"):** A's anchored ladder at matched snapshot count
  against `famine_comparator` (rev-1, the gen-era fresh 25M run). Bar: **NOT INFERIOR by more than
  the 38-Elo floor at n = 12** (the comparator's count). A is 75M and rev-1 was 25M, so at n = 12 A
  is at ~24M — the comparison is at matched COUNT, per the ELO rules.
- The untaught-meter column is DESCRIPTIVE (a fresh arm against a 28M parent).
**Outcomes:** PASS both ⇒ SPARSE is the recipe of record, the era flip is licensed (owner-gated,
`MIGRATION_FLOOR` 109), and B launches. Critic PASS but strength INFERIOR ⇒ the shaping question
becomes live and B's read matters most. Critic FAIL ⇒ the design's falsification sentence is printed
verbatim and the week pivots to the frozen-head bootstrap from the GEN-ERA head as the recovery arm.

### D3 — Do we need the bootstrap / PBRS? (Wed–Thu, B at 10–16M)
B is A's argv plus `--win-prob-pbrs-frozen <A@75M>` (actor-only potential from A's own frozen head;
no coefficient; the critic stays `V ≡ P(win)`). Under this critic the SELF-φ rung does not exist, so
this IS the PBRS question. Read: B's ladder vs A's ladder at matched snapshot count, n = 5 and n = 8.
**"Massively faster" is registered as:** B SIGNIFICANTLY above A at n = 5 (10M) by more than the
replicate floor AND still above at n = 8 (16M). Anything less ⇒ **B is stopped at n = 8** and SPARSE
stands; the bootstrap is recorded NOT NEEDED with the interval. **Floor caveat, UNVERIFIED:** no two
same-recipe fresh arms exist in this era, so the 38-Elo gen-era floor is BORROWED; if B's advantage
lands between 0 and 38 Elo the verdict is WITHIN FLOOR, which under the owner's prior reads "sparse".

### D4 — Does the win-prob model benefit from SEARCH? (Wed–Thu, CPU)
`python -m main.search_dividend` on A's final checkpoint, the defensive paired search with the
win-prob leaf (`--defensive-leaf winprob` collapses to the only leaf on this critic), `--opponents
self --side-swap`, the registered budget sweep. Bar: the search arm's win rate over the policy-only
mirror clears zero (Wilson, side-swapped). The gen-era cell read +0.0219 [+0.0089, +0.0364] on the
win-prob head with the scalar critic below it; a critic that IS P(win) should not do worse.
**Outcome:** the first of the two long-term properties, measured on the recipe of record.

### D5 — Does the win-prob model absorb a TEACHER without robbing? (Thu–Sat)
The distillation half. Fork A@75M twice: (i) a FOLD with one banked exploiter teacher in the
gen-era recipe (`--distill-target action`, `--fork-lr` at v8's dose, ~+4.45M steps), (ii) a plain
CONTINUATION at the same steps, the control the whole 2026-09-06 re-basing lesson demands. Read with
the untaught meter: taught-slice delta (gift) and untaught-slice delta (rob), fold vs continuation,
cluster-bootstrapped over teams; ×2 replicates each gives the floor. **Bar:** on-slice gift
SIGNIFICANT above the floor AND untaught delta NOT below −floor. **This is the v8-gift question
asked on the new critic**, and the gen-era result to beat is "every fold robs untaught".
**Caveat:** the teachers are gen-era exploiters (shaped critic); distillation reads their POLICY
only, so the critic mismatch is not a blocker, but it is stated.

---

## 2. What could go wrong, and what we do
- **D1 fires (kill).** The gen-era-head frozen-φ successor runs (33 h) and D2's questions move to
  it; B/D3 become "self-head vs gen-era head" and slip to the following week. The owner is notified
  once with the verdict and the sentence it rests on.
- **Throughput is lower than 2.3M steps/h.** A's 75M slips into Wednesday; B starts later; D5 loses
  a replicate before D4 loses anything (D4 is CPU).
- **The borrowed floor.** Every D3/D5 verdict is tagged with its floor's provenance; a WITHIN FLOOR
  read is the honest one and the owner's prior breaks the tie toward SPARSE.
- **Instruments new to the era** (draw rate, the Murphy family on train rollouts) have no gen-era
  comparator; they are read against the stripped arm's curve for cadence only, never for strength.

## 3. Not this week
The CLAUDE.md restructure, the era flip's execution, the TensorBoard backfill, retention — all
backlog rows, owner-gated or burn-down. No new architecture. No search-as-teacher. No human replays.
