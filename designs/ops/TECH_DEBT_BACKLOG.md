# TECH-DEBT BACKLOG

The one list of tech-debt work. Research and measurement work is NOT tracked here; it lives in
`designs/research_state/UNDERSTANDING.md` §6 and the ledger.

**Two rules (owner, 2026-09-06).** (1) During the week nothing here is dispatched on the
orchestrator's own initiative: the orchestrator ADDS rows; the owner moves a row to ACCEPTED, and an
ACCEPTED row is dispatched under [`ORCHESTRATOR_SOP.md`](ORCHESTRATOR_SOP.md) §2. (2) **Whatever
quota remains before the weekly reset is burned down on this list as a standing housekeeping
procedure** — ACCEPTED rows first, top to bottom; if ACCEPTED is empty and quota remains, the
orchestrator notifies the owner with the top PROPOSED rows and, absent a reply in 15 minutes, starts
the highest one. Read the reset time with `python3 ~/.claude/skills/usage-limits/check_usage.py`
(the weekly window resets **Tuesdays ~14:00 PT**). **Target: the weekly quota is used up to 95% by
Tuesday ~08:00 PT** (owner, 2026-09-06), leaving ~5% spare for an issue — so the burn-down is PACED to
land there (it starts Monday, earlier if the backlog is large) and STOPS at 95%: burn-down agents run
under the usage gate at `--weekly-threshold 95`, never higher. The live arm's needs always come first;
the burn-down uses what the arm does not.

**Ordering:** by CRITICALITY tier, then by size within a tier (small first, so a partial burn-down
still closes rows). Tiers: **P0** — a GIGO class: could make a measurement or a test lie ·
**P1** — blocks or slows correct work (a gate near its bound, a tool that will vanish, a doc every
session loads) · **P2** — hygiene and cost. Each row carries its size and its acceptance test; a row
is DONE when that test is in the routine suite or its artifact is on main, and moves to §3 with the
commit.

---

## 1. ACCEPTED

| tier | item | why | size | done when |
|---|---|---|---|---|

*(Empty as of 2026-09-07 — both accepted rows landed the same day: the mode-flag doc gate and
the anti-vacuity stub audit; see §3. Per the rule above, with ACCEPTED empty and quota
remaining, the orchestrator notifies the owner with the top PROPOSED rows and, absent a reply
in 15 minutes, starts the highest one.)*

## 2. PROPOSED — the orchestrator adds rows; only the owner moves one to §1 (or the week-end burn-down reaches it)

| tier | item | why | size |
|---|---|---|---|
| P1 | Era-boundary flip (`designs/research_state/era_boundary_deprecation_2026-09-06.md`; `MIGRATION_FLOOR` 109) — **licensed by the owner once the SPARSE arm proves out; not before** | frees 6 flag families / 11 flags; retires the shaped-critic scaffolding; the untaught meter's opponent must be re-measured on the same checkpoints first | M; gated on the arm |
| P1 | **129 git worktrees, 13 detached** — most are finished agents' or past runs' pinned checkouts; review and remove the dead ones (a run's pin is now a `/tmp/launcher-<sha>-*` tree, so the old `gen*-run-*` worktrees may be unreferenced — VERIFY against every live and resumable run before removing). **Census done 2026-09-07, report at `designs/research_state/measurements/tech_debt_census_2026-09-07/worktrees.md`: 77 worktrees — 17 leftover worktrees removable (7 stale + 10 from today's sessions, 3.9 GB), 55 need a decision (18 RESUMABLE at 16.5 GB + 37 DIRTY-UNMERGED), 4 LIVE. Every pin commit is an ancestor of `main`, so removing a pinned tree cannot orphan a resume.** | disk, submodule checkouts, and `git worktree list` is unreadable | S, reviewed |
| P1 | Lineage backfill: 105 legacy runs carry a parent DERIVED from `original_command`, and one is provably wrong (`ai_v8_01_zarch_film_0717` claims fresh while its curves start at 148M); `python -m main.lineage --backfill` reviewed run by run — a PREREQUISITE for the TensorBoard backfill row below. **Review done 2026-09-07, report at `designs/research_state/measurements/tech_debt_census_2026-09-07/lineage_review.md`: 161 consistent, 1 contradicted, 0 undecidable over the 162 derived-parent runs. `--backfill` CANNOT repair the contradicted row — the block is immutable and the derivation has no argv to read; it needs a hand edit. `main.lineage`'s own `derived` flag read FALSE for the 47 derived-`fresh` rows — reader defect fixed `6f6cfe73` (it now reads the block's own key: archive count 115 → 162, 0 lost, nothing written under `models/`); the CONTRADICTED row's hand edit remains the owner's.** | a wrong parent poisons every tool that walks ancestry | S, manual |
| P2 | Split the two giant leaves — `src/agents/training/CLAUDE.md` (7,503 lines at census) and `src/rust_sim/CLAUDE.md` (8,436 at census) — detail to the owning `designs/` tree, narrative to `research_state/claude_md_archive/` (census `claude_md_census_2026-09-06.md`). **BOTH HALVES DONE 2026-09-08.** Rust half `244431af`: 2,659 → 791 lines / 329 KB → 60 KB, seven topic docs under `designs/rust_sim/` + the rewritten-passage archive; the 55 ROUNDs were already in `port_build_log.md` from the 2026-09-07 pass, so 0 new rounds were appended. Training half `b51c2ac3`: 3,183 → 740 lines / 264 KB → 59 KB, nine new topic docs under `designs/training/` plus merges into four existing ones, two history files in `claude_md_archive/`. | ~400k tokens loaded when working in either area | L; DONE |
| P2 | The critic gate's G5 / G6 / G8 criteria print NOT RUNNABLE on every report (design §6 gaps) — research-INSTRUMENT debt: build the instruments or retire the criteria from the design | a gate with unrunnable criteria quietly becomes the runnable ones | M, research-gated |
| P2 | **The search-teacher's SELECTION half falsify-gates on NODE even under `--use-bridge rust`.** `selection.select_candidates` calls `falsifier.falsify_battle` with no `impl=`, so the re-rolls that decide which craters are reducible MISTAKEs run on the node driver whatever the run's engine is — while the search/confirm workers run on rust and the composition gate asserts exactly that about them. Found 2026-09-08 while moving selection into its own child; deliberately NOT fixed in the same pass, because changing the engine changes which craters survive the gate and would have voided that pass's identical-selection proof. Fix: thread `impl` from the callback through the select worker's config into `falsify_battle`, and A/B the selected list on one trace directory. | one half of a rust run's teacher is silently on node; it is also why the select child's config is `select_config.json` rather than `config_*.json` (the gate would otherwise assert something false) | S |
| P2 | Test-tier DURATIONS were last measured 2026-08-14 on an idle box; counts re-measured since but durations not | the routine-gate timing in CLAUDE.md is a month stale | XS, needs an idle box |
| P2 | **Three explicit-only `designs/` docs would mislead a reader who trusts their own framing** (census: `designs/research_state/measurements/docs_staleness_census_2026-09-08.md`). `model.md` calls itself "what the current model is" at a 2026-06-17 state table — signature, obs dim, config version, live run and the constants SoT path are all wrong, and it calls `WinProbHead` a side readout "never in pi/vf" when it IS the critic. `design_pathologies.md` instructs "review this before every retrain" over an ai_v4 fixed-bot run from May. `pubval_deletion_decision.md` opens "Status: NOT DELETED" for a subsystem deleted at v88. Fix is one header line each (retire-with-pointer / date-stamp the era / record the decision) | explicit-only, so no pass can fix them without being asked; each one costs a reader a wrong turn | XS each, owner-gated |
| P3 | Closed `ai_vN/` chapters carry no in-folder history marker — only `ai_v3/` has one (`README.md`). `designs/CLAUDE.md`'s version map labels them, but a search result lands a reader inside `designs/ai_v6/design_*.md` with no signal the era is closed | a one-paragraph `README.md` per closed chapter kills the whole class | S, docs-only |
| P3 | `src/agents/observation/species.py:45` raises `Update data/mappings/gen3_mapping.json` — a path that has not existed for months; the mapping is `data/pokemon/gen3_species.json` (`state_encoder.py:109`). Left unfixed by the 2026-09-08 docs pass because it is a runtime string literal, not a comment | the error message sends whoever hits it to a file that is not there | XS |
| P1 | **A green test is pinning a stale doc to a value the shipped era does not use.** `src/main/launch_runbook_test.py:128` asserts `$CLEAN` carries `--draw-penalty -1.0`, with the rationale "draw must equal a LOSS"; both shipped ai_v12 runs and `designs/baselines.json`'s `production.config_overrides` pin **0.0**, and `designs/ai_v12/launch_runbook.md` is itself SUPERSEDED (census 2026-09-08). Decide which is right — then fix the runbook and the test together, or record why the runbook's arms differ from the shipped arm | the gate reads as protection and is currently holding a wrong value in place | S; needs the owner's call on which value is correct |
| P2 | **The freshness gate's scan stops at `CLAUDE.md`.** `src/claude_md_freshness_gate_test.py` validates repo-relative paths and `--flags` only inside `CLAUDE.md` files, so `designs/ai_v12/*.md` and `designs/learning/*.md` carry ~10 deleted flags (the whole zarch/FiLM family, `--pubval-*`, `--threat-prob-outspeed`) and two dangling `CLAUDE.md`-section pointers. Extending the scan to those two trees is a small change | the same class the gate exists to kill, one directory out | S; will FAIL the tree on the first run, so it lands with the fixes or with an allowlist |
| P2 | **`designs/ai_v12/` — the LIVE chapter — misdescribes the running experiment** (census 2026-09-08). `design_winprob_only_critic.md` (the design of record) opens "Nothing here is built and nothing is sanctioned to run" 68M steps in, its §1 "WHAT IS TRUE NOW" is the pre-migration config, and its §5.1 claims an `ARCH_SIGNATURE` bump that never happened. `launch_runbook.md` calls itself "the thing the training session executes from" while its three arms never launched and it names the dead `ai_v12_01` as live. `todo.md` names the wrong design of record | an agent orienting on the live era reads the pre-pivot world | M, owner-gated (explicit-only docs) |
| P3 | **`designs/learning/`: 3 notes SUPERSEDED, 15 partly stale** (census 2026-09-08). The three — `self_discovered_archetype_latent.md`, `conditioning_architectures.md`, `regularization_and_noise_in_ppo.md` — are present-tense about the z_arch/FiLM family deleted at v78; `conditioning_architectures.md` is the worst because its two siblings carry a correction banner and it does not. The fifteen are the 09-06 critic pivot (7), a shipped thing still called unbuilt (4) and moved numbers (4, incl. `entity_tokens_biases_pointers.md`'s "fifteen edge families" — 17 now — and obs 2925 — 2501 now). `negative_transfer_and_shared_functions.md`'s TL;DR still calls a CLOSED hypothesis "under test" while its own body records the refutation | the notes teach; a wrong dated line teaches the wrong thing | M; one dated banner each, owner-gated |
| P3 | **`src/rust_sim/README.md` duplicates `designs/rust_sim/port_build_log.md` wholesale** — a ~430-line append-only status blockquote. That duplication caused nearly every defect the 2026-09-08 README audit found: the same e2e figure lived in 3-4 places with 3 different values. Cut it to ~15 lines of present truth plus pointers; keep the golden-regeneration chapters and the fuzzer runbook, which are unique. Same shape, smaller: `src/utils/bridge/README.md` (758 lines, in the size gate's reported band) re-narrates ~90 lines the rust leaf owns, and 4 of its 10 defects were in exactly that overlap | a number in two places is a number that will disagree | M |
| P3 | Stale counts in code headers found by the 2026-09-08 audit and left alone as out-of-scope: `src/rust_sim/CLAUDE.md` states protocol emission as 132 battles / 19348 lines (measured **138 / 22737**); `harness/ab_fuzz.js` says 22 teams, `ou_random_teams.js` 722, `gen_e2e_fuzz.js` 770 (pool is **762 valid of 813 files**); `tools/CLAUDE.md` says both downloaders key `teams.json` by `sha256(team_text)` — true only of `others_team_downloader`, the sample one keys by PokePaste id; `static_blocks_feature_test.py` and `trapping_signals_fuzz_test.py` docstrings contradict each other on whether the trapped/maybe_trapped reactive scalars still exist (both pass, so one is stale) | each is one edit; together they are the same drift the census is about | S each |
| P3 | `scripts/ops/README.md` claims "every one prints its contract on `--help`" — `main.ops.run_ref` has no argparse and no `__main__`; `-m main.ops.run_ref --help` prints nothing and exits 0. It is library code, as the README's own table row says. Landed 2026-09-07, so left for its author | a contributor following the sentence gets silence | XS |
| P2 | `models/` retention apply (`archive_grooming_dryrun.py --policy tiered --apply`, 87.9 GB) | dry-run only so far; disk at 43% so not urgent; **owner-run** | owner |
| P2 | **`entity_seats_test` and `scaffolding_gauge_test` FLAKE under `-n 2` on a loaded box** (2026-09-08, rust-leaf landing: both failed once under a training run + a concurrent agent, both passed serially and on retry) — find WHICH of the two causes it is and fix it | a test that flakes under contention is a timeout or shared-state defect, not noise; left alone it teaches the next agent to re-run a red gate instead of reading it | S |

| P1 | **Rust sim gen3 parity beyond OU — remaining gaps (ranked).** Census + method: `designs/rust_sim/gen3_coverage_census_2026-09-08.md`; recount with `PROBE_KIND=move ./target/release/scan_move_probe`, never the JS mirror. **State after ROUNDS 57-59: moves 321/369 (48 fail-loud); abilities 76/76 and species 392/392 CLOSED; items 102/106.** Ranked by legal learners (the only usage data committed is gen3 **OU**, and OU is the axis this campaign exists to see past — every remaining move is <0.05% of OU move-slot mass). **(1) ATTRACT, 338 — NOT a move round: blocked by the construction-time gender `sample(['M','F'])` that `start_with_switchins` does not model; spec already settled in `harness/probe_attract_move.js`, prerequisite is a construction round that moves every committed golden's seed (ROUND 59b).** (2) SNORE 372 + NIGHTMARE 69 — the sleep-interaction pair; Snore needs the asleep-only `onTry` + the sleepUsable cant-then-proceed. (3) TWO-TURN CHARGE 318 over 7 (dig 158 / dive 76 / fly 39 / skyattack 27 / skullbash 7 / razorwind 7 / bounce 4) — needs the semi-invulnerable `onInvulnerability` half; the charge machinery exists for Solar Beam. (4) MISC UTILITY 166 over 11 (focusenergy DONE; mist 20, spite 16, ingrain 13, teleport 10, followme 9, grudge 7, magiccoat 5, roleplay 5, camouflage 1, helpinghand 40). (5) IDENTIFY / LOCK-ON 71 over 4. (6) FIELD SPORT 48 (mudsport / watersport). (7) VARIABLE-BP 47 over 5 (bide / magnitude / psywave / present / triplekick). (8) STOCKPILE 36 over 3 (one shared counter). (9) OHKO 32 over 4. (10) RECHARGE trio 6 — `blastburn`/`hydrocannon`/`frenzyplant`, cheap and STRUCTURAL: `turn/moves.rs` hardcodes `move_id == "hyperbeam"`, so widening it to the dex `recharge` flag kills the "only X is modeled" class. **(11) MOVE-CALLERS 84 over 5 (metronome / mirrormove / naturepower / assist / sketch) — must be LAST: each dispatches ANOTHER move, so its correctness is a function of the whole rest of the census.** Also open: the 4 fail-loud ITEMS (`machobrace` is the DRAW-relevant one — `onModifySpe` halves Speed, so a silent miss desyncs the speed-tie shuffle), and **36 implemented moves that no committed battle golden has ever executed** (the census's tier D, corrected — the first tier table said 0 because it counted a `dex_golden.txt` row as battle exposure) | the owner's 2026-09-08 directive is "we believe we have all gen3 OU, but let's drive all gen3 if we can"; three rounds landed 9 moves and found 2 live bugs in a SHIPPED move on the way, so the remaining tail is worth the same treatment | M per family; (10) is S, (1) and (3) are L |
| P1 | **`critic_gate` / `snapshot_ladder` matched-count slice reads the SURVIVING pool (groomed to 20), not every step ever rated** — at run end "the arm's first 12" became 36M→58M vs the comparator's 2M→24M (D2 VERDICT 2026-09-08, ledger `6c4ae35d`). Fix: refit from the append-only `games.jsonl` over ALL rated steps for the matched-count comparison; refuse a run-end matched-count read on a groomed pool until then | an instrument that slices an artifact must know what the artifact's owner deleted — the run-end strength read on arm A is registered INVALID as a like-for-like because of this | M |
| P2 | **`critic_gate` G7 uses the parent's worst eval cycle as the era reference, not amendment 6's arm-own 4–6M window × 1.25** (`48747444`) — wire amendment 6 into the tool; neither reference was breached on arm A (D2 VERDICT 2026-09-08) | the gate reports against a reference the amendment replaced, so a future breach could be judged by the wrong bar | S |

## 3. DONE

| item | commit |
|---|---|
| **TensorBoard fork-prefix backfill** (`python -m main.tb_inherit --backfill --all --apply`) — owner-authorized 2026-09-08, run by the orchestrator from the main checkout at nice 10: dry run then apply, **136 written, 0 skipped, 0 failed**; the one fork left without a prefix is `ai_v8_01_zarch_film_0717` (claims `fresh` in an immutable block — owner decision). Prerequisite: the 2026-09-07 lineage review (105 derived parents CONSISTENT) | no commit — an archive operation; ledger 2026-09-08 *TensorBoard fork-prefix backfill APPLIED* |
| **The search-teacher's candidate SELECTION ran INLINE in `_on_step`** — the callback was advertised as *"non-blocking (subprocess workers)"* in its own docstring, in `src/agents/training/CLAUDE.md` and in the `--use-bridge=rust` startup banner, while one half of every cycle blocked the training loop. **MEASURED, not estimated: 48.1 s over 9 loss traces and 350.2 s over the default 60** (`scan_limit`), on a copied 123-trace archive — ~5.5 s per trace, so the cost scales with the thing a long run has most of. A cycle is now **two phases and both are children** (`_pending["phase"]`): `main.search_teacher_select_worker` runs `select_for_mode` and publishes `teacher_cycle/candidates.json` atomically, then the search/confirm workers spawn as before. Verified pure-over-files first (selection's `ProbeSession` loads no model and reads only `eval_traces/` + the reconstruction siblings), which is what makes a child the right shape rather than a thread. **The selected list is IDENTICAL** old vs new at `scan_limit` 9 (7 candidates) and 60 (58) — the acceptance test, run before the docs were written. Training-step cost: **0.26 s for a whole 30,000-step run** (worst single step: select-launch 3.9 ms, worker-spawn 83.5 ms — that one is `model.save`, the freeze the cycle always paid — collect 1.0 ms), recorded as `teacher/step_block_ms` / `_total` and ASSERTED by the composition gate against a bound (5 s x contention scale) two orders of magnitude above the measurement and two below the defect. A **failed selection is now a REPORTED zero-yield cycle** — the `selection failed` marker AND `status={'error:selection': 1}` in the same per-cycle histogram a worker crash uses, plus `teacher/selection_failures_total` — where it used to be a bare `except` and an optional print. `scan_limit` gets its flag, **`--teacher-scan-limit`** (config **v113**, the `capacity_telemetry` class: recorded, `_resolve`-inherited on a flagless resume, never gated), because 60 was hard-coded and is simultaneously the selection half's COST and its SUPPLY. **The composition gate caught a regression in this change on its first run** — the cycle-dir wipe, harmless when the worker configs followed it microseconds later, now ran a whole selection earlier and left a run that ended mid-selection with no `config_*.json` at all; the wipe is now per PHASE and a unit test pins it. The gate was re-sized to **30,000 steps** (the trainer no longer stalls, so a cycle spans ~4,800 steps instead of ~2,200) and runs 2.4x the steps in 74% of the old wall clock: **472 s, 4-6 cycles, 7-8 corrections**, against 638 s / 18,000 steps before. ONE finding recorded not fixed: selection's re-rolls are on node even under `--use-bridge rust` (§2) | `e22c1236` (2026-09-08) |
| **A full multi-cycle search-teacher run end-to-end on the rust bridge is GATED** — `src/main/train/search_teacher_composition_test.py` (`gen3_search_teacher_composition_rust_v1`, `sim` + `slow`, ~11 min). Every LEG was already gated on rust; the COMPOSITION was not, and the row read *"the first such run will find the seam"*. **It found no seam** — the real `train_rl_agent.py`, launched as a subprocess at smoke scale on `--use-bridge rust`, runs multiple cycles that select, search, confirm, shard, buffer and FOLD. Asserted on ARTIFACTS: >= 2 **COLLECTED** cycles, each worker config's `"impl": "rust"`, no `worker_no_shard` / `error:*` in the status histogram, no hung-cycle abort, total corrections > 0, and — read from the TB EVENTS, not the child log's rendered table — `teacher/corrections_per_cycle` at >= 2 points plus `teacher/loss` / `teacher/ce` / `teacher/n` / `grad/searchteacher_share`. What the run DID find is sizing, not correctness: 🚨 **`--teacher-confirm-rollouts` decides whether a cycle teaches anything at all** — the Wilson strictly-better gate needs the alternative line to win one confirm game, a 6k-step policy does that at p ~= 0.028, and at the pilot's 2 rollouts **0 corrections came out of 12 candidates over 3 cycles**, every one `gate_failed`, while at 16 the pooled conversion is ~15% per candidate; and **a cycle's LAUNCH is not its COLLECT** (a 6,000-step attempt launched two cycles and collected ONE — nothing waits for a per-cycle worker at `_on_training_end`), so `--steps` AND the candidate budget are both sized by the collect: 12,000 steps gave 3 launches / 2 collects / 13 candidate-shots (a ~2% flake on the "total > 0" bar) and 18,000 gives **8 / 8 / 34** in 10 m 38 s at contention factor 1.32. The three prose claims that said the composition was ungated — the `--use-bridge=rust` startup banner in `main/train/config.py`, `src/agents/training/CLAUDE.md`, `src/rust_sim/CLAUDE.md` — are corrected in the same pass, and `designs/ops/testing.md` gains a **COMPOSITION gates** section with the three rules such a test follows (tmp_path never `models/`, `ProgressDeadline` never a duration cap, a missing precondition FAILS never skips) | `49fb43ce` (2026-09-07) |
| **`designs/CLAUDE.md` cut from 95 KB / 504 lines to 20 KB / ~245** — the leaf every session that touches `designs/` loads. Its state table held two ~21 KB cells (the config-version ladder v51→v111; the ai_v9 concat-deletion + forward-design record), NARRATIVE by the census rule, which went verbatim to `research_state/claude_md_archive/` (HISTORY — additive only): `designs_version_map_config_version_ladder.md`, `designs_version_map_ai_v9_stages_and_forward_designs.md`, plus `designs_version_map_state_table_2026-09-07.md` for the live-arm and chapter cells. The leaf keeps what the root's leaf map asks of it — which `ai_vN` folder is relevant, and the version map — plus the two RULES those cells carried (read the version constants from the code, never from prose; read a live run from the run) | `b57b1ad0` (2026-09-07) |
| **The root `CLAUDE.md` stall section stated a DOWNGRADED cause** — it called the 1–2 agent concurrency cap "the actual cause" of stalls via API stream starvation, an account measured false on 2026-08-11 (19,286 remote turns: slow turns had LOWER mean concurrency, 1.40 vs 1.52; stall rate flat ~6% across every idle bucket). Both rules are unchanged; the stated reason now matches `ORCHESTRATOR_SOP.md` §2/§7 — cap fan-out for TOKEN COST, because a retry restarts an agent from scratch | `b57b1ad0` (2026-09-07) |
| **`restart_read.sh` computed the registered vf_coef statistic TWICE from TWO sources** — finding 1 of the `561e9510` promotion. The TB EVENTS are now the only source for the registered reading and its verdict (`main.ops.tb_read`, verdict bar imported from `vf_framings`, one definition in the tree); the child log survives as a labelled CROSS-CHECK printing both medians and their difference, warning above **0.10 log10** (one fifth of the narrowest verdict band, so a smaller gap cannot move the call) and never deciding a verdict. Verified on the live arm at 55.05M: events −0.708 → KEEP+FLAG, cross-check −0.709, difference −0.000 | `b57b1ad0` (2026-09-07) |
| **The Training Run session's INSTRUMENTS promoted out of a session-scoped temporary directory** — `/home/goodlad/.claude/jobs/1046b1d6/tmp/probes/`, 200 files, of which **fifteen are reusable instruments** and the rest are one-off chains, argv snapshots and per-cell readouts. `scripts/ops/` takes the shell layer that must outlive a dead session — `watch_run.sh` (SOP §2 layer 1: progress, the 35-min wedge limit, FAILURE WORDS, the arm-INVALIDATING `--sync-to-main` line, marginal fps from checkpoint mtimes), `marginal_fps.sh`, `famine_read.sh`, `restart_read.sh`, `_common.sh`; `src/main/ops/` takes the eleven Python instruments — `tb_read` (the registered scalars **from the TB EVENTS, not the child log's rendered table**; a missing tag WARNS and exits non-zero), `killbar`, `vf_framings`, `restart_startup`, `plateau_signal`, `g7_report`, `stall_exhibit`, `calib_trend`, `perbot_r`, `perbot_rank`, `negskill_null`, plus `run_ref`. **A SUBPACKAGE, not `src/main/*.py`:** that list is the OFFLINE meters (a finished run's artifacts, nothing written); these read a LIVE run's events, child log, checkpoint mtimes and ladder while all of it is still being appended to, and REFUSE when a precondition of the live read is unmet. **The run is an argument everywhere and a BARE INVOCATION REFUSES** — that is the defect removed, not a convenience; `models/` goes through `utils.paths.main_models_dir()` (its `None` a refusal), the interpreter through `$GEN3AI_PYTHON` → `sys.executable`, and the two per-arm constants that would silently score one registration against another's data are now REQUIRED. Gates: `tb_read_test.py` 7 tests on a SYNTHETIC events file (missing tag WARNS, never 0, whole read exits non-zero; `--last N` by STEP across every event dir; the regime marker never a central tendency) and `ops_scripts_test.py` 28 unmarked (`bash -n`, `--help`, bare invocation exits 2, no `/home` literal). **Three findings recorded not fixed** — `restart_read.sh` reads the registered vf_coef statistic from the child LOG while step 5 of the same script reads it from the events; `calib_trend.py` printed a HARDCODED conclusion (that one literal removed); `g7_report.py`'s `sustained` carried a vacuous clause | `561e9510` (2026-09-07) |
| **The eval-trace DRAW bucket** — `gen3_trace_result_v2`, one pure-stdlib declaration (`agents/training/trace_result.py`) the recorder writes and the prober / harvesters / gauge read. **WIN / LOSS / DRAW**, and a DRAW names `meta.draw_kind`: **`timeout`** (`lost` AND `turn >= MAX_TURNS`) or **`tie`** (the sim's `\|tie\|`). The defect: a timeout ARRIVES WEARING A LOSS'S FLAGS — the trainee forfeits at the cap, so poke-env reports `lost=True` — while the training reward always paid `draw_penalty` for that state, keyed on the TURN COUNT (which is why G7 survived: it reads `meta.turns`, never the label); and a true TIE matched NEITHER quota branch, so its capture was **dropped without a file**. **Measured before touching anything: 145,173 archived traces, every one `win_*`/`loss_*`, `meta.result` never anything but WIN/LOSS, zero TIE.** Draws get their **OWN quota** (5, beside win 5 / loss 10) — folding them into the loss quota is what the old code did and lets a stall storm evict the decisive losses the prober exists to study. Manifest **schema 2**, purely additive (`battles_drawn` / `traces_drawn` / `capture_rate_draw`; a draw is SUBTRACTED from the losses, not added to the played count), and **schema 1 still READS** rather than being demoted to SELECTION UNKNOWN. Unknown result **REFUSED** at the write (`check_result`) and at the read (`result_of`) — `"TIE"` included. **Every archived trace stays readable and is reported as written**; the capture-version field's ABSENCE dates a pre-bucket trace, and `era_note` states that its LOSS traces mix decisive losses with timeouts and its **TIE rate is NOT MEASURABLE** — the chart omits the draw series there rather than drawing a flat zero. Live arm `ai_v12_02_winprob_critic` is PINNED and keeps writing the old vocabulary until it ends | `a040e640` (2026-09-07) |
| **`launcher_child.full.log` — a BOUNDED rotation beside the ring.** The ~1 MiB ring trims SILENTLY (by design, after the 982 MB repaint log), and on 2026-09-06 a per-worker count across a restart became unrecoverable once it wrapped — *a read that cannot be redone is a read that cannot be checked*. `_RotatingChildLog` keeps a full copy rotating into `.1`…`.7` at 64 MiB: a **hard 512 MiB ceiling per run**, chosen against the incident rather than a typical run (the 982 MB storm fills the rotation and STOPS at ~half its size; a normal 3-hour cycle never engages it; 64 MiB is a file a reader can grep). One reader thread feeds both through `_ChildLogFanout`; **the ring is UNTOUCHED** — same path, cap, trim marker and tail, written FIRST, with a full-copy failure swallowed. ⚠️ The live launcher runs old PINNED code and will not pick this up | `a040e640` (2026-09-07) |
| **The frozen-φ "in flight" sentence in `ARCHITECTURE.md` §6.3 — CLOSED, no fix needed: it was already false when it was written.** `9943bbe0` (2026-09-06 **17:17** PT) landed `reward_composition.inert_reward_flags` + `reward_composition_block`, the write in `snapshot.save_model_snapshot` and the `_migrate_config` pop; `eb022481` (**18:20** PT, with `9943bbe0` as an ancestor) rewrote §6.3 and added the claim **63 minutes later**. It is live, not just landed: the running arm's `model_config.json` AND `metadata.json` both carry `inert_reward_flags` (11 entries). The sentence is replaced by the current truth — the resolver, both write sites, why it sits BESIDE the raw values (`check_reward_config` compares each recorded value against the RESUMING argv, so a recorded `false` would FATAL every restart), and `draw_penalty` as the one entry inert by MAGNITUDE not by term. No code, no sidecar, no `CHANGELOG` entry. **The lesson: the mode-flag doc gate was green throughout — a value gate cannot catch a prose claim about the STATE OF THE WORK, so never write one** | `386a6d03` (2026-09-07) |
| **A generated LEDGER TITLE INDEX** — `designs/research_state/ledger_index.md`, 448 lines (date · line · title) over the 13,798-line append-only ledger. Generator `src/main/ledger_index.py` (`python -m main.ledger_index --write`), in `src/main/` not `tools/` because `tools/` is the acquisition layer and this knows no upstream (the `promote_teams` precedent) — and because the gate imports it. Convention verified over the whole file, not assumed: **426 headings, 389 dated, two eras** (early = date parenthesised mid-title, current = date leading); one rule covers both, and every heading is emitted nested by its own level so nothing is lost to a wrong guess. Currency is a HARD unmarked gate, `src/ledger_index_gate_test.py` (5 tests, 0.07 s, proved to FIRE on a planted entry, also runs inside `scripts/land.sh`); a generation step in `land.sh` was rejected on mechanics — it gates and PUSHES, never commits, so the file would be written after the commit it belongs to. **Rebase conflict rule: keep BOTH ledger entries, take either side of the index, re-run the generator** | `386a6d03` (2026-09-07) |
| **A `slow`-marked test can ride RED invisibly — CLOSED** (`gen3_slow_tier_status_v1`). The slow tier WRITES its verdict per test id and the ROUTINE gate READS it: the root `conftest.py` captures the full `slow` set before `-m` deselection, folds every phase report into one verdict (`fail` > `inconclusive` > `skip` > `pass` — a SKIP is never banked as a pass) and MERGES it — never replaces, under an `flock` in the temp dir plus an atomic rename — into the **COMMITTED** `designs/ops/slow_tier_status.json`. `src/slow_tier_status_gate_test.py` (unmarked, ~0.03 s, the SEVENTH static gate) turns a recorded FAIL into a routine-gate failure naming the test id, the commit it failed at and the first `E ` line. **Only a recorded red is fatal**: `inconclusive` (a DECLARED timeout signature), `unrecorded` and `stale` (>25 commits) are reported as warnings that survive `-q` — a schedule fact must not read like a verdict. A MISSING or EMPTY artifact FAILS (it is committed, so 'missing' is a broken tree, not a fresh one), naming the restore command and `GEN3AI_SKIP_SLOW_STATUS_GATE=1`. ⚠️ Stated limitation: a test that broke SINCE the last recording still reads green — the staleness report is what prices it. First tier run recorded 80 rows (30 pass / 44 skip / 6 inconclusive / **0 fail** — the 6 are the arch viewer's chrome at its 180 s bound with a contention factor of 2.22, the 44 are the prober render suite skipping itself on the same starvation). Planted red proven to fail the routine gate end-to-end; 10 meta-tests pin every class | `8be27b4d` (2026-09-07) |
| **Six `win_prob/*` tags — the CLASSIFICATION was the stale half, RECLASSIFIED (nothing suppressed).** `fc10fbb4` called `{brier,acc}_contested` / `contested_{frac,label_mean}` / `brier_material` / `skill_vs_material` content-free at 15:42 on 2026-09-06; `b87604cd` (`gen3_obs_margin_unconditional_v1`) repaired their SOURCE later the same day — Φ_mat above the PBRS gate, so `win_margin` spreads under `--no-hand-shaping` — and the live arm's pin `f971caf2` CONTAINS it. Measured on `ai_v12_02_winprob_critic` (515 rollouts): `contested_frac` **0.444–0.844** (never the degenerate 1.0), `brier_contested` ≠ `brier` on **515/515** rows, `brier_material` 0.138–0.222 (not the flat 0.25), `skill_vs_material` mean **+0.323**. The six move into a `MARGIN_TAGS` tuple asserted PRESENT on BOTH critic modes, plus a `min(contested_frac) < 1.0` spread check; `_win_prob_loss`'s predicate stays as the consumer-side guard. 🚨 The live arm is PINNED and goes on emitting them CORRECTLY — the row's 'the arm stops emitting them' acceptance is superseded, and the run was not touched | `8be27b4d` (2026-09-07) |
| **The reward golden promoted out of a scratch directory** (`gen3_reward_golden_v1`) — `src/agents/training/reward_golden_test.py`, tier `sim`, **19.7 s**, in the ROUTINE gate. 30 real bridge battles (5 keys x 6 reward compositions), every `RewardBreakdown` field of every decision as `float.hex()`, sha256 `9463dc24…` — the same bytes the `reward_manager.py` decomposition was accepted on, reproduced **bit-for-bit in a fresh worktree** with an independently checked-out `data/`. `reward_golden.json` carries the hash, the commit it was produced at and a PER-SWEEP hash so a mismatch names the composition that moved; preconditions are ASSERTED (a diverged battle fails with THAT message, not as a reward change). Proven to fire on a one-ulp perturbation of a single reward weight | `8be27b4d` (2026-09-07) |
| **`reward_manager.py` decomposed around the fold contract** (1,990 → **808**; the size gate's hard bound is 2,000 and the next edit anywhere in it would have tripped). Three new modules, each one responsibility: `reward_bias_terms.py` 533 (the `RewardBiasTerms` mixin — every `_compute_*` BIAS field, plus the CURRENT-BOARD accessors), `reward_config.py` 445 (`RewardClass` / `RewardConfig` / `RewardBreakdown` + `_REGISTRY` / `SWITCH_BIAS_DROP_FAMILY`), `reward_potentials.py` 357 (the `RewardPotentials` mixin — the Φ potentials, `_pbrs_step`, `_hand_pbrs_on`, the eight `_fold_*_pbrs`). **`process_turn_reward`'s fold SEQUENCE stays one straight line**, the `ccd08003` rule. Reward stream **byte-identical**: 2,802 decisions × 39 fields × 6 compositions, sha256 `9463dc24…` before and after. The three `_encode_incoming_block` patch targets were repointed at `reward_potentials`; stub-vacuity gate green, allowlist still EMPTY | `b0b3a253` (2026-09-07) |
| **Mode-flag doc gate** — `src/mode_flag_doc_gate_test.py`: every MODE-flag value `ARCHITECTURE.md`'s PROSE states equals `production_config.json` (read via the baselines registry accessor), the (pattern → key) table is DECLARED so a rename FAILS, and a planted contradiction is proven to fire. Found and fixed 4 drifts: `out_dim` 660→**138** with `op_drop_renders` (not `damage_matrices_outgoing`) as the real gate, 15→**17** edge families, and `move_belief_mode` / `hp_belief_mode` stated nowhere in prose | `a0317cdf` (2026-09-07) |
| Landing script into the repo as `scripts/land.sh` — paths derived from `git rev-parse --git-common-dir`, all `src/*_gate_test.py` statics, `--help`, re-execs out of the worktree it removes; `ORCHESTRATOR_SOP.md` §3 points at it | `a0317cdf` (2026-09-07) |
| Stale numerics sweep — `--eval-workers` default 3→**5**; the rust_sim `better_line` "STILL NEEDED" block wrong on all 3 claims (impl switch, `--impl rust`, the `input_log` blocker) rewritten to the ungated COMPOSITION; the 722-team pool re-measured at **762/762** (813 files, 51 validate-fail) with the 719-vs-762 disambiguation recorded | `a0317cdf` (2026-09-07) |
| Root `CLAUDE.md` restructure to a constitution + command card + map (206 KB → 32 KB); the two giant leaves split by topic | `206f792c` (2026-09-07) |
| `instrumented_ppo/ppo.py` decomposed around the fold sequence (1998 → 1331) + the contested-mask guard | `ccd08003` |
| **Anti-vacuity audit of test stubs** — `gen3_stub_vacuity_gate_v1`, unmarked in the routine gate (3.6 s). Census 407 patch sites / 70 files: 267 ok (44 of them cleared only by the consumer search), 139 skipped with reasons, **1 vacuous, 0 tests green for the wrong reason**; allowlist EMPTY. The finding was `dry_run_test.py`'s `_launch_child` booby trap, installed on the defining module while `run.py` calls its own module-level import — the trap whose tripping means `--dry-run` spawned a real training child. Census: `designs/research_state/measurements/stub_vacuity_audit_2026-09-07.md` | `83478fcd` |
| Shaped-era timeout comparator — **CLOSED, no comparator** (owner, 2026-09-06); the stripped arm's curve is the only one and the kill bar is registered against it | — |
