# HANDOFF — orchestrator session, 2026-09-22 16:10 PT

**For the next orchestrator session (any model).** This is a point-in-time note; the procedure of
record is [`ORCHESTRATOR_SOP.md`](ORCHESTRATOR_SOP.md) §1 "Taking a handoff" — follow it, and verify
every claim below against the process table before acting. Delete this file once the successor has
rewritten `~/.claude/projects/-home-goodlad-dev-gen3ai/ORCHESTRATOR` and banked its first read.

## Read in this order (≈20 min)

1. root `CLAUDE.md` (the constitution)
2. `designs/research_state/UNDERSTANDING.md` §2.2–2.3 (the plateau era) and §7 rules 22–26
3. the ledger TAIL: every entry dated 2026-09-20 → 22 (`designs/research_state/ledger_index.md` finds them)
4. `designs/research_state/flywheel_era_pair_2026-09-12.md` §9d–9e (the owner's plateau plan)
5. `designs/endstate/` — the two end-state designs (explicit-only; not scheduled)
6. `designs/ops/TECH_DEBT_BACKLOG.md` — every open row carries a date

## The live state at 16:10 PT (verify with `nvidia-smi`, `ps`, `git worktree list`)

**GPU — Training Run session owns it** (`uds:/run/user/1000/cc-socks/2620285.sock`; it banks its
own completions and runs its own reads):

| arm | state | what it decides |
|---|---|---|
| `ai_v13_18_teach5_offense_hidose` | running since 14:23 PT, ETA ~19:00 PT | whether a teacher exists at all from the plateau parent at era-1's 1.78× dose (four refusals at 0.39×); its admission read + `main.best_response_gap` vs `ai_v13_13 --allow-unmatched` follow |
| K-ladder read (rows 1–2, 17,600 battles) | running on CPU, Training Run's | K = 1 / 3 / 11: which target form costs less on the untaught 8; C3 (K 11 vs K 3) is near share-matched by accident and is the cleanest contrast |
| share-matched K = 11 | queued behind hidose | isolates target form at matched pull, vs K = 1 |
| second control seed (1002) | last in queue | family claim on the +15.5 pp continuation |

**CPU agents dispatched by this session** — each in its own worktree under `/home/goodlad/dev/gen3ai-wt/`,
each lands on main by itself and reports once; the successor BANKS the landing (ledger paragraph is
§"Ready-to-append" of each record README; pattern: worktree → append ledger + trailer → UNDERSTANDING
amendment → `python -m main.ledger_index --write` → commit → `bash scripts/land.sh`):

| agent | worktree | lands | record dir |
|---|---|---|---|
| D10 — port emits the intermediate-request view | `d10` (+`d10base`) | zero `view_fallback_intermediate`; two-seed parity sweep | `search_profile_2026-09-22/` (cumulative table updated in place) |
| `expand_many` payload / transport | `expandmany` (+`expandmany_base`) | the largest search span (20 %) | `expand_many_2026-09-22/` |
| playoff repair — root_failed cause, shape mismatch, livelock (already found NOT a livelock, `4d75cfe3`), counters (landed `569f64ec`) | `playoff` | one game per orientation showing the arm ACTS | `playoff_repair_2026-09-22/` |
| H18 Baton Pass bias — patched vs unpatched Metamon, 2×400 | `h18` (pre-reg landed `6cc2f304`) | the size of the favourable bias in every anchor number | `h18_baton_pass_bias_2026-09-22/` |

Landed and BANKED today (nothing to do): search profile grind (`48e767ba`), best-response-gap meter
(`9625ed8d`), P1 batch (`10ee7d86`), Metamon faithfulness (`9d625934`), anchors batch (`e41f266f`).
Landed and NOT YET BANKED (successor banks): ladder refit audit (`7db504ac` — 68/93 files move,
apply tool built, NOT run on the archive: the owner's call), search-teacher composition (`0a137a92` —
both backlog rows were stale; a new P1: unseeded confirm RNG).

## Standing decisions in force

- Exploiters are valid as OPPONENTS, not teachers, unless they clear the transfer gate (four refusals). Next era = the population loop; its meter is `main.best_response_gap`.
- Every fold on the win-prob side is read against a CONTINUATION or the PLATEAU PARENT (`ai_v13_12_plateau/final_model.zip`), never the frozen parent.
- The K-ladder's cost read applies the asymmetry: "wider costs less" is confounded by pull (share 0.25 / 0.13 / 0.12); "wider costs more despite half the pull" is the strong reading.
- Search is WOUND DOWN as an objective; the four search agents are infrastructure. Do not dispatch search RESEARCH.
- Quota ceiling is 100 this week (`~/.claude/quota-aware.on`); the hook has an escape hatch (arm file + quota readers never gated).
- No new dispatches unless the owner asks; bank what lands.

## Hazards a successor inherits

- `git worktree list` shows ~80 worktrees; most are old launcher pins (`gen*-run-*`, `launcher-*`, `bridge-cse_*`) — **do not remove any `launcher-*`** (the live arm's checkout). Agent worktrees (`d10`, `expandmany`, `playoff`, `h18`, their `*_base`) are removed by `land.sh` when their agent lands.
- Box load 30–40 while the agents run: every timing number is a ratio, never an absolute.
- `--impl` is the BRIDGE; the search DRIVER follows `--use-bridge` — there is no `--search-impl` flag.
- A landing collision is routine: rebase, take either side of `ledger_index.md`, regenerate.
