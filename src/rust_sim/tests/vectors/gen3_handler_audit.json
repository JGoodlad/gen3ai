{
 "_meta": {
  "generator": "harness/dump_gen3_handlers.js",
  "gate": "node harness/dump_gen3_handlers.js --audit (cargo: tests/handler_audit_test.rs)",
  "surface": {
   "abilities": 77,
   "items": 63,
   "conditions": 51,
   "moves": 280,
   "rows": 1014
  },
  "excluded_deferred": {
   "forecast": "Castform forme+TYPE change under weather — deferred (probe_forecast_rng.js settled the map; the reporting surface is unprobed); the port FAIL-LOUDs in state::MonState::from_set (GIGO guard: an unmodeled ability would silently no-op then desync under weather); NOT in MODELED/NOOP + REJECT_ABILITIES/REJECT_SPECIES so the e2e filter + randbats adapter exclude its teams",
   "liquidooze": "reverses the Leech Seed drain — the port FAIL-LOUDs in apply_leech_seed; removed from NOOP_ABILITIES so its teams are off the modeled path",
   "ohko_psywave_counter_family": "fissure/horndrill/guillotine/sheercold/psywave/counter/mirrorcoat/bide/endeavor — the deferred fixed-damage family: isModeledMove rejects + MOVE_ID_BLOCKLIST + the engine fail-louds",
   "protocol_lines": "PROTOCOL PHASE 3 (gen3_protocol_phase3_v1) closed the formerly-deferred lines: the Trace |-ability| reveal, the taunt/disable residual -end lines + the Disable [miss]/[still] retro-edits, the Flash Fire -start/-immune/-end cycle, the STATUS_IMMUNE -immune [from] ability: blocks, the Synchronize→Lum interleave, the mid-battle switch-in ability lines, Leech Seed / Splash / Pay Day — all byte-verified vs the capture golden (114 battles / 16115 lines). Remaining un-emitted (uncapturable) forms are documented at their sites — see PROTOCOL_EMISSION_DESIGN.md"
  }
 },
 "rows": {
  "ability:airlock:onEnd": {
   "disposition": "implemented",
   "fp": "fn:905ec09c:onEnd(pokemon) { pokemon.abilityState.ending = true; this.eachEvent(\"WeatherChange\", this.effect); }",
   "anchor": "turn.rs::process_faints",
   "reason": "the onEnd eachEvent(WeatherChange) at the negater's faint — the faint-queue order fix (gen3_faint_queue_order_v1)"
  },
  "ability:airlock:onStart": {
   "disposition": "implemented",
   "fp": "fn:18ed06f0:onStart(pokemon) { pokemon.abilityState.ending = false; }",
   "anchor": "turn.rs::effective_weather",
   "reason": "weather-effect suppression while the negater is active (B3)"
  },
  "ability:arenatrap:onFoeMaybeTrapPokemon": {
   "disposition": "implemented",
   "fp": "fn:e6fbe600:onFoeMaybeTrapPokemon(pokemon, source) { if (!source) source = this.effectState.target; if (!source || !pokemon.isAdjace",
   "anchor": "turn.rs::is_trapped",
   "reason": "the endTurn (Maybe)TrapPokemon handler gather — magnetpull's onAny* draws the 2-handler tie-shuffle"
  },
  "ability:arenatrap:onFoeTrapPokemon": {
   "disposition": "implemented",
   "fp": "fn:7c54ba3e:onFoeTrapPokemon(pokemon) { if (!pokemon.isAdjacent(this.effectState.target)) return; if (pokemon.isGrounded()) { pokemo",
   "anchor": "turn.rs::is_trapped",
   "reason": "the switch-legality trap gate (gen3_trapping_v1 / B4-3 shadow tag)"
  },
  "ability:battlearmor:onCriticalHit": {
   "disposition": "implemented",
   "fp": "val:false",
   "anchor": "turn.rs::crit_immune",
   "reason": "the drawn-then-overridden crit (B1, probe_critimmune_rng.js)"
  },
  "ability:blaze:onBasePower": {
   "disposition": "implemented",
   "fp": "fn:b5cd8c4e:onBasePower(basePower, attacker, defender, move) { if (move.type === \"Fire\" && attacker.hp <= attacker.maxhp / 3) { this",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "pinch BP ×1.5 at 3·hp<=maxhp (gen3_item_mechanics_v1 P2)"
  },
  "ability:blaze:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "pinch BP ×1.5 at 3·hp<=maxhp (gen3_item_mechanics_v1 P2) — (order metadata of the sibling onBasePower handler)"
  },
  "ability:blaze:onModifyAtkPriority": {
   "disposition": "noop_justified",
   "fp": "val:5",
   "reason": "gen4-mod priority residue — the gen3 fold is the onBasePower pinch chain; no onModifyAtk/onModifySpA fn resolves (the mod-chain law)"
  },
  "ability:blaze:onModifySpAPriority": {
   "disposition": "noop_justified",
   "fp": "val:5",
   "reason": "gen4-mod priority residue — the gen3 fold is the onBasePower pinch chain; no onModifyAtk/onModifySpA fn resolves (the mod-chain law)"
  },
  "ability:chlorophyll:onModifySpe": {
   "disposition": "implemented",
   "fp": "fn:f6a187cb:onModifySpe(spe, pokemon) { if ([\"sunnyday\", \"desolateland\"].includes(pokemon.effectiveWeather())) { return this.chainMo",
   "anchor": "turn.rs::effective_speed",
   "reason": "weather ×2 folded into the cached tie-shuffle speed (B2)"
  },
  "ability:clearbody:onTryBoost": {
   "disposition": "implemented",
   "fp": "fn:6a5aec33:onTryBoost(boost, target, source, effect) { if (source && target === source) return; let showMsg = false; let i; for (i ",
   "anchor": "event.rs::intimidate_on_start",
   "reason": "the boost-drop immunity gate (Intimidate + secondary stat-drops)"
  },
  "ability:cloudnine:onEnd": {
   "disposition": "implemented",
   "fp": "fn:905ec09c:onEnd(pokemon) { pokemon.abilityState.ending = true; this.eachEvent(\"WeatherChange\", this.effect); }",
   "anchor": "turn.rs::process_faints",
   "reason": "the onEnd eachEvent(WeatherChange) at the negater's faint — the faint-queue order fix (gen3_faint_queue_order_v1)"
  },
  "ability:cloudnine:onStart": {
   "disposition": "implemented",
   "fp": "fn:18ed06f0:onStart(pokemon) { pokemon.abilityState.ending = false; }",
   "anchor": "turn.rs::effective_weather",
   "reason": "weather-effect suppression while the negater is active (B3)"
  },
  "ability:colorchange:onDamagingHit": {
   "disposition": "implemented",
   "fp": "fn:f818d3be:onDamagingHit(damage, target, source, move) { if (!damage || !target.hp) return; const type = move.type; if (target.isAc",
   "anchor": "turn.rs::types_override",
   "reason": "the mon_types choke-point type override (B4-5)"
  },
  "ability:compoundeyes:onSourceModifyAccuracy": {
   "disposition": "implemented",
   "fp": "fn:ba58383b:onSourceModifyAccuracy(accuracy) { if (typeof accuracy !== \"number\") return; this.debug(\"compoundeyes - enhancing accura",
   "anchor": "turn.rs::effective_accuracy",
   "reason": "acc ×1.3 attacker chain"
  },
  "ability:compoundeyes:onSourceModifyAccuracyPriority": {
   "disposition": "implemented",
   "fp": "val:9",
   "anchor": "turn.rs::effective_accuracy",
   "reason": "acc ×1.3 attacker chain — (order metadata of the sibling onSourceModifyAccuracy handler)"
  },
  "ability:cutecharm:onDamagingHit": {
   "disposition": "implemented",
   "fp": "fn:8bfba51d:onDamagingHit(damage, target, source, move) { if (damage && move.flags[\"contact\"]) { if (this.randomChance(1, 3)) { sour",
   "anchor": "turn.rs::try_add_attract",
   "reason": "the unconditional contact roll + gender-gated attract add (B4-4)"
  },
  "ability:damp:onAnyDamage": {
   "disposition": "unreachable_justified",
   "fp": "fn:244a340c:onAnyDamage(damage, target, source, effect) { if (effect && effect.name === \"Aftermath\") { return false; } }",
   "reason": "the Aftermath guard — Aftermath is a gen4 ability, not in the gen3 universe"
  },
  "ability:damp:onAnyTryMove": {
   "disposition": "implemented",
   "fp": "fn:4eaa2757:onAnyTryMove(target, source, effect) { if ([\"explosion\", \"mindblown\", \"mistyexplosion\", \"selfdestruct\"].includes(effect.",
   "anchor": "turn.rs::damp_holder",
   "reason": "Explosion/Self-Destruct cancelled at TryMove (B2-4)"
  },
  "ability:drizzle:onStart": {
   "disposition": "implemented",
   "fp": "fn:80ee6192:onStart(source) { if (source.species.id === \"kyogre\" && source.item === \"blueorb\") return; this.field.setWeather(\"rainda",
   "anchor": "event.rs::run_switch",
   "reason": "switch-in weather set (permanent, ability-sourced); the Kyogre/Groudon orb guard is gen4-only"
  },
  "ability:drought:onStart": {
   "disposition": "implemented",
   "fp": "fn:b1246fa8:onStart(source) { if (source.species.id === \"groudon\" && source.item === \"redorb\") return; this.field.setWeather(\"sunnyd",
   "anchor": "event.rs::run_switch",
   "reason": "switch-in weather set (permanent, ability-sourced); the Kyogre/Groudon orb guard is gen4-only"
  },
  "ability:effectspore:onDamagingHit": {
   "disposition": "implemented",
   "fp": "fn:0ed47add:onDamagingHit(damage, target, source, move) { if (damage && move.flags[\"contact\"] && this.randomChance(1, 10)) { const s",
   "anchor": "turn.rs::apply_contact_proc",
   "reason": "the after-secondary contact-proc draw (B2-1/B2-2)"
  },
  "ability:flamebody:onDamagingHit": {
   "disposition": "implemented",
   "fp": "fn:6a3d3244:onDamagingHit(damage, target, source, move) { if (damage && move.flags[\"contact\"]) { if (this.randomChance(1, 3)) { sour",
   "anchor": "turn.rs::apply_contact_proc",
   "reason": "the after-secondary contact-proc draw (B2-1/B2-2)"
  },
  "ability:flashfire:onEnd": {
   "disposition": "implemented",
   "fp": "fn:ca7e683a:onEnd(pokemon) { pokemon.removeVolatile(\"flashfire\"); }",
   "anchor": "turn.rs::execute_switch",
   "reason": "REACHED routinely (Lens-1 corrected the old unreachable label): the sim fires singleEvent(End, ability) on EVERY switch-out AND faint (the gen3_cloudnine_end_v1 mechanism; probe: an armed FF switch-out prints |-end|...|ability: Flash Fire|[silent]). The body (removeVolatile(flashfire)) is draw-free and STATE-EQUIVALENT to the port volatile clear in execute_switch + process_faints (proven at scale: the e2e corpus has FF teams with switches, bit-for-bit). The [silent] -end line is now EMITTED on an alive switch-out (gen3_protocol_phase3_v1, byte-verified vs the flashfire_cycle capture; the FAINT path emits nothing — capture-proven). Formerly a documented un-emitted level-2 protocol nicety."
  },
  "ability:flashfire:onTryHit": {
   "disposition": "implemented",
   "fp": "fn:c36eb706:onTryHit(target, source, move) { if (target !== source && move.type === \"Fire\") { if (move.id === \"willowisp\" && (target",
   "anchor": "turn.rs::apply_flash_fire_activation",
   "reason": "the acc_hit-gated flash_fire arm (gen3_flashfire_v1)"
  },
  "ability:forecast:onStart": {
   "disposition": "implemented",
   "fp": "fn:70a95ffe:onStart(pokemon) { this.singleEvent(\"WeatherChange\", this.effect, this.effectState, pokemon); }",
   "anchor": "forecast.rs::forecast_weather_change",
   "reason": "the entrant singleEvent(WeatherChange) — run_switch fires it for the just-switched-in mon off the standing weather (probe S2 t3), and start_with_switchins applies the start-window equivalent post-hoc"
  },
  "ability:forecast:onSwitchInPriority": {
   "disposition": "implemented",
   "fp": "val:-2",
   "anchor": "forecast.rs::forecast_weather_change",
   "reason": "ordering metadata for the sibling onStart (the −2 switch-in slot); PROBED IRRELEVANT in gen-3 singles (probe_r35_double_replacement.js: a double replacement emits switches in ENTRANT-SPEED order and each runSwitch fires its own singleEvent Start — the −2 never reorders anything observable; pin FC8)"
  },
  "ability:forecast:onWeatherChange": {
   "disposition": "implemented",
   "fp": "fn:bd59bcec:onWeatherChange(pokemon) { if (pokemon.baseSpecies.baseSpecies !== \"Castform\" || pokemon.transformed) return; let forme ",
   "anchor": "forecast.rs::forecast_each_event",
   "reason": "the whole handler (gates + forme + type + |-formechange|) at every WeatherChange site: the weather-set move arm, the mid-turn switch-in weather change, the two WEATHER_NEGATE onEnd sites (with the ending-negater exclusion), and the previously-missing UNCONDITIONAL expiry WeatherChange (the T1 8-vs-7 fix)"
  },
  "ability:guts:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:49ede268:onModifyAtk(atk, pokemon) { if (pokemon.status) { return this.chainModify(1.5); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "Atk ×1.5 whenStatused + the has_guts burn-halve suppression"
  },
  "ability:guts:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "Atk ×1.5 whenStatused + the has_guts burn-halve suppression — (order metadata of the sibling onModifyAtk handler)"
  },
  "ability:hugepower:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:b5d4212a:onModifyAtk(atk) { return this.chainModify(2); }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "Atk ×2 (physical only)"
  },
  "ability:hugepower:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "Atk ×2 (physical only) — (order metadata of the sibling onModifyAtk handler)"
  },
  "ability:hustle:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:928e2fd5:onModifyAtk(atk) { return this.modify(atk, 1.5); }",
   "anchor": "turn.rs::atk_direct_modify",
   "reason": "the DIRECT this.modify(atk,1.5) pre-chain fold"
  },
  "ability:hustle:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn.rs::atk_direct_modify",
   "reason": "the DIRECT this.modify(atk,1.5) pre-chain fold — (order metadata of the sibling onModifyAtk handler)"
  },
  "ability:hustle:onSourceModifyAccuracy": {
   "disposition": "implemented",
   "fp": "fn:5e10ec72:onSourceModifyAccuracy(accuracy, target, source, move) { const physicalTypes = [\"Normal\", \"Fighting\", \"Flying\", \"Poison\"",
   "anchor": "turn.rs::effective_accuracy",
   "reason": "acc ×3277/4096 for physical-TYPE moves (gen3_accuracy_pipeline_v1)"
  },
  "ability:hustle:onSourceModifyAccuracyPriority": {
   "disposition": "implemented",
   "fp": "val:7",
   "anchor": "turn.rs::effective_accuracy",
   "reason": "acc ×3277/4096 for physical-TYPE moves (gen3_accuracy_pipeline_v1) — (order metadata of the sibling onSourceModifyAccuracy handler)"
  },
  "ability:hypercutter:onTryBoost": {
   "disposition": "implemented",
   "fp": "fn:63035d05:onTryBoost(boost, target, source, effect) { if (source && target === source) return; if (boost.atk && boost.atk < 0) { d",
   "anchor": "event.rs::intimidate_on_start",
   "reason": "the boost-drop immunity gate (Intimidate + secondary stat-drops)"
  },
  "ability:immunity:onSetStatus": {
   "disposition": "implemented",
   "fp": "fn:44fccff5:onSetStatus(status, target, source, effect) { if (status.id !== \"psn\" && status.id !== \"tox\") return; if (effect?.status",
   "anchor": "turn.rs::status_immune_of",
   "reason": "the setStatus-phase block inside the already-drawn SetStatus event"
  },
  "ability:immunity:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:6213a735:onUpdate(pokemon) { if (pokemon.status === \"psn\" || pokemon.status === \"tox\") { this.add(\"-activate\", pokemon, \"ability:",
   "anchor": "turn.rs::status_immune_on_update",
   "reason": "the Update-site cure — reachable ONLY via a statused mon TRACING the ability (gen3_statusimmune_onupdate_cure_v1, the A/B Trace-Porygon2 cluster)"
  },
  "ability:innerfocus:onTryAddVolatile": {
   "disposition": "implemented",
   "fp": "fn:afa43b46:onTryAddVolatile(status, pokemon) { if (status.id === \"flinch\") return null; }",
   "anchor": "turn.rs::apply_one_secondary",
   "reason": "the flinch block at APPLY (the secondary random(100) still draws, B4-2)"
  },
  "ability:insomnia:onSetStatus": {
   "disposition": "implemented",
   "fp": "fn:e86da03c:onSetStatus(status, target, source, effect) { if (status.id !== \"slp\") return; if (effect?.status) { this.add(\"-immune\",",
   "anchor": "turn.rs::status_immune_of",
   "reason": "the setStatus-phase block inside the already-drawn SetStatus event"
  },
  "ability:insomnia:onTryAddVolatile": {
   "disposition": "unreachable_justified",
   "fp": "fn:6c6a2789:onTryAddVolatile(status, target) { if (status.id === \"yawn\") { this.add(\"-immune\", target, \"[from] ability: Insomnia\"); ",
   "reason": "gates the YAWN volatile only — Yawn is not a modeled move (fail-loud unmodeled status move)"
  },
  "ability:insomnia:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:fc95259c:onUpdate(pokemon) { if (pokemon.status === \"slp\") { this.add(\"-activate\", pokemon, \"ability: Insomnia\"); pokemon.cureSta",
   "anchor": "turn.rs::status_immune_on_update",
   "reason": "the Update-site cure — reachable ONLY via a statused mon TRACING the ability (gen3_statusimmune_onupdate_cure_v1, the A/B Trace-Porygon2 cluster)"
  },
  "ability:intimidate:onStart": {
   "disposition": "implemented",
   "fp": "fn:b91a5306:onStart(pokemon) { let activated = false; for (const target of pokemon.adjacentFoes()) { if (!target.volatiles[\"substitu",
   "anchor": "event.rs::intimidate_on_start"
  },
  "ability:keeneye:onTryBoost": {
   "disposition": "implemented",
   "fp": "fn:14651fe6:onTryBoost(boost, target, source, effect) { if (source && target === source) return; if (boost.accuracy && boost.accurac",
   "anchor": "event.rs::intimidate_on_start",
   "reason": "the boost-drop immunity gate (Intimidate + secondary stat-drops)"
  },
  "ability:lightningrod:onFoeRedirectTarget": {
   "disposition": "noop_justified",
   "fp": "fn:dcbec3cf:onFoeRedirectTarget(target, source, source2, move) { if (this.dex.moves.get(move.id).type !== \"Electric\") return; if (th",
   "reason": "redirects Electric moves in doubles — a singles battle has one target, nothing to redirect",
   "probe": "probe_ability_batch1_noop_verify.js"
  },
  "ability:limber:onSetStatus": {
   "disposition": "implemented",
   "fp": "fn:c3e8e36b:onSetStatus(status, target, source, effect) { if (status.id !== \"par\") return; if (effect?.status) { this.add(\"-immune\",",
   "anchor": "turn.rs::status_immune_of",
   "reason": "the setStatus-phase block inside the already-drawn SetStatus event"
  },
  "ability:limber:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:dbcbf21b:onUpdate(pokemon) { if (pokemon.status === \"par\") { this.add(\"-activate\", pokemon, \"ability: Limber\"); pokemon.cureStatu",
   "anchor": "turn.rs::status_immune_on_update",
   "reason": "the Update-site cure — reachable ONLY via a statused mon TRACING the ability (gen3_statusimmune_onupdate_cure_v1, the A/B Trace-Porygon2 cluster)"
  },
  "ability:liquidooze:onSourceTryHeal": {
   "disposition": "implemented",
   "fp": "fn:1bb60278:onSourceTryHeal(damage, target, source, effect) { this.debug(`Heal is occurring: ${target} <- ${source} :: ${effect.id}`",
   "anchor": "turn.rs::apply_drain",
   "reason": "the drain/leechseed heal REVERSAL (gen3_liquid_ooze_v1): the drainer/seeder takes the would-be heal as damage (|-damage|…|[from] ability: Liquid Ooze|[of]); apply_drain (drain moves) + apply_leech_seed (the leech residual); Dream Eater excluded (not a modeled drain move); DRAW-FREE"
  },
  "ability:magmaarmor:onImmunity": {
   "disposition": "implemented",
   "fp": "fn:a0ed8f95:onImmunity(type, pokemon) { if (type === \"frz\") return false; }",
   "anchor": "turn.rs::status_immune_of",
   "reason": "the immunity-phase frz block BEFORE the SetStatus event"
  },
  "ability:magmaarmor:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:5e3afc31:onUpdate(pokemon) { if (pokemon.status === \"frz\") { this.add(\"-activate\", pokemon, \"ability: Magma Armor\"); pokemon.cure",
   "anchor": "turn.rs::status_immune_on_update",
   "reason": "the Update-site cure — reachable ONLY via a statused mon TRACING the ability (gen3_statusimmune_onupdate_cure_v1, the A/B Trace-Porygon2 cluster)"
  },
  "ability:magnetpull:onAnyMaybeTrapPokemon": {
   "disposition": "implemented",
   "fp": "fn:dd1f7993:onAnyMaybeTrapPokemon(pokemon, source) { if (!source) source = this.effectState.target; if (!source || !pokemon.isAdjace",
   "anchor": "turn.rs::is_trapped",
   "reason": "the endTurn (Maybe)TrapPokemon handler gather — magnetpull's onAny* draws the 2-handler tie-shuffle"
  },
  "ability:magnetpull:onAnyTrapPokemon": {
   "disposition": "implemented",
   "fp": "fn:33fc84ab:onAnyTrapPokemon(pokemon) { if (pokemon.hasType(\"Steel\") && pokemon.isAdjacent(this.effectState.target)) { pokemon.tryTr",
   "anchor": "turn.rs::is_trapped",
   "reason": "the switch-legality trap gate (gen3_trapping_v1 / B4-3 shadow tag)"
  },
  "ability:marvelscale:onModifyDef": {
   "disposition": "implemented",
   "fp": "fn:d9aaa874:onModifyDef(def, pokemon) { if (pokemon.status) { return this.chainModify(1.5); } }",
   "anchor": "turn.rs::resolve_def_stat_mods",
   "reason": "Def ×1.5 while the defender is statused"
  },
  "ability:marvelscale:onModifyDefPriority": {
   "disposition": "implemented",
   "fp": "val:6",
   "anchor": "turn.rs::resolve_def_stat_mods",
   "reason": "Def ×1.5 while the defender is statused — (order metadata of the sibling onModifyDef handler)"
  },
  "ability:minus:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:2b7fe910:onModifySpA(spa, pokemon) { for (const active of this.getAllActive()) { if (!active.fainted && active.hasAbility(\"plus\")",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the cross-field Plus↔Minus SpA ×1.5 chain member (gen3_plus_minus_v1)"
  },
  "ability:naturalcure:onSwitchOut": {
   "disposition": "implemented",
   "fp": "fn:887ebce3:onSwitchOut(pokemon) { if (!pokemon.status || pokemon.status === \"fnt\") return; this.add(\"-curestatus\", pokemon, pokemon",
   "anchor": "turn.rs::execute_switch",
   "reason": "the draw-free switch-out cure (gen3_natural_cure_v1)"
  },
  "ability:oblivious:onImmunity": {
   "disposition": "implemented",
   "fp": "fn:7d40c712:onImmunity(type, pokemon) { if (type === \"attract\") return false; }",
   "anchor": "turn.rs::try_add_attract",
   "reason": "the attract immunity gate (batch-4 attract via Cute Charm)"
  },
  "ability:oblivious:onTryHit": {
   "disposition": "unreachable_justified",
   "fp": "fn:19b82d0e:onTryHit(pokemon, target, move) { if (move.id === \"captivate\") { this.add(\"-immune\", pokemon, \"[from] Oblivious\"); retur",
   "reason": "gates CAPTIVATE only — a gen4 move, not in the gen3 universe"
  },
  "ability:oblivious:onUpdate": {
   "disposition": "unreachable_justified",
   "fp": "fn:ef11fbb0:onUpdate(pokemon) { if (pokemon.volatiles[\"attract\"]) { pokemon.removeVolatile(\"attract\"); this.add(\"-end\", pokemon, \"mo",
   "reason": "removes an attract the holder already has — unreachable: attract clears when the holder switches out, so a TRACE copy can never find a pre-existing attract, and a direct add is blocked at the onImmunity gate"
  },
  "ability:overgrow:onBasePower": {
   "disposition": "implemented",
   "fp": "fn:9ecb62af:onBasePower(basePower, attacker, defender, move) { if (move.type === \"Grass\" && attacker.hp <= attacker.maxhp / 3) { thi",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "pinch BP ×1.5 at 3·hp<=maxhp (gen3_item_mechanics_v1 P2)"
  },
  "ability:overgrow:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "pinch BP ×1.5 at 3·hp<=maxhp (gen3_item_mechanics_v1 P2) — (order metadata of the sibling onBasePower handler)"
  },
  "ability:overgrow:onModifyAtkPriority": {
   "disposition": "noop_justified",
   "fp": "val:5",
   "reason": "gen4-mod priority residue — the gen3 fold is the onBasePower pinch chain; no onModifyAtk/onModifySpA fn resolves (the mod-chain law)"
  },
  "ability:overgrow:onModifySpAPriority": {
   "disposition": "noop_justified",
   "fp": "val:5",
   "reason": "gen4-mod priority residue — the gen3 fold is the onBasePower pinch chain; no onModifyAtk/onModifySpA fn resolves (the mod-chain law)"
  },
  "ability:owntempo:onHit": {
   "disposition": "noop_justified",
   "fp": "fn:9d26eded:onHit(target, source, move) { if (move?.volatileStatus === \"confusion\") { this.add(\"-immune\", target, \"confusion\", \"[fro",
   "reason": "display-only: the `-immune` line for a confusion-VOLATILE move (Confuse Ray etc. — no such move is modeled anyway); the block itself is onTryAddVolatile"
  },
  "ability:owntempo:onTryAddVolatile": {
   "disposition": "implemented",
   "fp": "fn:5c575674:onTryAddVolatile(status, pokemon) { if (status.id === \"confusion\") return null; }",
   "anchor": "turn.rs::add_confusion",
   "reason": "the confusion add block"
  },
  "ability:owntempo:onUpdate": {
   "disposition": "unreachable_justified",
   "fp": "fn:c39e4fc5:onUpdate(pokemon) { if (pokemon.volatiles[\"confusion\"]) { this.add(\"-activate\", pokemon, \"ability: Own Tempo\"); pokemon.",
   "reason": "cures a confusion the holder already has — unreachable: volatiles clear on switch-out, so a TRACE copy (the only ability-gain route) can never find a pre-existing confusion, and a direct add is blocked at onTryAddVolatile"
  },
  "ability:pickup:onResidualOrder": {
   "disposition": "noop_justified",
   "fp": "val:28",
   "reason": "base-data residue: an out-of-battle pickup residual ORDER with NO function handler in the gen3 resolution"
  },
  "ability:pickup:onResidualSubOrder": {
   "disposition": "noop_justified",
   "fp": "val:2",
   "reason": "base-data residue: an out-of-battle pickup residual ORDER with NO function handler in the gen3 resolution"
  },
  "ability:plus:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:1b7478f2:onModifySpA(spa, pokemon) { for (const active of this.getAllActive()) { if (!active.fainted && active.hasAbility(\"minus\"",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the cross-field Plus↔Minus SpA ×1.5 chain member (gen3_plus_minus_v1)"
  },
  "ability:poisonpoint:onDamagingHit": {
   "disposition": "implemented",
   "fp": "fn:c84287d1:onDamagingHit(damage, target, source, move) { if (damage && move.flags[\"contact\"]) { if (this.randomChance(1, 3)) { sour",
   "anchor": "turn.rs::apply_contact_proc",
   "reason": "the after-secondary contact-proc draw (B2-1/B2-2)"
  },
  "ability:pressure:onDeductPP": {
   "disposition": "implemented",
   "fp": "fn:a5fd52ba:onDeductPP(target, source) { if (target === source) return; return 1; }",
   "anchor": "turn.rs::pressure_extra",
   "reason": "the foe-Pressure +1 PP deduction (gen3_pp_tracking_v1, VERIFIED −2 total)"
  },
  "ability:pressure:onStart": {
   "disposition": "implemented",
   "fp": "fn:32835c7e:onStart(pokemon) { this.addSplit(pokemon.side.id, [\"-ability\", pokemon, \"Pressure\", \"[silent]\"]); }",
   "anchor": "turn.rs::emit_ability_start_lines",
   "reason": "the |-ability|<mon>|Pressure|[silent] switch-in reveal — EMITTED (gen3_protocol_phase3_v1, byte-verified; the omniscient stream carries the addSplit secret line)"
  },
  "ability:purepower:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:b5d4212a:onModifyAtk(atk) { return this.chainModify(2); }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "Atk ×2 (physical only)"
  },
  "ability:purepower:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "Atk ×2 (physical only) — (order metadata of the sibling onModifyAtk handler)"
  },
  "ability:raindish:onResidual": {
   "disposition": "implemented",
   "fp": "fn:c5a868d1:onResidual(pokemon) { if ([\"raindance\", \"primordialsea\"].includes(pokemon.effectiveWeather())) { this.heal(pokemon.baseM",
   "anchor": "turn.rs::run_residuals",
   "reason": "ResidualAction::RainDish +maxhp/16 in effective rain (B4b)"
  },
  "ability:raindish:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::run_residuals",
   "reason": "ResidualAction::RainDish +maxhp/16 in effective rain (B4b) — (order metadata of the sibling onResidual handler)"
  },
  "ability:raindish:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:3",
   "anchor": "turn.rs::run_residuals",
   "reason": "ResidualAction::RainDish +maxhp/16 in effective rain (B4b) — (order metadata of the sibling onResidual handler)"
  },
  "ability:rockhead:onDamage": {
   "disposition": "noop_justified",
   "fp": "fn:f6daeccd:onDamage(damage, target, source, effect) { if (effect.id === \"recoil\") { if (!this.activeMove) throw new Error(\"Battle.a",
   "reason": "negates recoil EXCEPT Struggle (the resolved handler: `if (this.activeMove.id !== \"struggle\") return null`) — Struggle is the ONLY recoil source in the modeled universe, so the handler never changes anything"
  },
  "ability:roughskin:onDamagingHit": {
   "disposition": "implemented",
   "fp": "fn:025f93c8:onDamagingHit(damage, target, source, move) { if (damage && move.flags[\"contact\"]) { this.damage(source.baseMaxhp / 16, ",
   "anchor": "turn.rs::apply_contact_proc",
   "reason": "the draw-free baseMaxhp/16 contact recoil (B2-3)"
  },
  "ability:roughskin:onDamagingHitOrder": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::apply_contact_proc",
   "reason": "the draw-free baseMaxhp/16 contact recoil (B2-3) — (order metadata of the sibling onDamagingHit handler)"
  },
  "ability:sandstream:onStart": {
   "disposition": "implemented",
   "fp": "fn:ebe37063:onStart(source) { this.field.setWeather(\"sandstorm\"); }",
   "anchor": "event.rs::run_switch",
   "reason": "switch-in weather set (permanent, ability-sourced); the Kyogre/Groudon orb guard is gen4-only"
  },
  "ability:sandveil:onImmunity": {
   "disposition": "implemented",
   "fp": "fn:80ab886c:onImmunity(type, pokemon) { if (type === \"sandstorm\") return false; }",
   "anchor": "turn.rs::weather_immune",
   "reason": "the sandstorm-chip immunity (AC5)"
  },
  "ability:sandveil:onModifyAccuracy": {
   "disposition": "implemented",
   "fp": "fn:8a435c29:onModifyAccuracy(accuracy) { if (typeof accuracy !== \"number\") return; if (this.field.isWeather(\"sandstorm\")) { this.deb",
   "anchor": "turn.rs::effective_accuracy",
   "reason": "acc ×0.8 defender chain in sand"
  },
  "ability:sandveil:onModifyAccuracyPriority": {
   "disposition": "implemented",
   "fp": "val:8",
   "anchor": "turn.rs::effective_accuracy",
   "reason": "acc ×0.8 defender chain in sand — (order metadata of the sibling onModifyAccuracy handler)"
  },
  "ability:serenegrace:onModifyMove": {
   "disposition": "implemented",
   "fp": "fn:d726eb02:onModifyMove(move) { if (move.secondaries && move.id !== \"chatter\") { this.debug(\"doubling secondary chance\"); for (cons",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "secondary chance ×2 (incl. the King's Rock appended one)"
  },
  "ability:serenegrace:onModifyMovePriority": {
   "disposition": "implemented",
   "fp": "val:-2",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "secondary chance ×2 (incl. the King's Rock appended one) — (order metadata of the sibling onModifyMove handler)"
  },
  "ability:shadowtag:onFoeMaybeTrapPokemon": {
   "disposition": "implemented",
   "fp": "fn:aa2fbee0:onFoeMaybeTrapPokemon(pokemon, source) { if (!source) source = this.effectState.target; if (!source || !pokemon.isAdjace",
   "anchor": "turn.rs::is_trapped",
   "reason": "the endTurn (Maybe)TrapPokemon handler gather — magnetpull's onAny* draws the 2-handler tie-shuffle"
  },
  "ability:shadowtag:onFoeTrapPokemon": {
   "disposition": "implemented",
   "fp": "fn:25754e19:onFoeTrapPokemon(pokemon) { pokemon.trapped = true; }",
   "anchor": "turn.rs::is_trapped",
   "reason": "the switch-legality trap gate (gen3_trapping_v1 / B4-3 shadow tag)"
  },
  "ability:shedskin:onResidual": {
   "disposition": "implemented",
   "fp": "fn:2bf43b0f:onResidual(pokemon) { if (pokemon.hp && pokemon.status && this.randomChance(33, 100)) { this.debug(\"shed skin\"); this.ad",
   "anchor": "turn.rs::run_residuals",
   "reason": "the order-10 subOrder-3 randomChance(33,100) cure-before-DoT residual (BR5; handler gathered unconditionally for the tie-shuffle)"
  },
  "ability:shedskin:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::run_residuals",
   "reason": "the order-10 subOrder-3 randomChance(33,100) cure-before-DoT residual (BR5; handler gathered unconditionally for the tie-shuffle) — (order metadata of the sibling onResidual handler)"
  },
  "ability:shedskin:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:3",
   "anchor": "turn.rs::run_residuals",
   "reason": "the order-10 subOrder-3 randomChance(33,100) cure-before-DoT residual (BR5; handler gathered unconditionally for the tie-shuffle) — (order metadata of the sibling onResidual handler)"
  },
  "ability:shellarmor:onCriticalHit": {
   "disposition": "implemented",
   "fp": "val:false",
   "anchor": "turn.rs::crit_immune",
   "reason": "the drawn-then-overridden crit (B1, probe_critimmune_rng.js)"
  },
  "ability:shielddust:onModifySecondaries": {
   "disposition": "implemented",
   "fp": "fn:8722f9c1:onModifySecondaries(secondaries) { this.debug(\"Shield Dust prevent secondary\"); return secondaries.filter((effect) => !!",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the filter-the-draw secondary suppression"
  },
  "ability:soundproof:onTryHit": {
   "disposition": "implemented",
   "fp": "fn:558acf16:onTryHit(target, source, move) { if (move.flags[\"sound\"]) { this.add(\"-immune\", target, \"[from] ability: Soundproof\"); r",
   "anchor": "turn.rs::move_is_sound",
   "reason": "sound-move immunity (B2-5)"
  },
  "ability:speedboost:onResidual": {
   "disposition": "implemented",
   "fp": "fn:a08bc956:onResidual(pokemon) { if (pokemon.activeTurns) { this.boost({ spe: 1 }); } }",
   "anchor": "turn.rs::run_residuals",
   "reason": "ResidualAction::SpeedBoost at order 10 subOrder 3 (B4)"
  },
  "ability:speedboost:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::run_residuals",
   "reason": "ResidualAction::SpeedBoost at order 10 subOrder 3 (B4) — (order metadata of the sibling onResidual handler)"
  },
  "ability:speedboost:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:3",
   "anchor": "turn.rs::run_residuals",
   "reason": "ResidualAction::SpeedBoost at order 10 subOrder 3 (B4) — (order metadata of the sibling onResidual handler)"
  },
  "ability:static:onDamagingHit": {
   "disposition": "implemented",
   "fp": "fn:e32bf5df:onDamagingHit(damage, target, source, move) { if (damage && move.flags[\"contact\"]) { if (this.randomChance(1, 3)) { sour",
   "anchor": "turn.rs::apply_contact_proc",
   "reason": "the after-secondary contact-proc draw (B2-1/B2-2)"
  },
  "ability:stench:onModifyMovePriority": {
   "disposition": "noop_justified",
   "fp": "val:-1",
   "reason": "base-data residue: the gen9 stench-flinch onModifyMove is not in the gen3 resolution — only its priority number survives"
  },
  "ability:stickyhold:onTakeItem": {
   "disposition": "unreachable_justified",
   "fp": "fn:0774dd31:onTakeItem(item, pokemon, source) { if (source && source !== pokemon || this.activeMove && this.activeMove.id === \"knock",
   "reason": "blocks Thief/Knock Off/Trick item removal — no item-removal move is modeled (MOVE_ID_BLOCKLIST)",
   "probe": "probe_ability_batch1_noop_verify.js"
  },
  "ability:sturdy:onDamagePriority": {
   "disposition": "noop_justified",
   "fp": "val:-30",
   "reason": "base-data residue: the gen5+ survive-at-1 onDamage is not in the gen3 resolution — only its priority number survives"
  },
  "ability:sturdy:onTryHit": {
   "disposition": "unreachable_justified",
   "fp": "fn:d9afd493:onTryHit(pokemon, target, move) { if (move.ohko) { this.add(\"-immune\", pokemon, \"[from] ability: Sturdy\"); return null; ",
   "reason": "the OHKO-move immunity — the OHKO family is deferred (fail-loud + MOVE_ID_BLOCKLIST keeps it off the modeled path)"
  },
  "ability:suctioncups:onDragOut": {
   "disposition": "implemented",
   "fp": "fn:d6c5b7f0:onDragOut(pokemon) { this.add(\"-activate\", pokemon, \"ability: Suction Cups\"); return null; }",
   "anchor": "turn.rs::drag_in",
   "reason": "the phaze-arm Suction Cups gate — no drag, no sample draw (B2-6)"
  },
  "ability:suctioncups:onDragOutPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::drag_in",
   "reason": "the phaze-arm Suction Cups gate — no drag, no sample draw (B2-6) — (order metadata of the sibling onDragOut handler)"
  },
  "ability:swarm:onBasePower": {
   "disposition": "implemented",
   "fp": "fn:6f0a0760:onBasePower(basePower, attacker, defender, move) { if (move.type === \"Bug\" && attacker.hp <= attacker.maxhp / 3) { this.",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "pinch BP ×1.5 at 3·hp<=maxhp (gen3_item_mechanics_v1 P2)"
  },
  "ability:swarm:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "pinch BP ×1.5 at 3·hp<=maxhp (gen3_item_mechanics_v1 P2) — (order metadata of the sibling onBasePower handler)"
  },
  "ability:swarm:onModifyAtkPriority": {
   "disposition": "noop_justified",
   "fp": "val:5",
   "reason": "gen4-mod priority residue — the gen3 fold is the onBasePower pinch chain; no onModifyAtk/onModifySpA fn resolves (the mod-chain law)"
  },
  "ability:swarm:onModifySpAPriority": {
   "disposition": "noop_justified",
   "fp": "val:5",
   "reason": "gen4-mod priority residue — the gen3 fold is the onBasePower pinch chain; no onModifyAtk/onModifySpA fn resolves (the mod-chain law)"
  },
  "ability:swiftswim:onModifySpe": {
   "disposition": "implemented",
   "fp": "fn:a5184935:onModifySpe(spe, pokemon) { if ([\"raindance\", \"primordialsea\"].includes(pokemon.effectiveWeather())) { return this.chain",
   "anchor": "turn.rs::effective_speed",
   "reason": "weather ×2 folded into the cached tie-shuffle speed (B2)"
  },
  "ability:synchronize:onAfterSetStatus": {
   "disposition": "implemented",
   "fp": "fn:5e202987:onAfterSetStatus(status, target, source, effect) { if (!source || source === target) return; if (effect && effect.id ===",
   "anchor": "turn.rs::try_set_status",
   "reason": "the source-aware status reflect (B2-7)"
  },
  "ability:thickfat:onSourceBasePower": {
   "disposition": "implemented",
   "fp": "fn:a145b480:onSourceBasePower(basePower, attacker, defender, move) { if (move.type === \"Ice\" || move.type === \"Fire\") { return this.",
   "anchor": "turn.rs::defender_thick_fat",
   "reason": "Ice/Fire sourceBasePower ×0.5 (defender handler on the attacker BP)"
  },
  "ability:thickfat:onSourceBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::defender_thick_fat",
   "reason": "Ice/Fire sourceBasePower ×0.5 (defender handler on the attacker BP) — (order metadata of the sibling onSourceBasePower handler)"
  },
  "ability:thickfat:onSourceModifyAtkPriority": {
   "disposition": "noop_justified",
   "fp": "val:6",
   "reason": "gen4-mod priority residue — the gen3 fold is onSourceBasePower; no onSourceModifyAtk/SpA fn resolves"
  },
  "ability:thickfat:onSourceModifySpAPriority": {
   "disposition": "noop_justified",
   "fp": "val:5",
   "reason": "gen4-mod priority residue — the gen3 fold is onSourceBasePower; no onSourceModifyAtk/SpA fn resolves"
  },
  "ability:torrent:onBasePower": {
   "disposition": "implemented",
   "fp": "fn:fd46839b:onBasePower(basePower, attacker, defender, move) { if (move.type === \"Water\" && attacker.hp <= attacker.maxhp / 3) { thi",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "pinch BP ×1.5 at 3·hp<=maxhp (gen3_item_mechanics_v1 P2)"
  },
  "ability:torrent:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "pinch BP ×1.5 at 3·hp<=maxhp (gen3_item_mechanics_v1 P2) — (order metadata of the sibling onBasePower handler)"
  },
  "ability:torrent:onModifyAtkPriority": {
   "disposition": "noop_justified",
   "fp": "val:5",
   "reason": "gen4-mod priority residue — the gen3 fold is the onBasePower pinch chain; no onModifyAtk/onModifySpA fn resolves (the mod-chain law)"
  },
  "ability:torrent:onModifySpAPriority": {
   "disposition": "noop_justified",
   "fp": "val:5",
   "reason": "gen4-mod priority residue — the gen3 fold is the onBasePower pinch chain; no onModifyAtk/onModifySpA fn resolves (the mod-chain law)"
  },
  "ability:trace:onStart": {
   "disposition": "implemented",
   "fp": "fn:66bdf2b0:onStart(pokemon) { const target = pokemon.side.randomFoe(); if (!target || target.fainted) return; const ability = targe",
   "anchor": "event.rs::trace_on_start",
   "reason": "the n=1 randomFoe sample + live ability copy (BR4)"
  },
  "ability:truant:onBeforeMove": {
   "disposition": "implemented",
   "fp": "fn:d87cdf46:onBeforeMove(pokemon) { if (pokemon.truantTurn) { this.add(\"cant\", pokemon, \"ability: Truant\"); return false; } }",
   "anchor": "turn.rs::truant_turn",
   "reason": "the priority-9 loaf cant (B4-1)"
  },
  "ability:truant:onBeforeMovePriority": {
   "disposition": "implemented",
   "fp": "val:9",
   "anchor": "turn.rs::truant_turn",
   "reason": "the priority-9 loaf cant (B4-1) — (order metadata of the sibling onBeforeMove handler)"
  },
  "ability:truant:onResidual": {
   "disposition": "implemented",
   "fp": "fn:6e5ea60a:onResidual(pokemon) { pokemon.truantTurn = !pokemon.truantTurn; }",
   "anchor": "turn.rs::run_residuals",
   "reason": "the order-27 truant toggle residual"
  },
  "ability:truant:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:27",
   "anchor": "turn.rs::run_residuals",
   "reason": "the order-27 truant toggle residual — (order metadata of the sibling onResidual handler)"
  },
  "ability:truant:onSwitchIn": {
   "disposition": "implemented",
   "fp": "fn:30e5e713:onSwitchIn(pokemon) { pokemon.truantTurn = this.turn !== 0; }",
   "anchor": "event.rs::run_switch",
   "reason": "truant_turn armed `turn != 0` at switch-in"
  },
  "ability:vitalspirit:onSetStatus": {
   "disposition": "implemented",
   "fp": "fn:f85016cd:onSetStatus(status, target, source, effect) { if (status.id !== \"slp\") return; if (effect?.status) { this.add(\"-immune\",",
   "anchor": "turn.rs::status_immune_of",
   "reason": "the setStatus-phase block inside the already-drawn SetStatus event"
  },
  "ability:vitalspirit:onTryAddVolatile": {
   "disposition": "unreachable_justified",
   "fp": "fn:2be51a6e:onTryAddVolatile(status, target) { if (status.id === \"yawn\") { this.add(\"-immune\", target, \"[from] ability: Vital Spirit",
   "reason": "gates the YAWN volatile only — Yawn is not a modeled move (fail-loud unmodeled status move)"
  },
  "ability:vitalspirit:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:757dd4b5:onUpdate(pokemon) { if (pokemon.status === \"slp\") { this.add(\"-activate\", pokemon, \"ability: Vital Spirit\"); pokemon.cur",
   "anchor": "turn.rs::status_immune_on_update",
   "reason": "the Update-site cure — reachable ONLY via a statused mon TRACING the ability (gen3_statusimmune_onupdate_cure_v1, the A/B Trace-Porygon2 cluster)"
  },
  "ability:voltabsorb:onTryHit": {
   "disposition": "implemented",
   "fp": "fn:8d65ab95:onTryHit(target, source, move) { if (target !== source && move.type === \"Electric\" && move.id !== \"thunderwave\") { if (!",
   "anchor": "turn.rs::apply_absorb_heal",
   "reason": "the acc_hit-gated maxhp/4 absorb heal"
  },
  "ability:waterabsorb:onTryHit": {
   "disposition": "implemented",
   "fp": "fn:faf24d4f:onTryHit(target, source, move) { if (target !== source && move.type === \"Water\") { if (!this.heal(target.baseMaxhp / 4))",
   "anchor": "turn.rs::apply_absorb_heal",
   "reason": "the acc_hit-gated maxhp/4 absorb heal"
  },
  "ability:waterveil:onSetStatus": {
   "disposition": "implemented",
   "fp": "fn:e527bcae:onSetStatus(status, target, source, effect) { if (status.id !== \"brn\") return; if (effect?.status) { this.add(\"-immune\",",
   "anchor": "turn.rs::status_immune_of",
   "reason": "the setStatus-phase block inside the already-drawn SetStatus event"
  },
  "ability:waterveil:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:37f94f2e:onUpdate(pokemon) { if (pokemon.status === \"brn\") { this.add(\"-activate\", pokemon, \"ability: Water Veil\"); pokemon.cureS",
   "anchor": "turn.rs::status_immune_on_update",
   "reason": "the Update-site cure — reachable ONLY via a statused mon TRACING the ability (gen3_statusimmune_onupdate_cure_v1, the A/B Trace-Porygon2 cluster)"
  },
  "ability:whitesmoke:onTryBoost": {
   "disposition": "implemented",
   "fp": "fn:7c3b143a:onTryBoost(boost, target, source, effect) { if (source && target === source) return; let showMsg = false; let i; for (i ",
   "anchor": "event.rs::intimidate_on_start",
   "reason": "the boost-drop immunity gate (Intimidate + secondary stat-drops)"
  },
  "ability:wonderguard:onTryHit": {
   "disposition": "implemented",
   "fp": "fn:844277d3:onTryHit(target, source, move) { if (move.id === \"firefang\") { this.hint(\"In Gen 4, Fire Fang is always able to hit thro",
   "anchor": "turn.rs::run_move",
   "reason": "the SE-only damage gate (gen3_wonder_guard_v1): a damaging move into a Wonder Guard holder CONNECTS only if runEffectiveness>0 (type-chart product >1.0) AND not type-immune, else -immune|[from] ability: Wonder Guard drawing only its accuracy roll; BYPASSED by Status / self-target / typeless(???/Struggle) / residuals; read-only gate, no new state; the firefang gen4-hint branch is unreachable (firefang not gen3-legal)"
  },
  "condition:attract:onBeforeMove": {
   "disposition": "implemented",
   "fp": "fn:02bc5ced:onBeforeMove(pokemon, target, move) { this.add(\"-activate\", pokemon, \"move: Attract\", \"[of] \" + this.effectState.source)",
   "anchor": "turn.rs::run_move",
   "reason": "the priority-2 -activate + randomChance(1,2) cant"
  },
  "condition:attract:onBeforeMovePriority": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::run_move",
   "reason": "the priority-2 -activate + randomChance(1,2) cant — (order metadata of the sibling onBeforeMove handler)"
  },
  "condition:attract:onEnd": {
   "disposition": "noop_justified",
   "fp": "fn:fbe69ccf:onEnd(pokemon) { this.add(\"-end\", pokemon, \"Attract\", \"[silent]\"); }",
   "reason": "display-only [silent] |-end| Attract line"
  },
  "condition:attract:onStart": {
   "disposition": "implemented",
   "fp": "fn:27126d9b:onStart(pokemon, source, effect) { if (!(pokemon.gender === \"M\" && source.gender === \"F\") && !(pokemon.gender === \"F\" &&",
   "anchor": "turn.rs::try_add_attract",
   "reason": "the gender gate INSIDE onStart (the CC roll drew unconditionally)"
  },
  "condition:attract:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:e1505792:onUpdate(pokemon) { if (this.effectState.source && !this.effectState.source.isActive && pokemon.volatiles[\"attract\"]) { ",
   "anchor": "turn.rs::run_update_items",
   "reason": "the source-left clear (the attract onUpdate removal)"
  },
  "condition:beatup:duration": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "state.rs::beat_up",
   "reason": "the duration:1 volatile — a NO_ORDER/subOrder-2 residual duration handler (the Beat Up mirror tie-shuffle); cleared at turn-top (clear_flinch) / switch-out / faint"
  },
  "condition:beatup:onFoeModifySpD": {
   "disposition": "implemented",
   "fp": "fn:95bf5471:onFoeModifySpD(def, pokemon) { this.event.modifier = 1; return this.dex.species.get(pokemon.set.species).baseStats.def; ",
   "anchor": "turn.rs::run_beat_up",
   "reason": "the stat swap: defender SpD REPLACED by the target dex baseStats.def (event.modifier=1) — computed directly in the per-strike DamageContext"
  },
  "condition:beatup:onFoeModifySpDPriority": {
   "disposition": "implemented",
   "fp": "val:-101",
   "anchor": "turn.rs::run_beat_up",
   "reason": "the stat swap: defender SpD REPLACED by the target dex baseStats.def (event.modifier=1) — computed directly in the per-strike DamageContext"
  },
  "condition:beatup:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:c394899d:onModifySpA(atk, pokemon, defender, move) { if (!this.ruleTable.has(\"beatupnicknamesmod\")) { this.add(\"-activate\", pokem",
   "anchor": "turn.rs::run_beat_up",
   "reason": "the stat swap: attacker SpA REPLACED by the current ally dex baseStats.atk (event.modifier=1) — computed directly in the per-strike DamageContext"
  },
  "condition:beatup:onModifySpAPriority": {
   "disposition": "implemented",
   "fp": "val:-101",
   "anchor": "turn.rs::run_beat_up",
   "reason": "the stat swap: attacker SpA REPLACED by the current ally dex baseStats.atk (event.modifier=1) — computed directly in the per-strike DamageContext"
  },
  "condition:brn:onResidual": {
   "disposition": "implemented",
   "fp": "fn:bf41b8fb:onResidual(pokemon) { this.damage(pokemon.baseMaxhp / 8); }",
   "anchor": "turn.rs::run_residuals",
   "reason": "the maxhp/8 status DoT at order 10 subOrder 6"
  },
  "condition:brn:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::run_residuals",
   "reason": "the maxhp/8 status DoT at order 10 subOrder 6 — (order metadata of the sibling onResidual handler)"
  },
  "condition:brn:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:6",
   "anchor": "turn.rs::run_residuals",
   "reason": "the maxhp/8 status DoT at order 10 subOrder 6 — (order metadata of the sibling onResidual handler)"
  },
  "condition:brn:onStart": {
   "disposition": "implemented",
   "fp": "fn:e40ff891:onStart(target, source, sourceEffect) { if (sourceEffect && sourceEffect.id === \"flameorb\") { this.add(\"-status\", target",
   "anchor": "turn.rs::try_set_status",
   "reason": "the status apply + |-status| emit (slp draws its random(2,6) duration at onStart)"
  },
  "condition:charge:onAfterMove": {
   "disposition": "implemented",
   "fp": "fn:dffe9569:onAfterMove(pokemon, target, move) { if (move.id !== \"charge\") { pokemon.removeVolatile(\"charge\"); } }",
   "anchor": "turn.rs::turn_loop",
   "reason": "the post-run_move consumption: any executed/aborted move != charge removes it (-end [silent]); keyed on the OUTER queued move (a Sleep Talk turn consumes on sleeptalk; the pursuit-interrupt bare useMove does NOT — no runMove → no AfterMove; MC93/MC98)"
  },
  "condition:charge:onBasePower": {
   "disposition": "implemented",
   "fp": "fn:4b49c126:onBasePower(basePower, attacker, defender, move) { if (move.type === \"Electric\") { this.debug(\"charge boost\"); return th",
   "anchor": "turn.rs::run_move",
   "reason": "the ×2 BP-chain fold for the next ELECTRIC move (MC93)"
  },
  "condition:charge:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:9",
   "anchor": "turn.rs::run_move",
   "reason": "the ×2 BP-chain fold for the next ELECTRIC move (MC93)"
  },
  "condition:charge:onEnd": {
   "disposition": "implemented",
   "fp": "fn:fa127fe8:onEnd(pokemon) { this.add(\"-end\", pokemon, \"Charge\", \"[silent]\"); }",
   "anchor": "turn.rs::turn_loop",
   "reason": "the post-run_move consumption: any executed/aborted move != charge removes it (-end [silent]); keyed on the OUTER queued move (a Sleep Talk turn consumes on sleeptalk; the pursuit-interrupt bare useMove does NOT — no runMove → no AfterMove; MC93/MC98)"
  },
  "condition:charge:onMoveAborted": {
   "disposition": "implemented",
   "fp": "fn:da4136ac:onMoveAborted(pokemon, target, move) { if (move.id !== \"charge\") { pokemon.removeVolatile(\"charge\"); } }",
   "anchor": "turn.rs::turn_loop",
   "reason": "the post-run_move consumption: any executed/aborted move != charge removes it (-end [silent]); keyed on the OUTER queued move (a Sleep Talk turn consumes on sleeptalk; the pursuit-interrupt bare useMove does NOT — no runMove → no AfterMove; MC93/MC98)"
  },
  "condition:charge:onRestart": {
   "disposition": "implemented",
   "fp": "fn:4cf45db1:onRestart(pokemon, source, effect) { if (effect && [\"Electromorphosis\", \"Wind Power\"].includes(effect.name)) { this.add(",
   "anchor": "turn.rs::run_status_move",
   "reason": "the charge arm sets MonState::charge (onRestart re-adds, -start again); gen3 has NO +1 SpD"
  },
  "condition:charge:onStart": {
   "disposition": "implemented",
   "fp": "fn:66eaede6:onStart(pokemon, source, effect) { if (effect && [\"Electromorphosis\", \"Wind Power\"].includes(effect.name)) { this.add(\"-",
   "anchor": "turn.rs::run_status_move",
   "reason": "the charge arm sets MonState::charge (onRestart re-adds, -start again); gen3 has NO +1 SpD"
  },
  "condition:choicelock:onDisableMove": {
   "disposition": "implemented",
   "fp": "fn:f52ed89b:onDisableMove(pokemon) { if (!pokemon.getItem().isChoice || !pokemon.hasMove(this.effectState.move)) { pokemon.removeVol",
   "anchor": "turn.rs::move_usable",
   "reason": "the non-locked slots unselectable while holding the Choice item"
  },
  "condition:choicelock:onStart": {
   "disposition": "implemented",
   "fp": "fn:61a6b21f:onStart(pokemon) { if (!pokemon.lastMove) return false; this.effectState.move = pokemon.lastMove.id; }",
   "anchor": "state.rs::choice_locked_move",
   "reason": "the lastMove lock arm (fails with no lastMove)"
  },
  "condition:confusion:onBeforeMove": {
   "disposition": "implemented",
   "fp": "fn:e78812b8:onBeforeMove(pokemon) { pokemon.volatiles[\"confusion\"].time--; if (!pokemon.volatiles[\"confusion\"].time) { pokemon.remov",
   "anchor": "turn.rs::apply_confusion_self_hit",
   "reason": "the priority-3 counter decrement + randomChance(1,2) self-hit"
  },
  "condition:confusion:onBeforeMovePriority": {
   "disposition": "implemented",
   "fp": "val:3",
   "anchor": "turn.rs::apply_confusion_self_hit",
   "reason": "the priority-3 counter decrement + randomChance(1,2) self-hit — (order metadata of the sibling onBeforeMove handler)"
  },
  "condition:confusion:onEnd": {
   "disposition": "noop_justified",
   "fp": "fn:ffbbe2dd:onEnd(target) { this.add(\"-end\", target, \"confusion\"); }",
   "reason": "display-only |-end| confusion line (the port emits it at the counter expiry)"
  },
  "condition:confusion:onStart": {
   "disposition": "implemented",
   "fp": "fn:ee4712e5:onStart(target, source, sourceEffect) { if (sourceEffect?.id === \"lockedmove\") { this.add(\"-start\", target, \"confusion\",",
   "anchor": "turn.rs::add_confusion",
   "reason": "the random(2,6) duration; the lockedmove-fatigue branch is unreachable (lockedmove moves rejected)"
  },
  "condition:counter:duration": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "state.rs::reactive",
   "reason": "the duration:1 volatile — a NO_ORDER/subOrder-2 residual duration handler (the counter-mirror tie-shuffle); cleared at turn-top (clear_flinch) / switch-out / faint"
  },
  "condition:counter:onDamage": {
   "disposition": "implemented",
   "fp": "fn:23c75c2a:onDamage(damage, target, source, effect) { if (effect.effectType === \"Move\" && !source.isAlly(target) && (effect.categor",
   "anchor": "turn.rs::record_reactive_hit",
   "reason": "the priority −101 (LAST — post-Focus-Band) recorder: 2× each qualifying DIRECT foe Move hit (counter: Physical || bare hiddenpower; mirrorcoat: Special && !hiddenpower), OVERWRITING per hit (multihit → 2× the LAST strike); a sub-absorbed hit never fires the mon Damage event"
  },
  "condition:counter:onDamagePriority": {
   "disposition": "implemented",
   "fp": "val:-101",
   "anchor": "turn.rs::record_reactive_hit",
   "reason": "the priority −101 (LAST — post-Focus-Band) recorder: 2× each qualifying DIRECT foe Move hit (counter: Physical || bare hiddenpower; mirrorcoat: Special && !hiddenpower), OVERWRITING per hit (multihit → 2× the LAST strike); a sub-absorbed hit never fires the mon Damage event — (order metadata of the sibling onDamage handler)"
  },
  "condition:counter:onRedirectTarget": {
   "disposition": "implemented",
   "fp": "fn:cdd5a2eb:onRedirectTarget(target, source, source2) { if (source !== this.effectState.target || !this.effectState.slot) return; re",
   "anchor": "turn.rs::run_fixed_damage_move",
   "reason": "target `scripted` redirects at the recorded damager slot — in gen-3 SINGLES that always resolves to the CURRENT foe active (probed C2: a foe switch fails the counter announcing the NEW active), which is what the port targets"
  },
  "condition:counter:onRedirectTargetPriority": {
   "disposition": "implemented",
   "fp": "val:-1",
   "anchor": "turn.rs::run_fixed_damage_move",
   "reason": "target `scripted` redirects at the recorded damager slot — in gen-3 SINGLES that always resolves to the CURRENT foe active (probed C2: a foe switch fails the counter announcing the NEW active), which is what the port targets — (order metadata of the sibling onRedirectTarget handler)"
  },
  "condition:counter:onStart": {
   "disposition": "implemented",
   "fp": "fn:c32788bc:onStart(target, source, move) { this.effectState.slot = null; this.effectState.damage = 0; }",
   "anchor": "turn.rs::run_full_battle",
   "reason": "the order-5 beforeTurnMove lays the reactive volatile with a RESET record ({slot:null,damage:0} — prev-turn damage never counts); draw-free, no protocol line"
  },
  "condition:curse:onResidual": {
   "disposition": "implemented",
   "fp": "fn:bd1f08df:onResidual(pokemon) { this.damage(pokemon.baseMaxhp / 4); }",
   "anchor": "turn.rs::apply_curse",
   "reason": "the order-10 subOrder-8 chip: the cursed foe loses floor(maxhp/4)/turn, draw-free"
  },
  "condition:curse:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_curse",
   "reason": "the order-10 subOrder-8 chip: the cursed foe loses floor(maxhp/4)/turn, draw-free — (order metadata of the sibling onResidual handler)"
  },
  "condition:curse:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:8",
   "anchor": "turn.rs::apply_curse",
   "reason": "the order-10 subOrder-8 chip: the cursed foe loses floor(maxhp/4)/turn, draw-free — (order metadata of the sibling onResidual handler)"
  },
  "condition:curse:onStart": {
   "disposition": "implemented",
   "fp": "fn:a2741b5c:onStart(pokemon, source) { this.add(\"-start\", pokemon, \"Curse\", `[of] ${source}`); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the |-start|<foe>|Curse|[of] <user> line — EMITTED at the ghost-curse lay (volatile_start_of)"
  },
  "condition:destinybond:onBeforeMove": {
   "disposition": "implemented",
   "fp": "fn:9a159a99:onBeforeMove(pokemon, target, move) { if (move.id === \"destinybond\") return; this.debug(\"removing Destiny Bond before at",
   "anchor": "turn.rs::run_move",
   "reason": "the window closes at the users next move ATTEMPT: priority −1 removal for any move != destinybond + the onMoveAborted removal at every cant site (MC84)"
  },
  "condition:destinybond:onBeforeMovePriority": {
   "disposition": "implemented",
   "fp": "val:-1",
   "anchor": "turn.rs::run_move",
   "reason": "the window closes at the users next move ATTEMPT: priority −1 removal for any move != destinybond + the onMoveAborted removal at every cant site (MC84)"
  },
  "condition:destinybond:onFaint": {
   "disposition": "implemented",
   "fp": "fn:f63bb9ee:onFaint(target, source, effect) { if (!source || !effect || target.isAlly(source)) return; if (effect.effectType === \"Mo",
   "anchor": "turn.rs::process_faints",
   "reason": "the mutual-faint chain: |faint| victim → -activate → the killers hp zeroed + drained in the SAME faintMessages worklist (a foe-Move KO only — the damage sites record destiny_bond_ko_by; residual/futuremove/sub-absorbed never trigger; MC83/MC85)"
  },
  "condition:destinybond:onMoveAborted": {
   "disposition": "implemented",
   "fp": "fn:f229ab84:onMoveAborted(pokemon, target, move) { pokemon.removeVolatile(\"destinybond\"); }",
   "anchor": "turn.rs::run_move",
   "reason": "the window closes at the users next move ATTEMPT: priority −1 removal for any move != destinybond + the onMoveAborted removal at every cant site (MC84)"
  },
  "condition:destinybond:onStart": {
   "disposition": "implemented",
   "fp": "fn:a60aa9d7:onStart(pokemon) { this.add(\"-singlemove\", pokemon, \"Destiny Bond\"); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the DB arm: a ZERO-draw cast; the re-cast draw-free self-removes+re-adds (the flag is idempotent) — |-singlemove| each cast, PP −1 each"
  },
  "condition:disable:durationCallback": {
   "disposition": "implemented",
   "fp": "fn:8ee07c32:durationCallback() { return this.random(2, 6); }",
   "anchor": "turn.rs::disable_move_event_shuffle",
   "reason": "the ONE random(2,6) duration draw on a landed Disable"
  },
  "condition:disable:onBeforeMove": {
   "disposition": "implemented",
   "fp": "fn:80a1db98:onBeforeMove(attacker, defender, move) { if (!(move.isZ && move.isZOrMaxPowered) && move.id === this.effectState.move) {",
   "anchor": "turn.rs::run_move",
   "reason": "the execution-time cant of the disabled queued move"
  },
  "condition:disable:onBeforeMovePriority": {
   "disposition": "implemented",
   "fp": "val:7",
   "anchor": "turn.rs::run_move",
   "reason": "the execution-time cant of the disabled queued move — (order metadata of the sibling onBeforeMove handler)"
  },
  "condition:disable:onDisableMove": {
   "disposition": "implemented",
   "fp": "fn:f2fcb4b7:onDisableMove(pokemon) { for (const moveSlot of pokemon.moveSlots) { if (moveSlot.id === this.effectState.move) { pokemo",
   "anchor": "turn.rs::move_usable",
   "reason": "the disabled slot unselectable"
  },
  "condition:disable:onEnd": {
   "disposition": "implemented",
   "fp": "fn:dd204bfd:onEnd(pokemon) { this.add(\"-end\", pokemon, \"Disable\"); }",
   "anchor": "turn.rs::DisableDuration",
   "reason": "the |-end|<mon>|Disable residual-expiry line — EMITTED (gen3_protocol_phase3_v1, byte-verified vs the disable_lifecycle capture)"
  },
  "condition:disable:onStart": {
   "disposition": "implemented",
   "fp": "fn:d9184044:onStart(pokemon) { if (!this.queue.willMove(pokemon)) { this.effectState.duration++; } if (!pokemon.lastMove) { return f",
   "anchor": "state.rs::disable",
   "reason": "the willMove +1 duration store (gen3_taunt_disable_v1)"
  },
  "condition:encore:durationCallback": {
   "disposition": "implemented",
   "fp": "fn:92b61f6c:durationCallback() { return this.random(3, 7); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the encore arm: acc-100 draw → protect block → already-encored fail (acc ONLY) → durationCallback random(3,7) → the onStart rejects (no lastMove / failencore / 0-PP lastMove, draws consumed) → stored = willMove ? rolled : rolled+1 (MC79/MC80)"
  },
  "condition:encore:onDisableMove": {
   "disposition": "implemented",
   "fp": "fn:c3176841:onDisableMove(pokemon) { if (!this.effectState.move || !pokemon.hasMove(this.effectState.move)) { return; } for (const m",
   "anchor": "state.rs::move_usable",
   "reason": "every non-encored slot is un-selectable while the volatile is up (the request disabled shape; a switch stays legal)"
  },
  "condition:encore:onEnd": {
   "disposition": "implemented",
   "fp": "fn:cfe58a0d:onEnd(target) { this.add(\"-end\", target, \"Encore\"); }",
   "anchor": "turn.rs::run_residuals",
   "reason": "the order-10/subOrder-14 tick (ResidualAction::EncoreDuration): decrement + the 0-PP EARLY -end (MC82) + the expiry -end"
  },
  "condition:encore:onOverrideAction": {
   "disposition": "implemented",
   "fp": "fn:7c241952:onOverrideAction(pokemon, target, move) { if (move.id !== this.effectState.move) return this.effectState.move; }",
   "anchor": "turn.rs::turn_loop",
   "reason": "a queued DIFFERENT move executes AS the encored move — the ENCORED slot PP deducts, the announce shows the encored move (MC79/EN7)"
  },
  "condition:encore:onResidual": {
   "disposition": "implemented",
   "fp": "fn:6b4e977c:onResidual(target) { const moveSlot = target.getMoveData(this.effectState.move); if (!moveSlot || moveSlot.pp <= 0) { ta",
   "anchor": "turn.rs::run_residuals",
   "reason": "the order-10/subOrder-14 tick (ResidualAction::EncoreDuration): decrement + the 0-PP EARLY -end (MC82) + the expiry -end"
  },
  "condition:encore:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::run_residuals",
   "reason": "the order-10/subOrder-14 tick (ResidualAction::EncoreDuration): decrement + the 0-PP EARLY -end (MC82) + the expiry -end"
  },
  "condition:encore:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:14",
   "anchor": "turn.rs::run_residuals",
   "reason": "the order-10/subOrder-14 tick (ResidualAction::EncoreDuration): decrement + the 0-PP EARLY -end (MC82) + the expiry -end"
  },
  "condition:encore:onStart": {
   "disposition": "implemented",
   "fp": "fn:ff78b1e3:onStart(target) { let move = target.lastMove; if (!move || target.volatiles[\"dynamax\"]) return false; if (move.isMax && ",
   "anchor": "turn.rs::run_status_move",
   "reason": "the encore arm: acc-100 draw → protect block → already-encored fail (acc ONLY) → durationCallback random(3,7) → the onStart rejects (no lastMove / failencore / 0-PP lastMove, draws consumed) → stored = willMove ? rolled : rolled+1 (MC79/MC80)"
  },
  "condition:endure:duration": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::run_residuals",
   "reason": "the duration:1 volatile registers a NO_ORDER/subOrder-2 residual duration handler (the endure+stall intra-mon tie — ONE shuffle on every SUCCESS turn); cleared at turn-top (clear_flinch)"
  },
  "condition:endure:onDamage": {
   "disposition": "implemented",
   "fp": "fn:a308a3d6:onDamage(damage, target, source, effect) { if (effect?.effectType === \"Move\" && damage >= target.hp) { this.add(\"-activa",
   "anchor": "turn.rs::endure_clamp",
   "reason": "the priority −10 survive-at-1 clamp on every MOVE-effect Damage site (plain / fixed / multihit strike / futuremove resolve); residual damage is NOT clamped (MC86/MC87)"
  },
  "condition:endure:onDamagePriority": {
   "disposition": "implemented",
   "fp": "val:-10",
   "anchor": "turn.rs::endure_clamp",
   "reason": "the priority −10 survive-at-1 clamp on every MOVE-effect Damage site (plain / fixed / multihit strike / futuremove resolve); residual damage is NOT clamped (MC86/MC87)"
  },
  "condition:endure:onStart": {
   "disposition": "implemented",
   "fp": "fn:6788061a:onStart(target) { this.add(\"-singleturn\", target, \"move: Endure\"); }",
   "anchor": "turn.rs::run_protect",
   "reason": "the endure arm rides the SHARED protect stallingMove machinery (willAct gate + randomChance(1,counter) 2→4→8 no-delete-on-fail; priority 4); success sets MonState::endure"
  },
  "condition:flashfire:onEnd": {
   "disposition": "implemented",
   "fp": "fn:3d463786:onEnd(target) { this.add(\"-end\", target, \"ability: Flash Fire\", \"[silent]\"); }",
   "anchor": "turn.rs::process_faints",
   "reason": "fires from the ABILITY's onEnd at every switch-out/faint — see ability:flashfire:onEnd; the volatile removal is state-equivalent to the port clear in execute_switch + process_faints."
  },
  "condition:flashfire:onModifyAtkPriority": {
   "disposition": "noop_justified",
   "fp": "val:5",
   "reason": "gen4+ priority residue — the gen3 fold is onModifyDamagePhase1; no onModifyAtk/SpA fn resolves"
  },
  "condition:flashfire:onModifyDamagePhase1": {
   "disposition": "implemented",
   "fp": "fn:0d791582:onModifyDamagePhase1(atk, attacker, defender, move) { if (move.type === \"Fire\") { this.debug(\"Flash Fire boost\"); return",
   "anchor": "turn.rs::flash_fire",
   "reason": "the armed ×1.5 Fire fold (gen3_flashfire_v1 ModifyDamagePhase1)"
  },
  "condition:flashfire:onModifySpAPriority": {
   "disposition": "noop_justified",
   "fp": "val:5",
   "reason": "gen4+ priority residue — the gen3 fold is onModifyDamagePhase1; no onModifyAtk/SpA fn resolves"
  },
  "condition:flashfire:onStart": {
   "disposition": "implemented",
   "fp": "fn:045e4711:onStart(target) { this.add(\"-start\", target, \"ability: Flash Fire\"); }",
   "anchor": "turn.rs::apply_flash_fire_activation",
   "reason": "the flash_fire arm + |-start| line"
  },
  "condition:flinch:duration": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "state.rs::flinch",
   "reason": "the duration-1 volatile — reset at end of turn (clear_flinch)"
  },
  "condition:flinch:onBeforeMove": {
   "disposition": "implemented",
   "fp": "fn:26187abe:onBeforeMove(pokemon) { this.add(\"cant\", pokemon, \"flinch\"); this.runEvent(\"Flinch\", pokemon); return false; }",
   "anchor": "turn.rs::clear_flinch",
   "reason": "the priority-8 draw-free cant"
  },
  "condition:flinch:onBeforeMovePriority": {
   "disposition": "implemented",
   "fp": "val:8",
   "anchor": "turn.rs::clear_flinch",
   "reason": "the priority-8 draw-free cant — (order metadata of the sibling onBeforeMove handler)"
  },
  "condition:focusenergy:onModifyCritRatio": {
   "disposition": "implemented",
   "fp": "fn:90b179a8:onModifyCritRatio(critRatio) { return critRatio + 2; }",
   "anchor": "turn.rs::CRIT_MULT",
   "reason": "the +2 crit-ratio fold (clamped 0..5) into the crit denominator"
  },
  "condition:focusenergy:onStart": {
   "disposition": "implemented",
   "fp": "fn:8149470d:onStart(target, source, effect) { if (target.volatiles[\"dragoncheer\"]) return false; if (effect?.id === \"zpower\") { this",
   "anchor": "state.rs::focus_energy",
   "reason": "the volatile arm (via a Lansat eat — the only gen3 route); the dragoncheer branch is gen9 cruft"
  },
  "condition:focuspunch:duration": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "state.rs::focus_punch",
   "reason": "the duration:1 volatile — cleared at turn-top (clear_flinch) / switch-out / faint; registers a NO_ORDER residual duration handler (the FP-mirror tie-shuffle)"
  },
  "condition:focuspunch:onHit": {
   "disposition": "implemented",
   "fp": "fn:355b1033:onHit(pokemon, source, move) { if (move.category !== \"Status\") { this.effectState.lostFocus = true; } }",
   "anchor": "turn.rs::run_move",
   "reason": "sets lostFocus when the user is hit DIRECTLY by a non-Status move (a sub-absorbed chip does not count)"
  },
  "condition:focuspunch:onStart": {
   "disposition": "implemented",
   "fp": "fn:0ddfccfd:onStart(pokemon) { this.add(\"-singleturn\", pokemon, \"move: Focus Punch\"); }",
   "anchor": "turn.rs::run_move",
   "reason": "the |-singleturn|<user>|move: Focus Punch line at the beforeTurnMove (draw-free)"
  },
  "condition:focuspunch:onTryAddVolatile": {
   "disposition": "implemented",
   "fp": "fn:afa43b46:onTryAddVolatile(status, pokemon) { if (status.id === \"flinch\") return null; }",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the focuspunch volatile BLOCKS a flinch (the flinch-secondary random(100) still draws; draw-then-block)"
  },
  "condition:freezeclausemod:onBegin": {
   "disposition": "noop_justified",
   "fp": "fn:55b5237c:onBegin() { this.add(\"rule\", \"Freeze Clause Mod: Limit one foe frozen\"); }",
   "reason": "display-only: the |rule| banner line at battle start"
  },
  "condition:freezeclausemod:onSetStatus": {
   "disposition": "implemented",
   "fp": "fn:195aa762:onSetStatus(status, target, source) { if (source?.isAlly(target)) { return; } if (status.id === \"frz\") { for (const poke",
   "anchor": "turn.rs::side_has_frozen",
   "reason": "the second-freeze block (gen3_freeze_clause_v1 — the audit's HA2 fix)"
  },
  "condition:frz:onAfterMoveSecondary": {
   "disposition": "unreachable_justified",
   "fp": "fn:1f797aa6:onAfterMoveSecondary(target, source, move) { if (move.thawsTarget) { target.cureStatus(); } }",
   "reason": "move.thawsTarget only — NO gen3 modeled move carries thawsTarget (the fire-thaw rides onDamagingHit instead)"
  },
  "condition:frz:onBeforeMove": {
   "disposition": "implemented",
   "fp": "fn:8b97fb73:onBeforeMove(pokemon, target, move) { if (this.randomChance(1, 5)) { pokemon.cureStatus(); return; } if (move.flags[\"def",
   "anchor": "turn.rs::run_move",
   "reason": "the priority-10 randomChance(1,5) thaw BEFORE the defrost check"
  },
  "condition:frz:onBeforeMovePriority": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::run_move",
   "reason": "the priority-10 randomChance(1,5) thaw BEFORE the defrost check — (order metadata of the sibling onBeforeMove handler)"
  },
  "condition:frz:onDamagingHit": {
   "disposition": "implemented",
   "fp": "fn:99627abf:onDamagingHit(damage, target, source, move) { if (this.dex.moves.get(move.id).type === \"Fire\" && move.category !== \"Stat",
   "anchor": "turn.rs::run_move",
   "reason": "a landed Fire damaging move thaws the target (the fire-thaw at the DamagingHit region; sub-absorbed hits do not thaw)"
  },
  "condition:frz:onModifyMove": {
   "disposition": "implemented",
   "fp": "fn:bef178a7:onModifyMove(move, pokemon) { if (move.flags[\"defrost\"]) { this.add(\"-curestatus\", pokemon, \"frz\", `[from] move: ${move}",
   "anchor": "turn.rs::run_move",
   "reason": "the flags.defrost user self-thaw (gen3_defrost_v1 — Sacred Fire / Flame Wheel)"
  },
  "condition:frz:onStart": {
   "disposition": "implemented",
   "fp": "fn:e58c5c29:onStart(target, source, sourceEffect) { if (sourceEffect && sourceEffect.effectType === \"Ability\") { this.add(\"-status\",",
   "anchor": "turn.rs::try_set_status",
   "reason": "the status apply + |-status| emit (slp draws its random(2,6) duration at onStart)"
  },
  "condition:futuremove:onEnd": {
   "disposition": "implemented",
   "fp": "fn:556cfb03:onEnd(target) { const data = this.effectState; const move = this.dex.moves.get(data.move); if (target.fainted || target ",
   "anchor": "turn.rs::apply_future_move",
   "reason": "the resolve: skip iff the occupant fainted; |-end|…|move: <Name>, remove Protect, ONE accuracy roll, the STORED number fixed-damage-style (sub absorbs, Focus Band rolls), the two hitStepMoveHitLoop Updates with the in-loop faintMessages between (a resolve KO draws one tie-Update + defers the Quick Claw)"
  },
  "condition:futuremove:onResidual": {
   "disposition": "implemented",
   "fp": "fn:0d77074e:onResidual(target) { if (this.getOverflowedTurnCount() < this.effectState.endingTurn) return; target.side.removeSlotCond",
   "anchor": "turn.rs::apply_future_move",
   "reason": "the order-11 residual tick (gathered EVERY end-of-turn while pending, speed = the slot occupant — the FS-mirror tie; Wish 7 → sand 8 → order-10s → futuremove 11 LAST), duration 3→2→1→resolve"
  },
  "condition:futuremove:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:11",
   "anchor": "turn.rs::apply_future_move",
   "reason": "the order-11 residual tick (gathered EVERY end-of-turn while pending, speed = the slot occupant — the FS-mirror tie; Wish 7 → sand 8 → order-10s → futuremove 11 LAST), duration 3→2→1→resolve"
  },
  "condition:futuremove:onStart": {
   "disposition": "implemented",
   "fp": "fn:92767d53:onStart(target) { this.effectState.targetSlot = target.getSlot(); this.effectState.endingTurn = this.turn - 1 + 2; if (t",
   "anchor": "state.rs::future_move",
   "reason": "FutureMove {duration:3, damage, move_id, accuracy, source_side/uid} — the slot-keyed pending strike (the Wish precedent)"
  },
  "condition:hail:duration": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn.rs::apply_weather_chip",
   "reason": "the weather_turns 5-turn countdown (a MOVE set; ability weather is permanent duration 0; the rock items are gen4)"
  },
  "condition:hail:durationCallback": {
   "disposition": "implemented",
   "fp": "fn:de12985a:durationCallback(source, effect) { if (source?.hasItem(\"icyrock\")) { return 8; } return 5; }",
   "anchor": "turn.rs::apply_weather_chip",
   "reason": "the weather_turns 5-turn countdown (a MOVE set; ability weather is permanent duration 0; the rock items are gen4)"
  },
  "condition:hail:onFieldEnd": {
   "disposition": "implemented",
   "fp": "fn:9dc7d496:onFieldEnd() { this.add(\"-weather\", \"none\"); }",
   "anchor": "turn.rs::apply_weather_chip",
   "reason": "the expiry branch: |-weather|none + the UNCONDITIONAL clearWeather eachEvent(WeatherChange) draw (the round-35 T1 fix)"
  },
  "condition:hail:onFieldResidual": {
   "disposition": "implemented",
   "fp": "fn:37efead8:onFieldResidual() { this.add(\"-weather\", \"Hail\", \"[upkeep]\"); if (this.field.isWeather(\"hail\")) this.eachEvent(\"Weather\"",
   "anchor": "turn.rs::run_residuals",
   "reason": "the upkeep + effective-weather-GATED eachEvent(Weather) chip shuffle (a negater silences it)"
  },
  "condition:hail:onFieldResidualOrder": {
   "disposition": "implemented",
   "fp": "val:8",
   "anchor": "turn.rs::run_residuals",
   "reason": "the upkeep + effective-weather-GATED eachEvent(Weather) chip shuffle (a negater silences it) — (order metadata of the sibling onFieldResidual handler)"
  },
  "condition:hail:onFieldStart": {
   "disposition": "implemented",
   "fp": "fn:a2af3ee7:onFieldStart(field, source, effect) { if (effect?.effectType === \"Ability\") { if (this.gen <= 5) this.effectState.durati",
   "anchor": "event.rs::run_switch",
   "reason": "the weather set (+ |-weather| line) — ability-sourced permanent OR the modeled_weather_set_move 5-turn arm"
  },
  "condition:hail:onWeather": {
   "disposition": "implemented",
   "fp": "fn:257afca3:onWeather(target) { this.damage(target.baseMaxhp / 16); }",
   "anchor": "turn.rs::weather_immune",
   "reason": "the maxhp/16 weather chip (typed + Sand Veil immunities folded)"
  },
  "condition:leechseed:onResidual": {
   "disposition": "implemented",
   "fp": "fn:d527ddb8:onResidual(pokemon) { const target = this.getAtSlot(pokemon.volatiles[\"leechseed\"].sourceSlot); if (!target || target.fa",
   "anchor": "turn.rs::apply_leech_seed",
   "reason": "the order-10 subOrder-5 drain/heal residual"
  },
  "condition:leechseed:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_leech_seed",
   "reason": "the order-10 subOrder-5 drain/heal residual — (order metadata of the sibling onResidual handler)"
  },
  "condition:leechseed:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn.rs::apply_leech_seed",
   "reason": "the order-10 subOrder-5 drain/heal residual — (order metadata of the sibling onResidual handler)"
  },
  "condition:leechseed:onStart": {
   "disposition": "implemented",
   "fp": "fn:cbcbef24:onStart(target) { this.add(\"-start\", target, \"move: Leech Seed\"); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the |-start|<mon>|move: Leech Seed line — EMITTED at the plant (gen3_protocol_phase3_v1, byte-verified vs the leechseed_splash_payday capture)"
  },
  "condition:lightscreen:duration": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn.rs::SCREEN_DURATION",
   "reason": "the fixed 5-turn duration (gen3 has no Light Clay → always 5)"
  },
  "condition:lightscreen:durationCallback": {
   "disposition": "implemented",
   "fp": "fn:cae69264:durationCallback(target, source, effect) { if (source?.hasItem(\"lightclay\")) { return 8; } return 5; }",
   "anchor": "turn.rs::SCREEN_DURATION",
   "reason": "the Light-Clay durationCallback resolves to 5 in gen3 (Light Clay is gen4)"
  },
  "condition:lightscreen:onAnyModifyDamagePhase1": {
   "disposition": "implemented",
   "fp": "fn:fa221693:onAnyModifyDamagePhase1(damage, source, target, move) { if (target !== source && this.effectState.target.hasAlly(target)",
   "anchor": "damage.rs::modify_damage",
   "reason": "the ×0.5 physical(Reflect)/special(Light Screen) halving (crit-bypassed) — plus, when BOTH screens are up, the run_move ModifyDamagePhase1 tie-shuffle draw (two_tied_handler_shuffle)"
  },
  "condition:lightscreen:onSideEnd": {
   "disposition": "implemented",
   "fp": "fn:b918aa82:onSideEnd(side) { this.add(\"-sideend\", side, \"move: Light Screen\"); }",
   "anchor": "turn.rs::run_residuals",
   "reason": "the |-sideend| line at expiry (the side-residual screen countdown in run_residuals)"
  },
  "condition:lightscreen:onSideResidualOrder": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::run_residuals",
   "reason": "the side-residual TICK order (Reflect 1 / Light Screen 2 — the countdown decrement; the screens have no drawing residual handler)"
  },
  "condition:lightscreen:onSideStart": {
   "disposition": "implemented",
   "fp": "fn:6eaeba2c:onSideStart(side) { this.add(\"-sidestart\", side, \"move: Light Screen\"); }",
   "anchor": "turn.rs::modeled_screen_move",
   "reason": "the |-sidestart| line + the 5-turn counter set (the screen-set arm in run_status_move)"
  },
  "condition:mirrorcoat:duration": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "state.rs::reactive",
   "reason": "the duration:1 volatile — a NO_ORDER/subOrder-2 residual duration handler (the counter-mirror tie-shuffle); cleared at turn-top (clear_flinch) / switch-out / faint"
  },
  "condition:mirrorcoat:onDamage": {
   "disposition": "implemented",
   "fp": "fn:7e939427:onDamage(damage, target, source, effect) { if (effect.effectType === \"Move\" && !source.isAlly(target) && effect.category",
   "anchor": "turn.rs::record_reactive_hit",
   "reason": "the priority −101 (LAST — post-Focus-Band) recorder: 2× each qualifying DIRECT foe Move hit (counter: Physical || bare hiddenpower; mirrorcoat: Special && !hiddenpower), OVERWRITING per hit (multihit → 2× the LAST strike); a sub-absorbed hit never fires the mon Damage event"
  },
  "condition:mirrorcoat:onDamagePriority": {
   "disposition": "implemented",
   "fp": "val:-101",
   "anchor": "turn.rs::record_reactive_hit",
   "reason": "the priority −101 (LAST — post-Focus-Band) recorder: 2× each qualifying DIRECT foe Move hit (counter: Physical || bare hiddenpower; mirrorcoat: Special && !hiddenpower), OVERWRITING per hit (multihit → 2× the LAST strike); a sub-absorbed hit never fires the mon Damage event — (order metadata of the sibling onDamage handler)"
  },
  "condition:mirrorcoat:onRedirectTarget": {
   "disposition": "implemented",
   "fp": "fn:cdd5a2eb:onRedirectTarget(target, source, source2) { if (source !== this.effectState.target || !this.effectState.slot) return; re",
   "anchor": "turn.rs::run_fixed_damage_move",
   "reason": "target `scripted` redirects at the recorded damager slot — in gen-3 SINGLES that always resolves to the CURRENT foe active (probed C2: a foe switch fails the counter announcing the NEW active), which is what the port targets"
  },
  "condition:mirrorcoat:onRedirectTargetPriority": {
   "disposition": "implemented",
   "fp": "val:-1",
   "anchor": "turn.rs::run_fixed_damage_move",
   "reason": "target `scripted` redirects at the recorded damager slot — in gen-3 SINGLES that always resolves to the CURRENT foe active (probed C2: a foe switch fails the counter announcing the NEW active), which is what the port targets — (order metadata of the sibling onRedirectTarget handler)"
  },
  "condition:mirrorcoat:onStart": {
   "disposition": "implemented",
   "fp": "fn:c32788bc:onStart(target, source, move) { this.effectState.slot = null; this.effectState.damage = 0; }",
   "anchor": "turn.rs::run_full_battle",
   "reason": "the order-5 beforeTurnMove lays the reactive volatile with a RESET record ({slot:null,damage:0} — prev-turn damage never counts); draw-free, no protocol line"
  },
  "condition:mustrecharge:duration": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::run_residuals",
   "reason": "the duration:2 volatile registers a NO_ORDER/subOrder-2 residual duration handler on the CAST turn residual (the HB-mirror tie-shuffle); the removal is the recharge cant, not the countdown"
  },
  "condition:mustrecharge:onBeforeMove": {
   "disposition": "implemented",
   "fp": "fn:908abf39:onBeforeMove(pokemon) { this.add(\"cant\", pokemon, \"recharge\"); pokemon.removeVolatile(\"mustrecharge\"); pokemon.removeVol",
   "anchor": "turn.rs::run_move",
   "reason": "the priority-11 recharge cant at the TOP of run_move (before every status handler — a par/slp locked user rolls/decrements NOTHING): |cant|…|recharge, clear must_recharge, ZERO draws, NO PP; removeVolatile(truant) is a no-op in the truant_turn toggle model (the order-27 toggle consumes the loaf — probed cadence)"
  },
  "condition:mustrecharge:onBeforeMovePriority": {
   "disposition": "implemented",
   "fp": "val:11",
   "anchor": "turn.rs::run_move",
   "reason": "the priority-11 recharge cant at the TOP of run_move (before every status handler — a par/slp locked user rolls/decrements NOTHING): |cant|…|recharge, clear must_recharge, ZERO draws, NO PP; removeVolatile(truant) is a no-op in the truant_turn toggle model (the order-27 toggle consumes the loaf — probed cadence)"
  },
  "condition:mustrecharge:onLockMove": {
   "disposition": "implemented",
   "fp": "val:\"recharge\"",
   "anchor": "state.rs::move_locked",
   "reason": "the locked single-`Recharge` request (choice_is_legal Move(0)-only + the firm trapped switch-reject; bridge serialize_active/resolve_choice)"
  },
  "condition:mustrecharge:onStart": {
   "disposition": "implemented",
   "fp": "fn:e1880e8b:onStart(pokemon) { this.add(\"-mustrecharge\", pokemon); }",
   "anchor": "turn.rs::run_move",
   "reason": "the |-mustrecharge|<user> announce at the apply site (log.must_recharge)"
  },
  "condition:par:onBeforeMove": {
   "disposition": "implemented",
   "fp": "fn:a6d16ff1:onBeforeMove(pokemon) { if (!pokemon.hasAbility(\"magicguard\") && this.randomChance(1, 4)) { this.add(\"cant\", pokemon, \"p",
   "anchor": "turn.rs::run_move",
   "reason": "the priority-1 full-para randomChance(1,4) (magicguard is gen4-only)"
  },
  "condition:par:onBeforeMovePriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::run_move",
   "reason": "the priority-1 full-para randomChance(1,4) (magicguard is gen4-only) — (order metadata of the sibling onBeforeMove handler)"
  },
  "condition:par:onModifySpe": {
   "disposition": "implemented",
   "fp": "fn:7fd90253:onModifySpe(spe, pokemon) { if (!pokemon.hasAbility(\"quickfeet\")) { return this.chainModify(0.25); } return spe; }",
   "anchor": "turn.rs::effective_speed",
   "reason": "the ×0.25 paralysis speed fold (quickfeet is gen4-only)"
  },
  "condition:par:onModifySpePriority": {
   "disposition": "implemented",
   "fp": "val:-101",
   "anchor": "turn.rs::effective_speed",
   "reason": "the ×0.25 paralysis speed fold (quickfeet is gen4-only) — (order metadata of the sibling onModifySpe handler)"
  },
  "condition:par:onStart": {
   "disposition": "implemented",
   "fp": "fn:6d079a78:onStart(target, source, sourceEffect) { if (sourceEffect && sourceEffect.effectType === \"Ability\") { this.add(\"-status\",",
   "anchor": "turn.rs::try_set_status",
   "reason": "the status apply + |-status| emit (slp draws its random(2,6) duration at onStart)"
  },
  "condition:partiallytrapped:duration": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn.rs::run_move",
   "reason": "THE family's ONE new draw: the gen4 override random(3,7) (uniform over {3,4,5,6}; the gripclaw arm is dead in gen3) stored as PartialTrap::duration. The base `duration: 5` is fully shadowed by the callback, so it never reaches the engine"
  },
  "condition:partiallytrapped:durationCallback": {
   "disposition": "implemented",
   "fp": "fn:1f5e2307:durationCallback(target, source) { if (source.hasItem(\"gripclaw\")) return 6; return this.random(3, 7); }",
   "anchor": "turn.rs::run_move",
   "reason": "THE family's ONE new draw: the gen4 override random(3,7) (uniform over {3,4,5,6}; the gripclaw arm is dead in gen3) stored as PartialTrap::duration. The base `duration: 5` is fully shadowed by the callback, so it never reaches the engine"
  },
  "condition:partiallytrapped:onEnd": {
   "disposition": "implemented",
   "fp": "fn:79f41f71:onEnd(pokemon) { this.add(\"-end\", pokemon, this.effectState.sourceEffect, \"[partiallytrapped]\"); }",
   "anchor": "turn.rs::apply_partial_trap",
   "reason": "the order-10 subOrder-9 residual: duration-- FIRST (at 0 the onEnd fires |-end|<m>|<Move>|[partiallytrapped] with the DURATION-END `continue` and NO chip — this WINS over the trapper-gone branch, probe O), else the trapper-gone release (|-end|…|[silent], no chip) else the floor(maxhp/16) chip |-damage|…|[from] move: <Move>|[partiallytrapped] (Focus Band draws its onDamage roll, never survives)"
  },
  "condition:partiallytrapped:onResidual": {
   "disposition": "implemented",
   "fp": "fn:5e727d61:onResidual(pokemon) { const trapper = this.effectState.source; if (trapper && (!trapper.isActive || trapper.hp <= 0 || !",
   "anchor": "turn.rs::apply_partial_trap",
   "reason": "the order-10 subOrder-9 residual: duration-- FIRST (at 0 the onEnd fires |-end|<m>|<Move>|[partiallytrapped] with the DURATION-END `continue` and NO chip — this WINS over the trapper-gone branch, probe O), else the trapper-gone release (|-end|…|[silent], no chip) else the floor(maxhp/16) chip |-damage|…|[from] move: <Move>|[partiallytrapped] (Focus Band draws its onDamage roll, never survives)"
  },
  "condition:partiallytrapped:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_partial_trap",
   "reason": "the order-10 subOrder-9 residual: duration-- FIRST (at 0 the onEnd fires |-end|<m>|<Move>|[partiallytrapped] with the DURATION-END `continue` and NO chip — this WINS over the trapper-gone branch, probe O), else the trapper-gone release (|-end|…|[silent], no chip) else the floor(maxhp/16) chip |-damage|…|[from] move: <Move>|[partiallytrapped] (Focus Band draws its onDamage roll, never survives)"
  },
  "condition:partiallytrapped:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:9",
   "anchor": "turn.rs::apply_partial_trap",
   "reason": "the order-10 subOrder-9 residual: duration-- FIRST (at 0 the onEnd fires |-end|<m>|<Move>|[partiallytrapped] with the DURATION-END `continue` and NO chip — this WINS over the trapper-gone branch, probe O), else the trapper-gone release (|-end|…|[silent], no chip) else the floor(maxhp/16) chip |-damage|…|[from] move: <Move>|[partiallytrapped] (Focus Band draws its onDamage roll, never survives)"
  },
  "condition:partiallytrapped:onStart": {
   "disposition": "implemented",
   "fp": "fn:855bf9c2:onStart(pokemon, source) { this.add(\"-activate\", pokemon, \"move: \" + this.effectState.sourceEffect, `[of] ${source}`); t",
   "anchor": "turn.rs::run_move",
   "reason": "the gen5-override onStart: |-activate|<victim>|move: <Move>|[of] <trapper>. Its boundDivisor branch is CONSTANT 16 in gen3 (no Binding Band / Grip Claw), so the divisor is not stored"
  },
  "condition:partiallytrapped:onTrapPokemon": {
   "disposition": "implemented",
   "fp": "fn:230df16c:onTrapPokemon(pokemon) { const gmaxEffect = [\"gmaxcentiferno\", \"gmaxsandblast\"].includes(this.effectState.sourceEffect.i",
   "anchor": "turn.rs::is_trapped",
   "reason": "the FIRM trap (bare tryTrap() → trapped:true on the FIRST request, a rejected switch is [Invalid choice] with NO re-request — probe M); the `source?.isActive` guard is mirrored by re-reading the foe active's uid, and the gmax arm is gen8-only (dead in gen3)"
  },
  "condition:perishsong:duration": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::run_status_move",
   "reason": "the perishsong arm: getAllActive in SIDE order — Soundproof -immune (counted as a result), fresh counters Some(4), ONE -fieldactivate iff >=1 newly applied, the all-counted [still]+-fail, the >=1-immune SILENT re-cast; ZERO draws every branch"
  },
  "condition:perishsong:onEnd": {
   "disposition": "implemented",
   "fp": "fn:e646783e:onEnd(target) { this.add(\"-start\", target, \"perish0\"); target.faint(); }",
   "anchor": "turn.rs::run_residuals",
   "reason": "the order-12 (LAST) tick (ResidualAction::Perish): decrement + |-start|perish<d>; the 1→0 tick prints perish0 + zeroes HP with the DURATION-END `continue` (NO per-handler faintMessages — the mutual perish-out double faint processes at the tail → the gen-3 TIE, MC89)"
  },
  "condition:perishsong:onResidual": {
   "disposition": "implemented",
   "fp": "fn:98db3d2a:onResidual(pokemon) { const duration = pokemon.volatiles[\"perishsong\"].duration; this.add(\"-start\", pokemon, `perish${du",
   "anchor": "turn.rs::run_residuals",
   "reason": "the order-12 (LAST) tick (ResidualAction::Perish): decrement + |-start|perish<d>; the 1→0 tick prints perish0 + zeroes HP with the DURATION-END `continue` (NO per-handler faintMessages — the mutual perish-out double faint processes at the tail → the gen-3 TIE, MC89)"
  },
  "condition:perishsong:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:12",
   "anchor": "turn.rs::run_residuals",
   "reason": "the order-12 (LAST) tick (ResidualAction::Perish): decrement + |-start|perish<d>; the 1→0 tick prints perish0 + zeroes HP with the DURATION-END `continue` (NO per-handler faintMessages — the mutual perish-out double faint processes at the tail → the gen-3 TIE, MC89)"
  },
  "condition:protect:duration": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "state.rs::protected",
   "reason": "the duration-1 volatile — cleared at turn top"
  },
  "condition:protect:onStart": {
   "disposition": "noop_justified",
   "fp": "fn:6e23d167:onStart(target) { this.add(\"-singleturn\", target, \"Protect\"); }",
   "reason": "display-only |-singleturn| Protect line"
  },
  "condition:protect:onTryHit": {
   "disposition": "implemented",
   "fp": "fn:a9259791:onTryHit(target, source, move) { if (this.checkMoveBypassesProtect(move, source, target)) return; this.add(\"-activate\", ",
   "anchor": "turn.rs::protect_blocks",
   "reason": "the priority-3 TryHit block (after the attacker's accuracy roll)"
  },
  "condition:protect:onTryHitPriority": {
   "disposition": "implemented",
   "fp": "val:3",
   "anchor": "turn.rs::protect_blocks",
   "reason": "the priority-3 TryHit block (after the attacker's accuracy roll) — (order metadata of the sibling onTryHit handler)"
  },
  "condition:psn:onResidual": {
   "disposition": "implemented",
   "fp": "fn:bf41b8fb:onResidual(pokemon) { this.damage(pokemon.baseMaxhp / 8); }",
   "anchor": "turn.rs::run_residuals",
   "reason": "the maxhp/8 status DoT at order 10 subOrder 6"
  },
  "condition:psn:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::run_residuals",
   "reason": "the maxhp/8 status DoT at order 10 subOrder 6 — (order metadata of the sibling onResidual handler)"
  },
  "condition:psn:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:6",
   "anchor": "turn.rs::run_residuals",
   "reason": "the maxhp/8 status DoT at order 10 subOrder 6 — (order metadata of the sibling onResidual handler)"
  },
  "condition:psn:onStart": {
   "disposition": "implemented",
   "fp": "fn:e395633c:onStart(target, source, sourceEffect) { if (sourceEffect && sourceEffect.effectType === \"Ability\") { this.add(\"-status\",",
   "anchor": "turn.rs::try_set_status",
   "reason": "the status apply + |-status| emit (slp draws its random(2,6) duration at onStart)"
  },
  "condition:pursuit:duration": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "state.rs::pursuit",
   "reason": "the duration:1 volatile on the target (Some(pursuer_uid)) — laid at the beforeTurnMove, consumed by the interrupt / cleared at turn-top / switch-out / faint"
  },
  "condition:pursuit:onBeforeSwitchOut": {
   "disposition": "implemented",
   "fp": "fn:e85d8f4d:onBeforeSwitchOut(pokemon) { this.debug(\"Pursuit start\"); let alreadyAdded = false; for (const source of this.effectStat",
   "anchor": "turn.rs::execute_switch",
   "reason": "the interrupt: on a VOLUNTARY (is_voluntary) switch-out of the pursued mon, cancelMove the pursuer + run Pursuit at ×2 never-miss BEFORE the swap (gen3_move_coverage_batch4_v1); Baton Pass/drag/faint-replacement do NOT intercept"
  },
  "condition:raindance:duration": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn.rs::apply_weather_chip",
   "reason": "the weather_turns 5-turn countdown (a MOVE set; ability weather is permanent duration 0; the rock items are gen4)"
  },
  "condition:raindance:durationCallback": {
   "disposition": "implemented",
   "fp": "fn:6d9de025:durationCallback(source, effect) { if (source?.hasItem(\"damprock\")) { return 8; } return 5; }",
   "anchor": "turn.rs::apply_weather_chip",
   "reason": "the weather_turns 5-turn countdown (a MOVE set; ability weather is permanent duration 0; the rock items are gen4)"
  },
  "condition:raindance:onFieldEnd": {
   "disposition": "implemented",
   "fp": "fn:9dc7d496:onFieldEnd() { this.add(\"-weather\", \"none\"); }",
   "anchor": "turn.rs::apply_weather_chip",
   "reason": "the expiry branch: |-weather|none + the UNCONDITIONAL clearWeather eachEvent(WeatherChange) draw (the round-35 T1 fix)"
  },
  "condition:raindance:onFieldResidual": {
   "disposition": "implemented",
   "fp": "fn:fcfce89e:onFieldResidual() { this.add(\"-weather\", \"RainDance\", \"[upkeep]\"); this.eachEvent(\"Weather\"); }",
   "anchor": "turn.rs::run_residuals",
   "reason": "the UNCONDITIONAL eachEvent(Weather) tie-shuffle (the sun_rain_weather_turn_tie fix — fired off RAW weather)"
  },
  "condition:raindance:onFieldResidualOrder": {
   "disposition": "implemented",
   "fp": "val:8",
   "anchor": "turn.rs::run_residuals",
   "reason": "the UNCONDITIONAL eachEvent(Weather) tie-shuffle (the sun_rain_weather_turn_tie fix — fired off RAW weather) — (order metadata of the sibling onFieldResidual handler)"
  },
  "condition:raindance:onFieldStart": {
   "disposition": "implemented",
   "fp": "fn:2e98845f:onFieldStart(field, source, effect) { if (effect?.effectType === \"Ability\") { if (this.gen <= 5) this.effectState.durati",
   "anchor": "event.rs::run_switch",
   "reason": "the weather set (+ |-weather| line) — ability-sourced permanent OR the modeled_weather_set_move 5-turn arm"
  },
  "condition:raindance:onWeatherModifyDamage": {
   "disposition": "implemented",
   "fp": "fn:29e062fd:onWeatherModifyDamage(damage, attacker, defender, move) { if (defender.effectiveWeather() !== \"raindance\") return; if (m",
   "anchor": "damage.rs::weather",
   "reason": "the Fire/Water ×1.5/×0.5 weather damage folds (hydrosteam is gen9 cruft)"
  },
  "condition:reflect:duration": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn.rs::SCREEN_DURATION",
   "reason": "the fixed 5-turn duration (gen3 has no Light Clay → always 5)"
  },
  "condition:reflect:durationCallback": {
   "disposition": "implemented",
   "fp": "fn:cae69264:durationCallback(target, source, effect) { if (source?.hasItem(\"lightclay\")) { return 8; } return 5; }",
   "anchor": "turn.rs::SCREEN_DURATION",
   "reason": "the Light-Clay durationCallback resolves to 5 in gen3 (Light Clay is gen4)"
  },
  "condition:reflect:onAnyModifyDamagePhase1": {
   "disposition": "implemented",
   "fp": "fn:a16e5a4c:onAnyModifyDamagePhase1(damage, source, target, move) { if (target !== source && this.effectState.target.hasAlly(target)",
   "anchor": "damage.rs::modify_damage",
   "reason": "the ×0.5 physical(Reflect)/special(Light Screen) halving (crit-bypassed) — plus, when BOTH screens are up, the run_move ModifyDamagePhase1 tie-shuffle draw (two_tied_handler_shuffle)"
  },
  "condition:reflect:onSideEnd": {
   "disposition": "implemented",
   "fp": "fn:84f66e1c:onSideEnd(side) { this.add(\"-sideend\", side, \"Reflect\"); }",
   "anchor": "turn.rs::run_residuals",
   "reason": "the |-sideend| line at expiry (the side-residual screen countdown in run_residuals)"
  },
  "condition:reflect:onSideResidualOrder": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::run_residuals",
   "reason": "the side-residual TICK order (Reflect 1 / Light Screen 2 — the countdown decrement; the screens have no drawing residual handler)"
  },
  "condition:reflect:onSideStart": {
   "disposition": "implemented",
   "fp": "fn:45b46426:onSideStart(side) { this.add(\"-sidestart\", side, \"Reflect\"); }",
   "anchor": "turn.rs::modeled_screen_move",
   "reason": "the |-sidestart| line + the 5-turn counter set (the screen-set arm in run_status_move)"
  },
  "condition:safeguard:duration": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn/status_moves.rs::run_status_move",
   "reason": "the safeguard arm: never-miss so NO accuracy roll, and the FIXED duration 5 (gen-3 durationCallback returns a constant; the Persistent +2 is gen5+) means the cast is entirely DRAW-FREE. Sets SideState::safeguard = 5 and emits |-sidestart|<side>|Safeguard (the BARE name — Light Screen prefixes `move: `, this does not). An already-up re-cast is addSideCondition-false with no onSideRestart, so it takes the [still] did-nothing form plus a bare -fail on the USER, also draw-free. landed=false."
  },
  "condition:safeguard:durationCallback": {
   "disposition": "implemented",
   "fp": "fn:9db853b4:durationCallback(target, source, effect) { if (source?.hasAbility(\"persistent\")) { this.add(\"-activate\", source, \"abilit",
   "anchor": "turn/status_moves.rs::run_status_move",
   "reason": "the safeguard arm: never-miss so NO accuracy roll, and the FIXED duration 5 (gen-3 durationCallback returns a constant; the Persistent +2 is gen5+) means the cast is entirely DRAW-FREE. Sets SideState::safeguard = 5 and emits |-sidestart|<side>|Safeguard (the BARE name — Light Screen prefixes `move: `, this does not). An already-up re-cast is addSideCondition-false with no onSideRestart, so it takes the [still] did-nothing form plus a bare -fail on the USER, also draw-free. landed=false."
  },
  "condition:safeguard:onSetStatus": {
   "disposition": "implemented",
   "fp": "fn:667d5e4c:onSetStatus(status, target, source, effect) { if (!effect || !source) return; if (effect.id === \"yawn\") return; if (effe",
   "anchor": "turn/status.rs::try_set_status_impl",
   "reason": "the (2b2) block, DRAW-FREE: the gen3ou handler-sort shuffle already fired and safeguard sorts into its OWN group, leaving the 2 Standard clauses a size-2 tie — so the shuffle COUNT is unchanged and the block is SEED-NEUTRAL (the STATUS_IMMUNE-ability pattern). Precedence already-statused > type immunity > STATUS_IMMUNE ability > SAFEGUARD > sleep clause. Emits |-activate|<target>|move: Safeguard for a status-MOVE source only; a blocked SECONDARY is silent and its random(100) still drew. The self-inflicted exemption is unreachable today — run_rest inlines its own status path."
  },
  "condition:safeguard:onSideEnd": {
   "disposition": "implemented",
   "fp": "fn:feee14a5:onSideEnd(side) { this.add(\"-sideend\", side, \"Safeguard\"); }",
   "anchor": "turn/residuals.rs::run_residuals",
   "reason": "ResidualAction::SafeguardDuration at order 4 / SIDE_CONDITION_SUBORDER / speed 0 — the reflect(1)/lightscreen(2)/mist(3) sibling. Decrement, |-sideend|<side>|Safeguard at 0, then the D4 END-branch continue. Only the SAME condition on the OTHER side ties, and that tie is real: BOTH sides warded draws ONE extra random(0,2) per residual (probe-measured 8 -> 9 on a mirror), pinned by both_sides_safeguard_ties_the_side_residual_and_draws_the_shuffle."
  },
  "condition:safeguard:onSideResidualOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn/residuals.rs::run_residuals",
   "reason": "ResidualAction::SafeguardDuration at order 4 / SIDE_CONDITION_SUBORDER / speed 0 — the reflect(1)/lightscreen(2)/mist(3) sibling. Decrement, |-sideend|<side>|Safeguard at 0, then the D4 END-branch continue. Only the SAME condition on the OTHER side ties, and that tie is real: BOTH sides warded draws ONE extra random(0,2) per residual (probe-measured 8 -> 9 on a mirror), pinned by both_sides_safeguard_ties_the_side_residual_and_draws_the_shuffle."
  },
  "condition:safeguard:onSideStart": {
   "disposition": "implemented",
   "fp": "fn:b3748154:onSideStart(side, source) { if (source?.hasAbility(\"persistent\")) { this.add(\"-sidestart\", side, \"Safeguard\", \"[persiste",
   "anchor": "turn/status_moves.rs::run_status_move",
   "reason": "the safeguard arm: never-miss so NO accuracy roll, and the FIXED duration 5 (gen-3 durationCallback returns a constant; the Persistent +2 is gen5+) means the cast is entirely DRAW-FREE. Sets SideState::safeguard = 5 and emits |-sidestart|<side>|Safeguard (the BARE name — Light Screen prefixes `move: `, this does not). An already-up re-cast is addSideCondition-false with no onSideRestart, so it takes the [still] did-nothing form plus a bare -fail on the USER, also draw-free. landed=false."
  },
  "condition:safeguard:onTryAddVolatile": {
   "disposition": "implemented",
   "fp": "fn:44506b4c:onTryAddVolatile(status, target, source, effect) { if (!effect || !source) return; if (effect.effectType === \"Move\" && e",
   "anchor": "turn/secondaries.rs::add_confusion",
   "reason": "the confusion half of the ward, gated BEFORE the onStart so the random(2,6) is NOT drawn (the Own Tempo shape). Announces only for a PRIMARY (Confuse Ray); a confusion SECONDARY is silent. The yawn arm of the sim handler is moot here — the port blocks a Yawn CAST at its own arm, and a PENDING yawn RESOLVE is exempt in the sim (`if (effect.id === 'yawn') return`)."
  },
  "condition:sandstorm:duration": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn.rs::apply_weather_chip",
   "reason": "the weather_turns 5-turn countdown (a MOVE set; ability weather is permanent duration 0; the rock items are gen4)"
  },
  "condition:sandstorm:durationCallback": {
   "disposition": "implemented",
   "fp": "fn:44452f71:durationCallback(source, effect) { if (source?.hasItem(\"smoothrock\")) { return 8; } return 5; }",
   "anchor": "turn.rs::apply_weather_chip",
   "reason": "the weather_turns 5-turn countdown (a MOVE set; ability weather is permanent duration 0; the rock items are gen4)"
  },
  "condition:sandstorm:onFieldEnd": {
   "disposition": "implemented",
   "fp": "fn:9dc7d496:onFieldEnd() { this.add(\"-weather\", \"none\"); }",
   "anchor": "turn.rs::apply_weather_chip",
   "reason": "the expiry branch: |-weather|none + the UNCONDITIONAL clearWeather eachEvent(WeatherChange) draw (the round-35 T1 fix)"
  },
  "condition:sandstorm:onFieldResidual": {
   "disposition": "implemented",
   "fp": "fn:9f77ac74:onFieldResidual() { this.add(\"-weather\", \"Sandstorm\", \"[upkeep]\"); if (this.field.isWeather(\"sandstorm\")) this.eachEvent",
   "anchor": "turn.rs::run_residuals",
   "reason": "the upkeep + effective-weather-GATED eachEvent(Weather) chip shuffle (a negater silences it)"
  },
  "condition:sandstorm:onFieldResidualOrder": {
   "disposition": "implemented",
   "fp": "val:8",
   "anchor": "turn.rs::run_residuals",
   "reason": "the upkeep + effective-weather-GATED eachEvent(Weather) chip shuffle (a negater silences it) — (order metadata of the sibling onFieldResidual handler)"
  },
  "condition:sandstorm:onFieldStart": {
   "disposition": "implemented",
   "fp": "fn:692d5e7b:onFieldStart(field, source, effect) { if (effect?.effectType === \"Ability\") { if (this.gen <= 5) this.effectState.durati",
   "anchor": "event.rs::run_switch",
   "reason": "the weather set (+ |-weather| line) — ability-sourced permanent OR the modeled_weather_set_move 5-turn arm"
  },
  "condition:sandstorm:onModifySpDPriority": {
   "disposition": "noop_justified",
   "fp": "val:10",
   "reason": "gen4+ SpD-boost priority residue — no onModifySpD fn resolves in gen3"
  },
  "condition:sandstorm:onWeather": {
   "disposition": "implemented",
   "fp": "fn:257afca3:onWeather(target) { this.damage(target.baseMaxhp / 16); }",
   "anchor": "turn.rs::weather_immune",
   "reason": "the maxhp/16 weather chip (typed + Sand Veil immunities folded)"
  },
  "condition:sleepclausemod:onBegin": {
   "disposition": "noop_justified",
   "fp": "fn:66c5381d:onBegin() { this.add(\"rule\", \"Sleep Clause Mod: Limit one foe put to sleep\"); }",
   "reason": "display-only: the |rule| banner line at battle start"
  },
  "condition:sleepclausemod:onSetStatus": {
   "disposition": "implemented",
   "fp": "fn:33d295fc:onSetStatus(status, target, source) { if (source?.isAlly(target)) { return; } if (status.id === \"slp\") { for (const poke",
   "anchor": "turn.rs::side_has_sleeper",
   "reason": "the clause block inside the drawn SetStatus event (clause formats only)"
  },
  "condition:slp:onBeforeMove": {
   "disposition": "implemented",
   "fp": "fn:605c2352:onBeforeMove(pokemon, target, move) { if (pokemon.hasAbility(\"earlybird\")) { pokemon.statusState.time--; } pokemon.statu",
   "anchor": "turn.rs::run_move",
   "reason": "the draw-free counter decrement + wake; Early Bird double-decrements (earlybird read inline)"
  },
  "condition:slp:onBeforeMovePriority": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::run_move",
   "reason": "the draw-free counter decrement + wake; Early Bird double-decrements (earlybird read inline) — (order metadata of the sibling onBeforeMove handler)"
  },
  "condition:slp:onStart": {
   "disposition": "implemented",
   "fp": "fn:a1388940:onStart(target, source, sourceEffect) { if (sourceEffect && sourceEffect.effectType === \"Move\") { this.add(\"-status\", ta",
   "anchor": "turn.rs::try_set_status",
   "reason": "the status apply + |-status| emit (slp draws its random(2,6) duration at onStart)"
  },
  "condition:slp:onSwitchIn": {
   "disposition": "unreachable_justified",
   "fp": "fn:1d0a77fd:onSwitchIn(target) { this.effectState.time += this.effectState.skippedTime; this.effectState.skippedTime = 0; }",
   "reason": "restores skippedTime — skippedTime is only ever incremented by a `sleepUsable` move (Snore / Sleep Talk, both MOVE_ID_BLOCKLISTed), so it is always 0"
  },
  "condition:snatch:duration": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::run_residuals",
   "reason": "the duration:1 volatile registers a NO_ORDER/subOrder-2 residual duration handler (the SNATCH-mirror tie-shuffle — 8 draws vs the both-Splash control 7, MC104); cleared at turn-top (clear_flinch) / switch-out / faint"
  },
  "condition:snatch:onAnyPrepareHit": {
   "disposition": "implemented",
   "fp": "fn:36e3d85c:onAnyPrepareHit(source, target, move) { const snatchUser = this.effectState.source; if (snatchUser.isSkyDropped()) retur",
   "anchor": "turn.rs::run_status_move",
   "reason": "the INTERCEPTION (gated on the data-derived MoveData::is_snatchable + the foe snatch volatile): removeVolatile FIRST → |-activate|…|move: Snatch|[of] FOE → DeductPP (draw-free no-op) → the snatcher useMove(stolen) via a recursive run_status_move + [from] Snatch → return null (the foe move does nothing); MC100-MC104"
  },
  "condition:snatch:onAnyPrepareHitPriority": {
   "disposition": "implemented",
   "fp": "val:-1",
   "anchor": "turn.rs::run_status_move",
   "reason": "the INTERCEPTION (gated on the data-derived MoveData::is_snatchable + the foe snatch volatile): removeVolatile FIRST → |-activate|…|move: Snatch|[of] FOE → DeductPP (draw-free no-op) → the snatcher useMove(stolen) via a recursive run_status_move + [from] Snatch → return null (the foe move does nothing); MC100-MC104"
  },
  "condition:snatch:onStart": {
   "disposition": "implemented",
   "fp": "fn:90bda241:onStart(pokemon) { this.add(\"-singleturn\", pokemon, \"Snatch\"); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the snatch-cast arm: priority +4 sets MonState::snatch DRAW-FREE + emits |move|U|Snatch|U + |-singleturn|U|Snatch (the onStart line); landed FALSE"
  },
  "condition:spikes:onEntryHazard": {
   "disposition": "implemented",
   "fp": "fn:2baa157a:onEntryHazard(pokemon) { if (!pokemon.isGrounded() || pokemon.hasItem(\"heavydutyboots\")) return; const damageAmounts = [",
   "anchor": "turn.rs::apply_entry_hazards",
   "reason": "the grounded switch-in chip ([_,3,4,6][layers]·maxhp/24, floored, min 1) at the gen3 runSwitch EntryHazard"
  },
  "condition:spikes:onSideRestart": {
   "disposition": "implemented",
   "fp": "fn:fa0c6550:onSideRestart(side) { if (this.effectState.layers >= 3) return false; this.add(\"-sidestart\", side, \"Spikes\"); this.effec",
   "anchor": "turn.rs::run_status_move",
   "reason": "the layer set/increment (cap 3, a 4th FAILS) + |-sidestart| line — the spikes arm"
  },
  "condition:spikes:onSideStart": {
   "disposition": "implemented",
   "fp": "fn:cdada21b:onSideStart(side) { this.add(\"-sidestart\", side, \"Spikes\"); this.effectState.layers = 1; }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the layer set/increment (cap 3, a 4th FAILS) + |-sidestart| line — the spikes arm"
  },
  "condition:stall:counterMax": {
   "disposition": "implemented",
   "fp": "val:8",
   "anchor": "state.rs::protect_counter",
   "reason": "the gen3 stall counter 0→2→4→8 (counterMax 8) + duration-2 expiry (stall_duration)"
  },
  "condition:stall:duration": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "state.rs::protect_counter",
   "reason": "the gen3 stall counter 0→2→4→8 (counterMax 8) + duration-2 expiry (stall_duration)"
  },
  "condition:stall:onRestart": {
   "disposition": "implemented",
   "fp": "fn:f043508f:onRestart() { if (this.effectState.counter < this.effect.counterMax) { this.effectState.counter *= 2; } this.effectState",
   "anchor": "state.rs::protect_counter",
   "reason": "the gen3 stall counter 0→2→4→8 (counterMax 8) + duration-2 expiry (stall_duration)"
  },
  "condition:stall:onStallMove": {
   "disposition": "implemented",
   "fp": "fn:f9351da7:onStallMove() { const counter = this.effectState.counter || 1; if (counter >= 256) { return this.randomChance(1, 2 ** 32",
   "anchor": "turn.rs::run_protect",
   "reason": "the consecutive-protect randomChance(1, counter); the counter>=256 branch is unreachable (counterMax 8)"
  },
  "condition:stall:onStart": {
   "disposition": "implemented",
   "fp": "fn:37b7c571:onStart() { this.effectState.counter = 2; }",
   "anchor": "state.rs::protect_counter",
   "reason": "the gen3 stall counter 0→2→4→8 (counterMax 8) + duration-2 expiry (stall_duration)"
  },
  "condition:substitute:onEnd": {
   "disposition": "noop_justified",
   "fp": "fn:55398a21:onEnd(target) { this.add(\"-end\", target, \"Substitute\"); }",
   "reason": "display-only |-end| Substitute line (the port emits it at the break)"
  },
  "condition:substitute:onStart": {
   "disposition": "implemented",
   "fp": "fn:4c69fbe8:onStart(target, source, effect) { if (effect?.id === \"shedtail\") { this.add(\"-start\", target, \"Substitute\", \"[from] move",
   "anchor": "turn.rs::run_status_move",
   "reason": "the sub create (maxhp/4 cost, draw-free); the shedtail branch is gen9 cruft"
  },
  "condition:substitute:onTryPrimaryHit": {
   "disposition": "implemented",
   "fp": "fn:c4020290:onTryPrimaryHit(target, source, move) { if (target === source || move.flags[\"bypasssub\"]) { return; } let damage = this.",
   "anchor": "turn.rs::absorb_into_sub",
   "reason": "the sub absorb / break / no-carry + blocked status/stat-drop"
  },
  "condition:substitute:onTryPrimaryHitPriority": {
   "disposition": "implemented",
   "fp": "val:-1",
   "anchor": "turn.rs::absorb_into_sub",
   "reason": "the sub absorb / break / no-carry + blocked status/stat-drop — (order metadata of the sibling onTryPrimaryHit handler)"
  },
  "condition:sunnyday:duration": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn.rs::apply_weather_chip",
   "reason": "the weather_turns 5-turn countdown (a MOVE set; ability weather is permanent duration 0; the rock items are gen4)"
  },
  "condition:sunnyday:durationCallback": {
   "disposition": "implemented",
   "fp": "fn:2779616d:durationCallback(source, effect) { if (source?.hasItem(\"heatrock\")) { return 8; } return 5; }",
   "anchor": "turn.rs::apply_weather_chip",
   "reason": "the weather_turns 5-turn countdown (a MOVE set; ability weather is permanent duration 0; the rock items are gen4)"
  },
  "condition:sunnyday:onFieldEnd": {
   "disposition": "implemented",
   "fp": "fn:9dc7d496:onFieldEnd() { this.add(\"-weather\", \"none\"); }",
   "anchor": "turn.rs::apply_weather_chip",
   "reason": "the expiry branch: |-weather|none + the UNCONDITIONAL clearWeather eachEvent(WeatherChange) draw (the round-35 T1 fix)"
  },
  "condition:sunnyday:onFieldResidual": {
   "disposition": "implemented",
   "fp": "fn:d83ce1ee:onFieldResidual() { this.add(\"-weather\", \"SunnyDay\", \"[upkeep]\"); this.eachEvent(\"Weather\"); }",
   "anchor": "turn.rs::run_residuals",
   "reason": "the UNCONDITIONAL eachEvent(Weather) tie-shuffle (the sun_rain_weather_turn_tie fix — fired off RAW weather)"
  },
  "condition:sunnyday:onFieldResidualOrder": {
   "disposition": "implemented",
   "fp": "val:8",
   "anchor": "turn.rs::run_residuals",
   "reason": "the UNCONDITIONAL eachEvent(Weather) tie-shuffle (the sun_rain_weather_turn_tie fix — fired off RAW weather) — (order metadata of the sibling onFieldResidual handler)"
  },
  "condition:sunnyday:onFieldStart": {
   "disposition": "implemented",
   "fp": "fn:c327f383:onFieldStart(battle, source, effect) { if (effect?.effectType === \"Ability\") { if (this.gen <= 5) this.effectState.durat",
   "anchor": "event.rs::run_switch",
   "reason": "the weather set (+ |-weather| line) — ability-sourced permanent OR the modeled_weather_set_move 5-turn arm"
  },
  "condition:sunnyday:onImmunity": {
   "disposition": "implemented",
   "fp": "fn:041698eb:onImmunity(type, pokemon) { if (pokemon.effectiveWeather() !== \"sunnyday\") return; if (type === \"frz\") return false; }",
   "anchor": "turn.rs::try_set_status",
   "reason": "the sun-freeze immunity gate (gen3_sun_freeze_immunity_v1, FZ1)"
  },
  "condition:sunnyday:onWeatherModifyDamage": {
   "disposition": "implemented",
   "fp": "fn:d5fa9857:onWeatherModifyDamage(damage, attacker, defender, move) { if (move.id === \"hydrosteam\" && attacker.effectiveWeather() ==",
   "anchor": "damage.rs::weather",
   "reason": "the Fire/Water ×1.5/×0.5 weather damage folds (hydrosteam is gen9 cruft)"
  },
  "condition:taunt:duration": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "state.rs::taunt",
   "reason": "the FIXED duration-2 taunt volatile (gen3_taunt_disable_v1); start/end lines emitted at the arm/expiry"
  },
  "condition:taunt:onBeforeMove": {
   "disposition": "implemented",
   "fp": "fn:ea630d38:onBeforeMove(attacker, defender, move) { if (move.category === \"Status\") { this.add(\"cant\", attacker, \"move: Taunt\", mov",
   "anchor": "turn.rs::run_move",
   "reason": "the execution-time cant of a QUEUED status move"
  },
  "condition:taunt:onDisableMove": {
   "disposition": "implemented",
   "fp": "fn:8e1800e0:onDisableMove(pokemon) { for (const moveSlot of pokemon.moveSlots) { if (this.dex.moves.get(moveSlot.id).category === \"S",
   "anchor": "turn.rs::move_usable",
   "reason": "Status-category moves unselectable while taunted"
  },
  "condition:taunt:onEnd": {
   "disposition": "implemented",
   "fp": "fn:cc4636df:onEnd(target) { this.add(\"-end\", target, \"move: Taunt\", \"[silent]\"); }",
   "anchor": "state.rs::taunt",
   "reason": "the FIXED duration-2 taunt volatile (gen3_taunt_disable_v1); start/end lines emitted at the arm/expiry"
  },
  "condition:taunt:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::run_residuals",
   "reason": "the duration-bearing taunt volatile's residual TICK slot (order 10 subOrder 15 — the fieldEvent Residual duration decrement; taunt itself has no onResidual fn)"
  },
  "condition:taunt:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::run_residuals",
   "reason": "the duration-bearing taunt volatile's residual TICK slot (order 10 subOrder 15 — the fieldEvent Residual duration decrement; taunt itself has no onResidual fn)"
  },
  "condition:taunt:onStart": {
   "disposition": "implemented",
   "fp": "fn:66b572fc:onStart(target) { this.add(\"-start\", target, \"move: Taunt\"); }",
   "anchor": "state.rs::taunt",
   "reason": "the FIXED duration-2 taunt volatile (gen3_taunt_disable_v1); start/end lines emitted at the arm/expiry"
  },
  "condition:torment:onDisableMove": {
   "disposition": "implemented",
   "fp": "fn:e9369097:onDisableMove(pokemon) { if (pokemon.lastMove && pokemon.lastMove.id !== \"struggle\") pokemon.disableMove(pokemon.lastMov",
   "anchor": "turn.rs::run_status_move",
   "reason": "the torment arm applies the volatile (bare `|-start|<t>|Torment`, NOT `move: Torment`); the onDisableMove restriction is folded into state.rs::move_usable against a LIVE `last_move`, and it JOINS the endTurn DisableMove tie group (speed.rs::disable_move_event_shuffle) — the only draw torment introduces"
  },
  "condition:torment:onEnd": {
   "disposition": "implemented",
   "fp": "fn:6d3304f2:onEnd(pokemon) { this.add(\"-end\", pokemon, \"Torment\"); }",
   "anchor": "turn.rs::execute_switch",
   "reason": "UNREACHABLE in gen 3: nothing removes the volatile mid-battle (no duration, no onResidual), and clearVolatile wipes it SILENTLY without firing End — so the port clears the flag on switch-out/faint and deliberately emits NO `|-end|<t>|Torment`. Emitting one would be a pure protocol divergence; the byte form is UNVERIFIED because no probe can reach it."
  },
  "condition:torment:onStart": {
   "disposition": "implemented",
   "fp": "fn:3ae7b825:onStart(pokemon, source, effect) { if (pokemon.volatiles[\"dynamax\"]) { delete pokemon.volatiles[\"torment\"]; return false",
   "anchor": "turn.rs::run_status_move",
   "reason": "the torment arm applies the volatile (bare `|-start|<t>|Torment`, NOT `move: Torment`); the onDisableMove restriction is folded into state.rs::move_usable against a LIVE `last_move`, and it JOINS the endTurn DisableMove tie group (speed.rs::disable_move_event_shuffle) — the only draw torment introduces"
  },
  "condition:tox:onResidual": {
   "disposition": "implemented",
   "fp": "fn:43b4df96:onResidual(pokemon) { if (this.effectState.stage < 15) { this.effectState.stage++; } this.damage(this.clampIntRange(poke",
   "anchor": "turn.rs::tox_stage",
   "reason": "the ramping stage chip at order 10 subOrder 6"
  },
  "condition:tox:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::tox_stage",
   "reason": "the ramping stage chip at order 10 subOrder 6 — (order metadata of the sibling onResidual handler)"
  },
  "condition:tox:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:6",
   "anchor": "turn.rs::tox_stage",
   "reason": "the ramping stage chip at order 10 subOrder 6 — (order metadata of the sibling onResidual handler)"
  },
  "condition:tox:onStart": {
   "disposition": "implemented",
   "fp": "fn:fcbf9676:onStart(target, source, sourceEffect) { this.effectState.stage = 0; if (sourceEffect && sourceEffect.id === \"toxicorb\") ",
   "anchor": "turn.rs::try_set_status",
   "reason": "the status apply + |-status| emit (slp draws its random(2,6) duration at onStart)"
  },
  "condition:tox:onSwitchIn": {
   "disposition": "implemented",
   "fp": "fn:192a07e2:onSwitchIn() { this.effectState.stage = 0; }",
   "anchor": "turn.rs::stage_reset",
   "reason": "the runSwitch-time toxic stage reset (TX pins)"
  },
  "condition:toxic:duration": {
   "disposition": "unreachable_justified",
   "fp": "val:1",
   "reason": "the gen6+ poison-type-Toxic never-miss volatile — NOTHING in the gen3 resolution adds it (the resolved gen3 toxic move carries no handler referencing it)"
  },
  "condition:toxic:onSourceAccuracy": {
   "disposition": "unreachable_justified",
   "fp": "fn:b1afeec2:onSourceAccuracy(accuracy, target, source, move) { if (move && source === this.effectState.target) return true; }",
   "reason": "the gen6+ poison-type-Toxic never-miss volatile — NOTHING in the gen3 resolution adds it (the resolved gen3 toxic move carries no handler referencing it)"
  },
  "condition:toxic:onSourceInvulnerability": {
   "disposition": "unreachable_justified",
   "fp": "fn:35c357bd:onSourceInvulnerability(target, source, move) { if (move && source === this.effectState.target) return 0; }",
   "reason": "the gen6+ poison-type-Toxic never-miss volatile — NOTHING in the gen3 resolution adds it (the resolved gen3 toxic move carries no handler referencing it)"
  },
  "condition:toxic:onSourceInvulnerabilityPriority": {
   "disposition": "unreachable_justified",
   "fp": "val:1",
   "reason": "the gen6+ poison-type-Toxic never-miss volatile — NOTHING in the gen3 resolution adds it (the resolved gen3 toxic move carries no handler referencing it) — (order metadata of the sibling onSourceInvulnerability handler)"
  },
  "condition:trapped:onStart": {
   "disposition": "implemented",
   "fp": "fn:26d44d10:onStart(target) { this.add(\"-activate\", target, \"trapped\"); }",
   "anchor": "turn.rs::is_trapped",
   "reason": "the FIRM trap (trap_is_firm true — the Shadow-Tag request shape); noCopy FALSE → the Baton Pass snapshot PASSES trapped_by (the link re-points, MC91); the link ends when the trapper leaves ANY way (execute_switch source-left clear + the process_faints corpse clear)"
  },
  "condition:trapped:onTrapPokemon": {
   "disposition": "implemented",
   "fp": "fn:cd8dddd3:onTrapPokemon(pokemon) { pokemon.tryTrap(); }",
   "anchor": "turn.rs::is_trapped",
   "reason": "the FIRM trap (trap_is_firm true — the Shadow-Tag request shape); noCopy FALSE → the Baton Pass snapshot PASSES trapped_by (the link re-points, MC91); the link ends when the trapper leaves ANY way (execute_switch source-left clear + the process_faints corpse clear)"
  },
  "condition:twoturnmove:duration": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::run_residuals",
   "reason": "the duration:2 volatile registers a NO_ORDER/subOrder-2 residual duration handler on BOTH the charge- and fire-turn residuals (the SB-mirror tie-shuffle; after a fire-turn KO the resumed tail residual cleans the linger)"
  },
  "condition:twoturnmove:onEnd": {
   "disposition": "implemented",
   "fp": "fn:6a085fa9:onEnd(target) { target.removeVolatile(this.effectState.move); }",
   "anchor": "turn.rs::run_residuals",
   "reason": "the duration expiry removes the volatile (TwoTurnDuration 2→1→0); its removeVolatile(solarbeam) is a no-op when the beam fired (charging already false)"
  },
  "condition:twoturnmove:onLockMove": {
   "disposition": "implemented",
   "fp": "fn:6914eb29:onLockMove() { return this.effectState.move; }",
   "anchor": "state.rs::move_locked",
   "reason": "the locked single-move fire request (choice_is_legal Move(0)-only + the firm trapped switch-reject; the queue build maps Move(0) to the locked slot)"
  },
  "condition:twoturnmove:onMoveAborted": {
   "disposition": "implemented",
   "fp": "fn:4c80a515:onMoveAborted(pokemon) { pokemon.removeVolatile(\"twoturnmove\"); }",
   "anchor": "turn.rs::run_move",
   "reason": "an onBeforeMove cant clears two_turn entirely — THE CHARGE IS LOST (a fresh charge re-pays PP)"
  },
  "condition:twoturnmove:onStart": {
   "disposition": "implemented",
   "fp": "fn:8ce1af72:onStart(attacker, defender, effect) { this.effectState.move = effect.id; attacker.addVolatile(effect.id); let moveTarget",
   "anchor": "state.rs::two_turn",
   "reason": "TwoTurnMove {move_index, duration:2, charging:true} set at the charge (the solarbeam sub-volatile == charging)"
  },
  "condition:wish:duration": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "state.rs::wish_pending",
   "reason": "the slot condition duration 2 (cast N → decrement to 1 → fire at 0 = end of N+1)"
  },
  "condition:wish:onEnd": {
   "disposition": "implemented",
   "fp": "fn:3dd3a159:onEnd(target) { if (!target.fainted) { const source = this.effectState.source; const damage = this.heal(target.baseMaxhp",
   "anchor": "turn.rs::apply_wish",
   "reason": "the order-7 delayed heal floor(maxhp/2) at N+1 onto the slot occupant (|-heal|…|move: Wish|[wisher]); silent at full HP"
  },
  "condition:wish:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:7",
   "anchor": "turn.rs::apply_wish",
   "reason": "order 7 — the Wish heal fires BEFORE the sand chip (order 8) + all order-10 handlers"
  },
  "condition:yawn:duration": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::run_residuals",
   "reason": "the duration:2 volatile: decrements 2→1 (cast turn) then 1→0 (resolve → fires onEnd); the handler is gathered while yawn.is_some(); cleared on switch-out + faint"
  },
  "condition:yawn:onEnd": {
   "disposition": "implemented",
   "fp": "fn:49a39258:onEnd(target) { this.add(\"-end\", target, \"move: Yawn\", \"[silent]\"); target.trySetStatus(\"slp\", this.effectState.source);",
   "anchor": "turn.rs::run_residuals",
   "reason": "the RESOLVE (ResidualAction::Yawn at order 10 subOrder 19): |-end|…|move: Yawn|[silent] then trySetStatus(slp) via the EXISTING try_set_status path — so the sleep random(2,6) at RESOLVE + the gen3ou Sleep Clause + the SetStatus shuffle come for free; the (10,19) tie-shuffle in a yawn MIRROR (Y1/Y2/Y3)"
  },
  "condition:yawn:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::run_residuals",
   "reason": "the RESOLVE (ResidualAction::Yawn at order 10 subOrder 19): |-end|…|move: Yawn|[silent] then trySetStatus(slp) via the EXISTING try_set_status path — so the sleep random(2,6) at RESOLVE + the gen3ou Sleep Clause + the SetStatus shuffle come for free; the (10,19) tie-shuffle in a yawn MIRROR (Y1/Y2/Y3)"
  },
  "condition:yawn:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:19",
   "anchor": "turn.rs::run_residuals",
   "reason": "the RESOLVE (ResidualAction::Yawn at order 10 subOrder 19): |-end|…|move: Yawn|[silent] then trySetStatus(slp) via the EXISTING try_set_status path — so the sleep random(2,6) at RESOLVE + the gen3ou Sleep Clause + the SetStatus shuffle come for free; the (10,19) tie-shuffle in a yawn MIRROR (Y1/Y2/Y3)"
  },
  "condition:yawn:onStart": {
   "disposition": "implemented",
   "fp": "fn:6eb912fc:onStart(target, source) { this.add(\"-start\", target, \"move: Yawn\", `[of] ${source}`); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the |-start|<t>|move: Yawn|[of] <source> line — EMITTED at the cast (volatile_start_of); DRAW-FREE"
  },
  "item:aguavberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:833097ac:onEat(pokemon) { this.heal(pokemon.baseMaxhp / 8); if (pokemon.getNature().minus === \"spd\") { pokemon.addVolatile(\"confu",
   "anchor": "turn.rs::BE::Heal",
   "reason": "the heal-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:aguavberry:onResidual": {
   "disposition": "implemented",
   "fp": "fn:74d8f67b:onResidual(pokemon) { if (pokemon.hp <= pokemon.maxhp / 2) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6)"
  },
  "item:aguavberry:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:aguavberry:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:aguavberry:onTryEatItem": {
   "disposition": "noop_justified",
   "fp": "fn:74d8d82a:onTryEatItem(item, pokemon) { if (!this.runEvent(\"TryHeal\", pokemon, null, this.effect, pokemon.baseMaxhp / 3)) return f",
   "reason": "the runEvent(TryHeal) guard before a heal-berry eat — NO TryHeal handler exists in the gen3 modeled universe, so the guard is vacuous (the eat always proceeds)"
  },
  "item:apicotberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:338b4359:onEat(pokemon) { this.boost({ spd: 1 }); }",
   "anchor": "turn.rs::BE::Pinch",
   "reason": "the pinch-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:apicotberry:onResidual": {
   "disposition": "implemented",
   "fp": "fn:11035fa9:onResidual(pokemon) { if (pokemon.hp <= pokemon.maxhp / 4) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6)"
  },
  "item:apicotberry:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:apicotberry:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:aspearberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:44c60366:onEat(pokemon) { if (pokemon.status === \"frz\") { pokemon.cureStatus(); } }",
   "anchor": "turn.rs::berry_cure",
   "reason": "the cure-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:aspearberry:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:6a0a84c9:onUpdate(pokemon) { if (pokemon.status === \"frz\") { pokemon.eatItem(); } }",
   "anchor": "turn.rs::berry_on_update",
   "reason": "the Update-site eat (cure/PP berries)"
  },
  "item:blackbelt:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:blackbelt:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:9f12af94:onModifyAtk(atk, user, target, move) { if (move?.type === \"Fighting\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:blackbelt:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:blackglasses:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:blackglasses:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:3e4e740e:onModifySpA(spa, user, target, move) { if (move?.type === \"Dark\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:blackglasses:onModifySpAPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:brightpowder:onModifyAccuracy": {
   "disposition": "implemented",
   "fp": "fn:49bf927a:onModifyAccuracy(accuracy) { if (typeof accuracy !== \"number\") return; this.debug(\"brightpowder - decreasing accuracy\");",
   "anchor": "turn.rs::effective_accuracy",
   "reason": "the DIRECT defender acc multiply (gen3_accuracy_pipeline_v1, AC3)"
  },
  "item:brightpowder:onModifyAccuracyPriority": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn.rs::effective_accuracy",
   "reason": "the DIRECT defender acc multiply (gen3_accuracy_pipeline_v1, AC3) — (order metadata of the sibling onModifyAccuracy handler)"
  },
  "item:charcoal:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:charcoal:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:65eb384a:onModifySpA(spa, user, target, move) { if (move?.type === \"Fire\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:charcoal:onModifySpAPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:cheriberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:d2e7c62b:onEat(pokemon) { if (pokemon.status === \"par\") { pokemon.cureStatus(); } }",
   "anchor": "turn.rs::berry_cure",
   "reason": "the cure-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:cheriberry:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:0850820e:onUpdate(pokemon) { if (pokemon.status === \"par\") { pokemon.eatItem(); } }",
   "anchor": "turn.rs::berry_on_update",
   "reason": "the Update-site eat (cure/PP berries)"
  },
  "item:chestoberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:4fd0a0e5:onEat(pokemon) { if (pokemon.status === \"slp\") { pokemon.cureStatus(); } }",
   "anchor": "turn.rs::berry_cure",
   "reason": "the cure-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:chestoberry:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:d3014c58:onUpdate(pokemon) { if (pokemon.status === \"slp\") { pokemon.eatItem(); } }",
   "anchor": "turn.rs::berry_on_update",
   "reason": "the Update-site eat (cure/PP berries)"
  },
  "item:choiceband:onAfterMove": {
   "disposition": "implemented",
   "fp": "fn:d7de6873:onAfterMove(pokemon) { pokemon.addVolatile(\"choicelock\"); }",
   "anchor": "state.rs::choice_locked_move",
   "reason": "the post-move choicelock arm"
  },
  "item:choiceband:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:d5f3a65c:onModifyAtk(atk, pokemon) { if (pokemon.volatiles[\"dynamax\"]) return; return this.chainModify(1.5); }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the species-gated offensive stat chain"
  },
  "item:choiceband:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the species-gated offensive stat chain"
  },
  "item:deepseascale:onModifySpD": {
   "disposition": "implemented",
   "fp": "fn:df793b3a:onModifySpD(spd, pokemon) { if (pokemon.species.name === \"Clamperl\") { return this.chainModify(2); } }",
   "anchor": "turn.rs::resolve_def_stat_mods",
   "reason": "the species-gated defensive stat chain"
  },
  "item:deepseascale:onModifySpDPriority": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::resolve_def_stat_mods",
   "reason": "the species-gated defensive stat chain"
  },
  "item:deepseatooth:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:f6eb970a:onModifySpA(spa, pokemon) { if (pokemon.species.name === \"Clamperl\") { return this.chainModify(2); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the species-gated offensive stat chain"
  },
  "item:deepseatooth:onModifySpAPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the species-gated offensive stat chain"
  },
  "item:dragonfang:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:dragonfang:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:d8390919:onModifySpA(spa, user, target, move) { if (move?.type === \"Dragon\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:dragonfang:onModifySpAPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:figyberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:d8cc648b:onEat(pokemon) { this.heal(pokemon.baseMaxhp / 8); if (pokemon.getNature().minus === \"atk\") { pokemon.addVolatile(\"confu",
   "anchor": "turn.rs::BE::Heal",
   "reason": "the heal-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:figyberry:onResidual": {
   "disposition": "implemented",
   "fp": "fn:74d8f67b:onResidual(pokemon) { if (pokemon.hp <= pokemon.maxhp / 2) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6)"
  },
  "item:figyberry:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:figyberry:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:figyberry:onTryEatItem": {
   "disposition": "noop_justified",
   "fp": "fn:74d8d82a:onTryEatItem(item, pokemon) { if (!this.runEvent(\"TryHeal\", pokemon, null, this.effect, pokemon.baseMaxhp / 3)) return f",
   "reason": "the runEvent(TryHeal) guard before a heal-berry eat — NO TryHeal handler exists in the gen3 modeled universe, so the guard is vacuous (the eat always proceeds)"
  },
  "item:focusband:onDamage": {
   "disposition": "implemented",
   "fp": "fn:184b7d78:onDamage(damage, target, source, effect) { if (this.randomChance(1, 10) && damage >= target.hp && effect && effect.effec",
   "anchor": "turn.rs::focus_band_damage",
   "reason": "the randomChance(1,10) on EVERY Damage event + survive-at-1 on a lethal MOVE hit"
  },
  "item:focusband:onDamagePriority": {
   "disposition": "implemented",
   "fp": "val:-40",
   "anchor": "turn.rs::focus_band_damage",
   "reason": "the randomChance(1,10) on EVERY Damage event + survive-at-1 on a lethal MOVE hit — (order metadata of the sibling onDamage handler)"
  },
  "item:ganlonberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:ac84aa0b:onEat(pokemon) { this.boost({ def: 1 }); }",
   "anchor": "turn.rs::BE::Pinch",
   "reason": "the pinch-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:ganlonberry:onResidual": {
   "disposition": "implemented",
   "fp": "fn:11035fa9:onResidual(pokemon) { if (pokemon.hp <= pokemon.maxhp / 4) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6)"
  },
  "item:ganlonberry:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:ganlonberry:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:hardstone:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:hardstone:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:5f56604b:onModifyAtk(atk, user, target, move) { if (move?.type === \"Rock\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:hardstone:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:iapapaberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:b7315776:onEat(pokemon) { this.heal(pokemon.baseMaxhp / 8); if (pokemon.getNature().minus === \"def\") { pokemon.addVolatile(\"confu",
   "anchor": "turn.rs::BE::Heal",
   "reason": "the heal-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:iapapaberry:onResidual": {
   "disposition": "implemented",
   "fp": "fn:74d8f67b:onResidual(pokemon) { if (pokemon.hp <= pokemon.maxhp / 2) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6)"
  },
  "item:iapapaberry:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:iapapaberry:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:iapapaberry:onTryEatItem": {
   "disposition": "noop_justified",
   "fp": "fn:74d8d82a:onTryEatItem(item, pokemon) { if (!this.runEvent(\"TryHeal\", pokemon, null, this.effect, pokemon.baseMaxhp / 3)) return f",
   "reason": "the runEvent(TryHeal) guard before a heal-berry eat — NO TryHeal handler exists in the gen3 modeled universe, so the guard is vacuous (the eat always proceeds)"
  },
  "item:kingsrock:onModifyMove": {
   "disposition": "implemented",
   "fp": "fn:915e2237:onModifyMove(move) { const affectedByKingsRock = [ \"aerialace\", \"aeroblast\", \"aircutter\", \"armthrust\", \"barrage\", \"beatu",
   "anchor": "turn.rs::flinch_secondary",
   "reason": "the appended trailing 10% flinch secondary (execution-derived move list)"
  },
  "item:kingsrock:onModifyMovePriority": {
   "disposition": "implemented",
   "fp": "val:-1",
   "anchor": "turn.rs::flinch_secondary",
   "reason": "the appended trailing 10% flinch secondary (execution-derived move list) — (order metadata of the sibling onModifyMove handler)"
  },
  "item:lansatberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:89bc2ac4:onEat(pokemon) { pokemon.addVolatile(\"focusenergy\"); }",
   "anchor": "turn.rs::BE::Pinch",
   "reason": "the pinch-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:lansatberry:onResidual": {
   "disposition": "implemented",
   "fp": "fn:11035fa9:onResidual(pokemon) { if (pokemon.hp <= pokemon.maxhp / 4) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6)"
  },
  "item:lansatberry:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:lansatberry:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:laxincense:onModifyAccuracy": {
   "disposition": "implemented",
   "fp": "fn:d346a95e:onModifyAccuracy(accuracy) { if (typeof accuracy !== \"number\") return; this.debug(\"lax incense - decreasing accuracy\"); ",
   "anchor": "turn.rs::effective_accuracy",
   "reason": "the DIRECT defender acc multiply (gen3_accuracy_pipeline_v1, AC3)"
  },
  "item:laxincense:onModifyAccuracyPriority": {
   "disposition": "implemented",
   "fp": "val:5",
   "anchor": "turn.rs::effective_accuracy",
   "reason": "the DIRECT defender acc multiply (gen3_accuracy_pipeline_v1, AC3) — (order metadata of the sibling onModifyAccuracy handler)"
  },
  "item:leftovers:onResidual": {
   "disposition": "implemented",
   "fp": "fn:8b0f1d3d:onResidual(pokemon) { this.heal(pokemon.baseMaxhp / 16); }",
   "anchor": "turn.rs::run_residuals",
   "reason": "the maxhp/16 heal at order 10 subOrder 4"
  },
  "item:leftovers:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::run_residuals",
   "reason": "the maxhp/16 heal at order 10 subOrder 4 — (order metadata of the sibling onResidual handler)"
  },
  "item:leftovers:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::run_residuals",
   "reason": "the maxhp/16 heal at order 10 subOrder 4 — (order metadata of the sibling onResidual handler)"
  },
  "item:leppaberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:c18554a2:onEat(pokemon) { const moveSlot = pokemon.moveSlots.find((move) => move.pp === 0) || pokemon.moveSlots.find((move) => mo",
   "anchor": "turn.rs::BE::PpRestore",
   "reason": "the pp-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:leppaberry:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:4a3f01df:onUpdate(pokemon) { if (!pokemon.hp) return; if (pokemon.moveSlots.some((move) => move.pp === 0)) { pokemon.eatItem(); }",
   "anchor": "turn.rs::berry_on_update",
   "reason": "the Update-site eat (cure/PP berries)"
  },
  "item:liechiberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:a36c1430:onEat(pokemon) { this.boost({ atk: 1 }); }",
   "anchor": "turn.rs::BE::Pinch",
   "reason": "the pinch-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:liechiberry:onResidual": {
   "disposition": "implemented",
   "fp": "fn:11035fa9:onResidual(pokemon) { if (pokemon.hp <= pokemon.maxhp / 4) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6)"
  },
  "item:liechiberry:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:liechiberry:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:lightball:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the species-gated offensive stat chain"
  },
  "item:lightball:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:36cf2713:onModifySpA(spa, pokemon) { if (pokemon.species.name === \"Pikachu\") { return this.chainModify(2); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the species-gated offensive stat chain"
  },
  "item:lightball:onModifySpAPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the species-gated offensive stat chain"
  },
  "item:luckypunch:onModifyCritRatio": {
   "disposition": "implemented",
   "fp": "fn:ec6c75a5:onModifyCritRatio(critRatio, user) { if (user.species.name === \"Chansey\") { return critRatio + 2; } }",
   "anchor": "turn.rs::effective_crit_ratio",
   "reason": "the CRIT_ITEM +N crit-ratio fold (gen3_crit_item_v1): Scope Lens +1, Lucky Punch +2 Chansey, Stick +2 Farfetch'd — species-gated, draw-free (only the CRIT_MULT denominator index shifts)"
  },
  "item:lumberry:onAfterSetStatus": {
   "disposition": "implemented",
   "fp": "fn:ca2588d8:onAfterSetStatus(status, pokemon) { pokemon.eatItem(); }",
   "anchor": "turn.rs::try_set_status",
   "reason": "LUM's IMMEDIATE eat (priority -1, AFTER Synchronize) at the setStatus tail"
  },
  "item:lumberry:onAfterSetStatusPriority": {
   "disposition": "implemented",
   "fp": "val:-1",
   "anchor": "turn.rs::try_set_status",
   "reason": "LUM's IMMEDIATE eat (priority -1, AFTER Synchronize) at the setStatus tail — (order metadata of the sibling onAfterSetStatus handler)"
  },
  "item:lumberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:a8dac035:onEat(pokemon) { pokemon.cureStatus(); pokemon.removeVolatile(\"confusion\"); }",
   "anchor": "turn.rs::berry_cure",
   "reason": "the cure-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:lumberry:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:fa80f472:onUpdate(pokemon) { if (pokemon.status || pokemon.volatiles[\"confusion\"]) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::berry_on_update",
   "reason": "the Update-site eat (cure/PP berries)"
  },
  "item:magnet:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:magnet:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:d3f5743d:onModifySpA(spa, user, target, move) { if (move?.type === \"Electric\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:magnet:onModifySpAPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:magoberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:3d57df23:onEat(pokemon) { this.heal(pokemon.baseMaxhp / 8); if (pokemon.getNature().minus === \"spe\") { pokemon.addVolatile(\"confu",
   "anchor": "turn.rs::BE::Heal",
   "reason": "the heal-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:magoberry:onResidual": {
   "disposition": "implemented",
   "fp": "fn:74d8f67b:onResidual(pokemon) { if (pokemon.hp <= pokemon.maxhp / 2) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6)"
  },
  "item:magoberry:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:magoberry:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:magoberry:onTryEatItem": {
   "disposition": "noop_justified",
   "fp": "fn:74d8d82a:onTryEatItem(item, pokemon) { if (!this.runEvent(\"TryHeal\", pokemon, null, this.effect, pokemon.baseMaxhp / 3)) return f",
   "reason": "the runEvent(TryHeal) guard before a heal-berry eat — NO TryHeal handler exists in the gen3 modeled universe, so the guard is vacuous (the eat always proceeds)"
  },
  "item:metalcoat:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:metalcoat:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:87c2fc03:onModifyAtk(atk, user, target, move) { if (move?.type === \"Steel\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:metalcoat:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:metalpowder:onModifyDef": {
   "disposition": "implemented",
   "fp": "fn:11ac2fad:onModifyDef(def, pokemon) { if (pokemon.species.name === \"Ditto\" && !pokemon.transformed) { return this.chainModify(2); ",
   "anchor": "turn.rs::resolve_def_stat_mods",
   "reason": "the species-gated defensive stat chain"
  },
  "item:metalpowder:onModifyDefPriority": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::resolve_def_stat_mods",
   "reason": "the species-gated defensive stat chain"
  },
  "item:miracleseed:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:miracleseed:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:6a3583ba:onModifySpA(spa, user, target, move) { if (move?.type === \"Grass\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:miracleseed:onModifySpAPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:mysticwater:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:mysticwater:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:6c749bf3:onModifySpA(spa, user, target, move) { if (move?.type === \"Water\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:mysticwater:onModifySpAPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:nevermeltice:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:nevermeltice:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:c10f9ad7:onModifySpA(spa, user, target, move) { if (move?.type === \"Ice\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:nevermeltice:onModifySpAPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:oddincense:onBasePower": {
   "disposition": "implemented",
   "fp": "fn:98dbb45a:onBasePower(basePower, user, target, move) { if (move.type === \"Psychic\") { return this.chainModify([4915, 4096]); } }",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:oddincense:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:oranberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:83d28413:onEat(pokemon) { this.heal(10); }",
   "anchor": "turn.rs::BE::Heal",
   "reason": "the heal-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:oranberry:onResidual": {
   "disposition": "implemented",
   "fp": "fn:74d8f67b:onResidual(pokemon) { if (pokemon.hp <= pokemon.maxhp / 2) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6)"
  },
  "item:oranberry:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:oranberry:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:oranberry:onTryEatItem": {
   "disposition": "noop_justified",
   "fp": "fn:aeb986a7:onTryEatItem(item, pokemon) { if (!this.runEvent(\"TryHeal\", pokemon, null, this.effect, 10)) return false; }",
   "reason": "the runEvent(TryHeal) guard before a heal-berry eat — NO TryHeal handler exists in the gen3 modeled universe, so the guard is vacuous (the eat always proceeds)"
  },
  "item:pechaberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:b1fd5ebc:onEat(pokemon) { if (pokemon.status === \"psn\" || pokemon.status === \"tox\") { pokemon.cureStatus(); } }",
   "anchor": "turn.rs::berry_cure",
   "reason": "the cure-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:pechaberry:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:719f6533:onUpdate(pokemon) { if (pokemon.status === \"psn\" || pokemon.status === \"tox\") { pokemon.eatItem(); } }",
   "anchor": "turn.rs::berry_on_update",
   "reason": "the Update-site eat (cure/PP berries)"
  },
  "item:persimberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:059b8bfb:onEat(pokemon) { pokemon.removeVolatile(\"confusion\"); }",
   "anchor": "turn.rs::berry_cure",
   "reason": "the cure-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:persimberry:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:7d304595:onUpdate(pokemon) { if (pokemon.volatiles[\"confusion\"]) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::berry_on_update",
   "reason": "the Update-site eat (cure/PP berries)"
  },
  "item:petayaberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:41f52e24:onEat(pokemon) { this.boost({ spa: 1 }); }",
   "anchor": "turn.rs::BE::Pinch",
   "reason": "the pinch-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:petayaberry:onResidual": {
   "disposition": "implemented",
   "fp": "fn:11035fa9:onResidual(pokemon) { if (pokemon.hp <= pokemon.maxhp / 4) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6)"
  },
  "item:petayaberry:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:petayaberry:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:pinkbow:onBasePower": {
   "disposition": "implemented",
   "fp": "fn:e95a691a:onBasePower(basePower, user, target, move) { if (move.type === \"Normal\") { return basePower * 1.1; } }",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:poisonbarb:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:poisonbarb:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:056fbb2a:onModifyAtk(atk, user, target, move) { if (move?.type === \"Poison\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:poisonbarb:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:polkadotbow:onBasePower": {
   "disposition": "implemented",
   "fp": "fn:e95a691a:onBasePower(basePower, user, target, move) { if (move.type === \"Normal\") { return basePower * 1.1; } }",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:quickclaw:onFractionalPriorityPriority": {
   "disposition": "implemented",
   "fp": "val:-2",
   "anchor": "turn.rs::quick_claw",
   "reason": "the gen3 item entry is a bare priority number — the randomChance(1,5) lives in the gen3 action scripts, modeled as the per-turn QC draw"
  },
  "item:rawstberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:ce5e557e:onEat(pokemon) { if (pokemon.status === \"brn\") { pokemon.cureStatus(); } }",
   "anchor": "turn.rs::berry_cure",
   "reason": "the cure-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:rawstberry:onUpdate": {
   "disposition": "implemented",
   "fp": "fn:6610b499:onUpdate(pokemon) { if (pokemon.status === \"brn\") { pokemon.eatItem(); } }",
   "anchor": "turn.rs::berry_on_update",
   "reason": "the Update-site eat (cure/PP berries)"
  },
  "item:rockincense:onBasePower": {
   "disposition": "implemented",
   "fp": "fn:503223e0:onBasePower(basePower, user, target, move) { if (move.type === \"Rock\") { return this.chainModify([4915, 4096]); } }",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:rockincense:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:roseincense:onBasePower": {
   "disposition": "implemented",
   "fp": "fn:7381bd6d:onBasePower(basePower, user, target, move) { if (move.type === \"Grass\") { return this.chainModify([4915, 4096]); } }",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:roseincense:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:salacberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:870404d8:onEat(pokemon) { this.boost({ spe: 1 }); }",
   "anchor": "turn.rs::BE::Pinch",
   "reason": "the pinch-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:salacberry:onResidual": {
   "disposition": "implemented",
   "fp": "fn:11035fa9:onResidual(pokemon) { if (pokemon.hp <= pokemon.maxhp / 4) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6)"
  },
  "item:salacberry:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:salacberry:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:scopelens:onModifyCritRatio": {
   "disposition": "implemented",
   "fp": "fn:998e33ef:onModifyCritRatio(critRatio) { return critRatio + 1; }",
   "anchor": "turn.rs::effective_crit_ratio",
   "reason": "the CRIT_ITEM +N crit-ratio fold (gen3_crit_item_v1): Scope Lens +1, Lucky Punch +2 Chansey, Stick +2 Farfetch'd — species-gated, draw-free (only the CRIT_MULT denominator index shifts)"
  },
  "item:seaincense:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:seaincense:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:d6c7cdcf:onModifySpA(spa, user, target, move) { if (move?.type === \"Water\") { return this.chainModify(1.05); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:seaincense:onModifySpAPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:sharpbeak:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:sharpbeak:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:13f19625:onModifyAtk(atk, user, target, move) { if (move?.type === \"Flying\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:sharpbeak:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:silkscarf:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:silkscarf:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:e9490999:onModifyAtk(atk, user, target, move) { if (move?.type === \"Normal\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:silkscarf:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:silverpowder:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:silverpowder:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:3a2a0a6a:onModifyAtk(atk, user, target, move) { if (move?.type === \"Bug\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:silverpowder:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:sitrusberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:181446b1:onEat(pokemon) { this.heal(30); }",
   "anchor": "turn.rs::BE::Heal",
   "reason": "the heal-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:sitrusberry:onResidual": {
   "disposition": "implemented",
   "fp": "fn:74d8f67b:onResidual(pokemon) { if (pokemon.hp <= pokemon.maxhp / 2) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6)"
  },
  "item:sitrusberry:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:sitrusberry:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:sitrusberry:onTryEatItem": {
   "disposition": "noop_justified",
   "fp": "fn:11801e43:onTryEatItem(item, pokemon) { if (!this.runEvent(\"TryHeal\", pokemon, null, this.effect, pokemon.baseMaxhp / 4)) return f",
   "reason": "the runEvent(TryHeal) guard before a heal-berry eat — NO TryHeal handler exists in the gen3 modeled universe, so the guard is vacuous (the eat always proceeds)"
  },
  "item:softsand:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:softsand:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:631077b9:onModifyAtk(atk, user, target, move) { if (move?.type === \"Ground\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:softsand:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:souldew:onBasePowerPriority": {
   "disposition": "noop_justified",
   "fp": "val:15",
   "reason": "base-data BP priority residue — Soul Dew's gen3 folds are the onModifySpA/onModifySpD stat chains; no onBasePower fn resolves"
  },
  "item:souldew:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:17468b08:onModifySpA(spa, pokemon) { if (pokemon.baseSpecies.num === 380 || pokemon.baseSpecies.num === 381) { return this.chainM",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the species-gated offensive stat chain"
  },
  "item:souldew:onModifySpAPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the species-gated offensive stat chain"
  },
  "item:souldew:onModifySpD": {
   "disposition": "implemented",
   "fp": "fn:f782f018:onModifySpD(spd, pokemon) { if (pokemon.baseSpecies.num === 380 || pokemon.baseSpecies.num === 381) { return this.chainM",
   "anchor": "turn.rs::resolve_def_stat_mods",
   "reason": "the species-gated defensive stat chain"
  },
  "item:souldew:onModifySpDPriority": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::resolve_def_stat_mods",
   "reason": "the species-gated defensive stat chain"
  },
  "item:spelltag:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:spelltag:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:12cef07f:onModifyAtk(atk, user, target, move) { if (move?.type === \"Ghost\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:spelltag:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:starfberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:6fe6199f:onEat(pokemon) { const stats = []; let stat; for (stat in pokemon.boosts) { if (stat !== \"accuracy\" && stat !== \"evasion",
   "anchor": "turn.rs::BE::Pinch",
   "reason": "the pinch-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:starfberry:onResidual": {
   "disposition": "implemented",
   "fp": "fn:11035fa9:onResidual(pokemon) { if (pokemon.hp <= pokemon.maxhp / 4) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6)"
  },
  "item:starfberry:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:starfberry:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:stick:onModifyCritRatio": {
   "disposition": "implemented",
   "fp": "fn:81d86889:onModifyCritRatio(critRatio, user) { if (user.species.id === \"farfetchd\") { return critRatio + 2; } }",
   "anchor": "turn.rs::effective_crit_ratio",
   "reason": "the CRIT_ITEM +N crit-ratio fold (gen3_crit_item_v1): Scope Lens +1, Lucky Punch +2 Chansey, Stick +2 Farfetch'd — species-gated, draw-free (only the CRIT_MULT denominator index shifts)"
  },
  "item:thickclub:onModifyAtk": {
   "disposition": "implemented",
   "fp": "fn:43a78e97:onModifyAtk(atk, pokemon) { if (pokemon.species.name === \"Cubone\" || pokemon.species.name === \"Marowak\") { return this.c",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the species-gated offensive stat chain"
  },
  "item:thickclub:onModifyAtkPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the species-gated offensive stat chain"
  },
  "item:twistedspoon:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:twistedspoon:onModifySpA": {
   "disposition": "implemented",
   "fp": "fn:20395fb3:onModifySpA(spa, user, target, move) { if (move?.type === \"Psychic\") { return this.chainModify(1.1); } }",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:twistedspoon:onModifySpAPriority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::resolve_atk_stat_mods",
   "reason": "the type-gated offensive stat chain (gen3_item_mechanics_v1)"
  },
  "item:waveincense:onBasePower": {
   "disposition": "implemented",
   "fp": "fn:08a3c9d2:onBasePower(basePower, user, target, move) { if (move.type === \"Water\") { return this.chainModify([4915, 4096]); } }",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:waveincense:onBasePowerPriority": {
   "disposition": "implemented",
   "fp": "val:15",
   "anchor": "turn.rs::resolve_bp_mods",
   "reason": "the BP chain / direct-float fold (bows + incenses; the stat-fold members carry only the base-data BP priority residue, same anchor)"
  },
  "item:whiteherb:onAnyAfterMega": {
   "disposition": "unreachable_justified",
   "fp": "fn:2888c020:onAnyAfterMega() { this.effect.onStart.call(this, this.effectState.target); }",
   "reason": "Mega Evolution is gen6+ — no AfterMega event fires in gen3"
  },
  "item:whiteherb:onAnyAfterMove": {
   "disposition": "implemented",
   "fp": "fn:2ff64a31:onAnyAfterMove() { this.effect.onStart.call(this, this.effectState.target); }",
   "anchor": "turn.rs::white_herb_restore",
   "reason": "restore all NEGATIVE boost stages to 0 + consume, single-use (gen3_white_herb_v1); called at the after-move / switch-in stat-drop sites (apply_secondary_boost / apply_self_drops / run_switch / start_with_switchins); DRAW-FREE"
  },
  "item:whiteherb:onAnySwitchIn": {
   "disposition": "implemented",
   "fp": "fn:da1690f1:onAnySwitchIn() { this.effect.onStart.call(this, this.effectState.target); }",
   "anchor": "turn.rs::white_herb_restore",
   "reason": "restore all NEGATIVE boost stages to 0 + consume, single-use (gen3_white_herb_v1); called at the after-move / switch-in stat-drop sites (apply_secondary_boost / apply_self_drops / run_switch / start_with_switchins); DRAW-FREE"
  },
  "item:whiteherb:onAnySwitchInPriority": {
   "disposition": "implemented",
   "fp": "val:-2",
   "anchor": "turn.rs::white_herb_restore",
   "reason": "restore all NEGATIVE boost stages to 0 + consume, single-use (gen3_white_herb_v1); called at the after-move / switch-in stat-drop sites (apply_secondary_boost / apply_self_drops / run_switch / start_with_switchins); DRAW-FREE"
  },
  "item:whiteherb:onResidual": {
   "disposition": "implemented",
   "fp": "fn:1357fd7a:onResidual(pokemon) { this.effect.onStart.call(this, pokemon); }",
   "anchor": "turn.rs::white_herb_restore",
   "reason": "restore all NEGATIVE boost stages to 0 + consume, single-use (gen3_white_herb_v1); called at the after-move / switch-in stat-drop sites (apply_secondary_boost / apply_self_drops / run_switch / start_with_switchins); DRAW-FREE"
  },
  "item:whiteherb:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:29",
   "anchor": "turn.rs::white_herb_restore",
   "reason": "restore all NEGATIVE boost stages to 0 + consume, single-use (gen3_white_herb_v1); called at the after-move / switch-in stat-drop sites (apply_secondary_boost / apply_self_drops / run_switch / start_with_switchins); DRAW-FREE"
  },
  "item:whiteherb:onStart": {
   "disposition": "implemented",
   "fp": "fn:0f9dd333:onStart(pokemon) { this.effectState.boosts = {}; let ready = false; let i; for (i in pokemon.boosts) { if (pokemon.boost",
   "anchor": "turn.rs::white_herb_restore",
   "reason": "restore all NEGATIVE boost stages to 0 + consume, single-use (gen3_white_herb_v1); called at the after-move / switch-in stat-drop sites (apply_secondary_boost / apply_self_drops / run_switch / start_with_switchins); DRAW-FREE"
  },
  "item:whiteherb:onUse": {
   "disposition": "implemented",
   "fp": "fn:e07812f2:onUse(pokemon) { pokemon.setBoost(this.effectState.boosts); this.add(\"-clearnegativeboost\", pokemon, \"[silent]\"); }",
   "anchor": "turn.rs::white_herb_restore",
   "reason": "restore all NEGATIVE boost stages to 0 + consume, single-use (gen3_white_herb_v1); called at the after-move / switch-in stat-drop sites (apply_secondary_boost / apply_self_drops / run_switch / start_with_switchins); DRAW-FREE"
  },
  "item:wikiberry:onEat": {
   "disposition": "implemented",
   "fp": "fn:db8e1ca7:onEat(pokemon) { this.heal(pokemon.baseMaxhp / 8); if (pokemon.getNature().minus === \"spa\") { pokemon.addVolatile(\"confu",
   "anchor": "turn.rs::BE::Heal",
   "reason": "the heal-berry eat effect (gen3_berry_trace_shedskin_v1)"
  },
  "item:wikiberry:onResidual": {
   "disposition": "implemented",
   "fp": "fn:74d8f67b:onResidual(pokemon) { if (pokemon.hp <= pokemon.maxhp / 2) { pokemon.eatItem(); } }",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6)"
  },
  "item:wikiberry:onResidualOrder": {
   "disposition": "implemented",
   "fp": "val:10",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:wikiberry:onResidualSubOrder": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::apply_berry_residual",
   "reason": "the order-10 subOrder-4 threshold eat (heal <=1/2, pinch <=1/4, exact — BR6) — (order metadata of the sibling onResidual handler)"
  },
  "item:wikiberry:onTryEatItem": {
   "disposition": "noop_justified",
   "fp": "fn:74d8d82a:onTryEatItem(item, pokemon) { if (!this.runEvent(\"TryHeal\", pokemon, null, this.effect, pokemon.baseMaxhp / 3)) return f",
   "reason": "the runEvent(TryHeal) guard before a heal-berry eat — NO TryHeal handler exists in the gen3 modeled universe, so the guard is vacuous (the eat always proceeds)"
  },
  "move:absorb:drain": {
   "disposition": "implemented",
   "fp": "val:[1,2]",
   "anchor": "turn.rs::apply_drain",
   "reason": "the drain family (Absorb/Mega/Giga Drain/Leech Life); floor non-sub, ceil behind a sub, draw-free"
  },
  "move:acid:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"boosts\":{\"def\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:acidarmor:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"def\":2}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:acidarmor:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:acidarmor:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:aerialace:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:aeroblast:critRatio": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::CRIT_MULT",
   "reason": "the high-crit denominator table (critRatio 2 => 1/8)"
  },
  "move:agility:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"spe\":2}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:agility:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:agility:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:aircutter:critRatio": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::CRIT_MULT",
   "reason": "the high-crit denominator table (critRatio 2 => 1/8)"
  },
  "move:amnesia:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"spd\":2}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:amnesia:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:amnesia:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:ancientpower:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"self\":{\"boosts\":{\"atk\":1,\"def\":1,\"spa\":1,\"spd\":1,\"spe\":1}}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:armthrust:multihit": {
   "disposition": "implemented",
   "fp": "val:[2,5]",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:aromatherapy:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:aromatherapy:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:aromatherapy:onHit": {
   "disposition": "implemented",
   "fp": "fn:58fd983f:onHit(target, source) { this.add(\"-cureteam\", source, \"[from] move: Aromatherapy\"); const allies = [...target.side.pokem",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Aromatherapy whole-team clearStatus cure (no Soundproof gate, draw-free)"
  },
  "move:astonish:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"volatileStatus\":\"flinch\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:aurorabeam:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"boosts\":{\"atk\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:barrage:multihit": {
   "disposition": "implemented",
   "fp": "val:[2,5]",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:barrier:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"def\":2}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:barrier:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:barrier:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:batonpass:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:batonpass:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:batonpass:onHit": {
   "disposition": "implemented",
   "fp": "fn:d51cfb15:onHit(target) { if (!this.canSwitch(target.side) || target.volatiles[\"commanded\"]) { this.attrLastMove(\"[still]\"); this.",
   "anchor": "turn.rs::run_status_move",
   "reason": "the no-eligible-bench FAIL ([still]+-fail, NOT_FAIL); else set switch_flag + baton_pass_pending for the forced self-switch"
  },
  "move:batonpass:self": {
   "disposition": "noop_justified",
   "fp": "val:{}",
   "reason": "an empty `self: {}` marker (Rapid Spin) — its effect is the onAfterHit/onAfterSubDamage clear"
  },
  "move:batonpass:selfSwitch": {
   "disposition": "implemented",
   "fp": "val:\"copyvolatile\"",
   "anchor": "turn.rs::execute_switch",
   "reason": "copyvolatile: the copyVolatileFrom pass of boosts + the copyable volatiles (substitute/leech-seed/confusion/curse) to the entrant, [from] Baton Pass"
  },
  "move:beatup:onModifyMove": {
   "disposition": "implemented",
   "fp": "fn:1e40f787:onModifyMove(move, pokemon) { pokemon.addVolatile(\"beatup\"); move.type = \"???\"; move.category = \"Special\"; move.allies =",
   "anchor": "turn.rs::run_beat_up",
   "reason": "adds the beatup volatile + sets type ??? / category Special / allies (healthy non-statused party) / multihit; the port runs the per-strike loop with the stat swap"
  },
  "move:bellydrum:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:bellydrum:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:bellydrum:onHit": {
   "disposition": "implemented",
   "fp": "fn:9e44e581:onHit(target) { if (target.hp <= target.maxhp / 2 || target.boosts.atk >= 6 || target.maxhp === 1) { return false; } thi",
   "anchor": "turn.rs::run_status_move",
   "reason": "the bellydrum arm: the FLOAT hp<=maxhp/2 gate as integer-exact 2*hp<=maxhp (262/524 fails, 263 succeeds — MC92) + atk>=6 + maxhp==1 fails; directDamage(floor(maxhp/2)) then the SET to +6 (-setboost)"
  },
  "move:bind:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"partiallytrapped\"",
   "anchor": "turn.rs::is_partial_trap_move",
   "reason": "the post-hit partial-trap arm in run_move: gated on !absorbed (a Substitute intercepts before runMoveEffects — no volatile, NO duration draw) + a LIVE target (addVolatile early-returns on hp 0) + no trap already present (addVolatile returns false, no -fail); sets MonState::partial_trap and emits |-activate|<t>|move: <M>|[of] <u> after the -damage"
  },
  "move:bite:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"volatileStatus\":\"flinch\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:blazekick:critRatio": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::CRIT_MULT",
   "reason": "the high-crit denominator table (critRatio 2 => 1/8)"
  },
  "move:blazekick:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"brn\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:blizzard:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"frz\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:block:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:block:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:block:onHit": {
   "disposition": "implemented",
   "fp": "fn:87e84bff:onHit(target, source, move) { return target.addVolatile(\"trapped\", source, move, \"trapper\"); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the trap-move arm: protect block → substitute block ([still]+-fail) → already-trapped fail → the linked trapped_by = Some(trapper uid); ZERO draws every branch (MC90/MC91)"
  },
  "move:bodyslam:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"status\":\"par\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:boneclub:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"volatileStatus\":\"flinch\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:bonemerang:multihit": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:bonerush:multihit": {
   "disposition": "implemented",
   "fp": "val:[2,5]",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:brickbreak:onTryHit": {
   "disposition": "unreachable_justified",
   "fp": "fn:9f3bf201:onTryHit(target, source) { const foe = source.side.foe; foe.removeSideCondition(\"reflect\"); foe.removeSideCondition(\"lig",
   "reason": "removes Reflect/Light Screen before the hit — the screens are unmodeled SIDE CONDITIONS no modeled move can set, so there is never a screen to break (the move's damaging path is fully modeled)"
  },
  "move:bubble:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"boosts\":{\"spe\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:bubblebeam:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"boosts\":{\"spe\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:bulkup:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"atk\":1,\"def\":1}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:bulkup:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:bulkup:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:bulletseed:multihit": {
   "disposition": "implemented",
   "fp": "val:[2,5]",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:calmmind:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"spa\":1,\"spd\":1}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:calmmind:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:calmmind:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:charge:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:charge:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:charge:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"charge\"",
   "anchor": "turn.rs::run_status_move",
   "reason": "the charge arm sets MonState::charge (onRestart re-adds, -start again); gen3 has NO +1 SpD"
  },
  "move:charm:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"atk\":-2}",
   "anchor": "turn.rs::stat_drop_boosts",
   "reason": "the standalone foe stat-drop arm (Screech/Charm/Metal Sound/… — gen3_moves.json statDropBoosts; accuracy roll + a draw-free boost, gen3_move_coverage_batch2_v1)"
  },
  "move:charm:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:clamp:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"partiallytrapped\"",
   "anchor": "turn.rs::is_partial_trap_move",
   "reason": "the post-hit partial-trap arm in run_move: gated on !absorbed (a Substitute intercepts before runMoveEffects — no volatile, NO duration draw) + a LIVE target (addVolatile early-returns on hp 0) + no trap already present (addVolatile returns false, no -fail); sets MonState::partial_trap and emits |-activate|<t>|move: <M>|[of] <u> after the -damage"
  },
  "move:cometpunch:multihit": {
   "disposition": "implemented",
   "fp": "val:[2,5]",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:confuseray:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:confuseray:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"confusion\"",
   "anchor": "secondaries.rs::add_confusion",
   "reason": "the confusion volatile via the shared add_confusion — the same path Water Pulse & co use, owning the KO / already-confused / Own-Tempo gates and the random(2,6) duration draw (gen3_confuse_ray_v1)"
  },
  "move:confusion:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"volatileStatus\":\"confusion\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:constrict:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"boosts\":{\"spe\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:conversion2:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::conversion2_candidates",
   "reason": "keys off the TARGET's lastMoveUsed (MonState::last_move_used, reset on switch-out) and type-changes the SOURCE. The list is every type whose damageTaken[attackType] is resist or immune, built in CONVERSION2_TYPE_ORDER — the typedex FILE key order, neither alphabetical nor the port Type enum order — because the single random(n) indexes into it. No lastMoveUsed, or a typeless ??? last move (Curse), gives an empty list and a draw-free [still]+-fail. Extracted as a pure fn so the order is pinned seed-free against the sim's own dex dump (all 17 rows)."
  },
  "move:conversion2:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::conversion2_candidates",
   "reason": "keys off the TARGET's lastMoveUsed (MonState::last_move_used, reset on switch-out) and type-changes the SOURCE. The list is every type whose damageTaken[attackType] is resist or immune, built in CONVERSION2_TYPE_ORDER — the typedex FILE key order, neither alphabetical nor the port Type enum order — because the single random(n) indexes into it. No lastMoveUsed, or a typeless ??? last move (Curse), gives an empty list and a draw-free [still]+-fail. Extracted as a pure fn so the order is pinned seed-free against the sim's own dex dump (all 17 rows)."
  },
  "move:conversion2:onHit": {
   "disposition": "implemented",
   "fp": "fn:5579b6e0:onHit(target, source) { if (!target.lastMoveUsed) { return false; } const possibleTypes = []; const lastMoveUsed = targe",
   "anchor": "turn.rs::conversion2_candidates",
   "reason": "keys off the TARGET's lastMoveUsed (MonState::last_move_used, reset on switch-out) and type-changes the SOURCE. The list is every type whose damageTaken[attackType] is resist or immune, built in CONVERSION2_TYPE_ORDER — the typedex FILE key order, neither alphabetical nor the port Type enum order — because the single random(n) indexes into it. No lastMoveUsed, or a typeless ??? last move (Curse), gives an empty list and a draw-free [still]+-fail. Extracted as a pure fn so the order is pinned seed-free against the sim's own dex dump (all 17 rows)."
  },
  "move:conversion:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn/status_moves.rs::run_status_move",
   "reason": "the conversion arm: never-miss so no accuracy roll; the candidate list is the USER's LIVE move slots IN SLOT ORDER keeping move.type for every slot whose id != \"curse\" and whose type the user does not CURRENTLY have, DUPLICATES KEPT. Exactly ONE random(n) on success (n==1 still draws) and ZERO draws when the list is empty ([still] + a bare -fail on the USER). setType replaces the whole array with one type via types_override, which already reverts on switch-out for Color Change. LIVE matters three ways: a Mimic-overwritten slot contributes the COPIED type, a Color-Changed type excludes its own move, and a second Conversion sees the first result (which is how Conversion's own Normal slot enters the list once the user is no longer Normal)."
  },
  "move:conversion:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn/status_moves.rs::run_status_move",
   "reason": "the conversion arm: never-miss so no accuracy roll; the candidate list is the USER's LIVE move slots IN SLOT ORDER keeping move.type for every slot whose id != \"curse\" and whose type the user does not CURRENTLY have, DUPLICATES KEPT. Exactly ONE random(n) on success (n==1 still draws) and ZERO draws when the list is empty ([still] + a bare -fail on the USER). setType replaces the whole array with one type via types_override, which already reverts on switch-out for Color Change. LIVE matters three ways: a Mimic-overwritten slot contributes the COPIED type, a Color-Changed type excludes its own move, and a second Conversion sees the first result (which is how Conversion's own Normal slot enters the list once the user is no longer Normal)."
  },
  "move:conversion:onHit": {
   "disposition": "implemented",
   "fp": "fn:4ebcf449:onHit(target) { const possibleTypes = target.moveSlots.map((moveSlot) => { const move = this.dex.moves.get(moveSlot.id);",
   "anchor": "turn/status_moves.rs::run_status_move",
   "reason": "the conversion arm: never-miss so no accuracy roll; the candidate list is the USER's LIVE move slots IN SLOT ORDER keeping move.type for every slot whose id != \"curse\" and whose type the user does not CURRENTLY have, DUPLICATES KEPT. Exactly ONE random(n) on success (n==1 still draws) and ZERO draws when the list is empty ([still] + a bare -fail on the USER). setType replaces the whole array with one type via types_override, which already reverts on switch-out for Color Change. LIVE matters three ways: a Mimic-overwritten slot contributes the COPIED type, a Color-Changed type excludes its own move, and a second Conversion sees the first result (which is how Conversion's own Normal slot enters the list once the user is no longer Normal)."
  },
  "move:cosmicpower:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"def\":1,\"spd\":1}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:cosmicpower:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:cosmicpower:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:cottonspore:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"spe\":-2}",
   "anchor": "turn.rs::stat_drop_boosts",
   "reason": "the standalone foe stat-drop arm (Screech/Charm/Metal Sound/… — gen3_moves.json statDropBoosts; accuracy roll + a draw-free boost, gen3_move_coverage_batch2_v1)"
  },
  "move:cottonspore:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:counter:onTry": {
   "disposition": "implemented",
   "fp": "fn:36e273fc:onTry(source) { if (!source.volatiles[\"counter\"]) return false; if (source.volatiles[\"counter\"].slot === null) return fa",
   "anchor": "turn.rs::run_fixed_damage_move",
   "reason": "the ZERO-DRAW un-armed fail (no volatile / slot===null): a bare |move| line, no -fail, PP already deducted — BEFORE the accuracy roll"
  },
  "move:counter:priority": {
   "disposition": "implemented",
   "fp": "val:-5",
   "anchor": "turn.rs::move_priority",
   "reason": "the action-order priority sort (draws only on a priority+speed tie)"
  },
  "move:covet:onAfterHit": {
   "disposition": "implemented",
   "fp": "fn:a3097623:onAfterHit(target, source, move) { if (source.item || source.volatiles[\"gem\"]) { return; } const yourItem = target.takeI",
   "anchor": "turn.rs::apply_item_removal",
   "reason": "the Knock Off/Thief/Covet item removal (Sticky Hold block + the gen3 itemKnockedOff gate)"
  },
  "move:crabhammer:critRatio": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::CRIT_MULT",
   "reason": "the high-crit denominator table (critRatio 2 => 1/8)"
  },
  "move:crosschop:critRatio": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::CRIT_MULT",
   "reason": "the high-crit denominator table (critRatio 2 => 1/8)"
  },
  "move:crunch:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":20,\"boosts\":{\"spd\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:crushclaw:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":50,\"boosts\":{\"def\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:curse:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:curse:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:curse:nonGhostTarget": {
   "disposition": "implemented",
   "fp": "val:\"self\"",
   "anchor": "turn.rs::run_status_move",
   "reason": "the NON-GHOST self-redirect target (`self`) — the port renders the announce/boost on the USER"
  },
  "move:curse:onHit": {
   "disposition": "implemented",
   "fp": "fn:fd16995e:onHit(target, source) { this.directDamage(source.maxhp / 2, source, source); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the GHOST branch: lay the curse volatile on the foe THEN pay floor(maxhp/2) HP (draw-free)"
  },
  "move:curse:onModifyMove": {
   "disposition": "implemented",
   "fp": "fn:175f9c03:onModifyMove(move, source, target) { if (!source.hasType(\"Ghost\")) { delete move.volatileStatus; delete move.onHit; move",
   "anchor": "turn.rs::run_status_move",
   "reason": "the type-conditional re-target: NON-GHOST → move.self={boosts:{atk:1,def:1,spe:-1}} + target self (the selfDrops random(100) + boost); GHOST-into-a-sub deletes both (does nothing → [still]+-fail). (The resolved dist splits the logic across onModifyMove + onTryHit; the port handles both in the curse arm.)"
  },
  "move:curse:onTryHit": {
   "disposition": "implemented",
   "fp": "fn:c5ffaf7a:onTryHit(target, source, move) { if (!source.hasType(\"Ghost\")) { delete move.volatileStatus; delete move.onHit; move.sel",
   "anchor": "turn.rs::run_status_move",
   "reason": "the type-conditional re-target: NON-GHOST → move.self={boosts:{atk:1,def:1,spe:-1}} + target self (the selfDrops random(100) + boost); GHOST-into-a-sub deletes both (does nothing → [still]+-fail). (The resolved dist splits the logic across onModifyMove + onTryHit; the port handles both in the curse arm.)"
  },
  "move:curse:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"curse\"",
   "anchor": "state.rs::curse",
   "reason": "the `curse` volatile marker laid on the foe (Some(source_side))"
  },
  "move:destinybond:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:destinybond:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:destinybond:onPrepareHit": {
   "disposition": "implemented",
   "fp": "fn:b84c06e0:onPrepareHit(pokemon) { pokemon.removeVolatile(\"destinybond\"); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the DB arm: a ZERO-draw cast; the re-cast draw-free self-removes+re-adds (the flag is idempotent) — |-singlemove| each cast, PP −1 each"
  },
  "move:destinybond:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"destinybond\"",
   "anchor": "turn.rs::run_status_move",
   "reason": "the DB arm: a ZERO-draw cast; the re-cast draw-free self-removes+re-adds (the flag is idempotent) — |-singlemove| each cast, PP −1 each"
  },
  "move:detect:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:detect:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:detect:onHit": {
   "disposition": "implemented",
   "fp": "fn:fd8966cf:onHit(pokemon) { pokemon.addVolatile(\"stall\"); }",
   "anchor": "state.rs::protect_counter",
   "reason": "the stall volatile (re)add on success"
  },
  "move:detect:onPrepareHit": {
   "disposition": "implemented",
   "fp": "fn:90156567:onPrepareHit(pokemon) { return !!this.queue.willAct() && this.runEvent(\"StallMove\", pokemon); }",
   "anchor": "turn.rs::run_protect",
   "reason": "the willAct + StallMove gate (first protect short-circuits draw-free)"
  },
  "move:detect:priority": {
   "disposition": "implemented",
   "fp": "val:3",
   "anchor": "turn.rs::move_priority",
   "reason": "the action-order priority sort (draws only on a priority+speed tie)"
  },
  "move:detect:stallingMove": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_protect",
   "reason": "the protect/detect stall machinery"
  },
  "move:detect:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"protect\"",
   "anchor": "turn.rs::run_protect"
  },
  "move:disable:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:disable:onTryHit": {
   "disposition": "implemented",
   "fp": "fn:043c7017:onTryHit(target) { if (!target.lastMove || target.lastMove.isZOrMaxPowered || target.lastMove.isMax || target.lastMove.i",
   "anchor": "turn.rs::run_status_move",
   "reason": "the no-lastMove draw-free fail (gen3_taunt_disable_v1)"
  },
  "move:disable:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"disable\"",
   "anchor": "state.rs::disable",
   "reason": "the disable volatile (gen3_taunt_disable_v1)"
  },
  "move:dizzypunch:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":20,\"volatileStatus\":\"confusion\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:doomdesire:onTry": {
   "disposition": "implemented",
   "fp": "fn:4d6fe38f:onTry(source, target) { if (!target.side.addSlotCondition(target, \"futuremove\")) return false; const moveData = { name: ",
   "anchor": "turn.rs::run_future_move_cast",
   "reason": "the cast: addSlotCondition futuremove (a double-cast fails with a bare |move|, zero draws, PP still deducted) + ONE random(16) getDamage snapshot (typeless, cast-time stats, willCrit false) + |-start|<caster>|<Name>; BEFORE the protect block (a cast-turn Protect does not block)"
  },
  "move:doubleedge:recoil": {
   "disposition": "implemented",
   "fp": "val:[1,3]",
   "anchor": "turn.rs::apply_recoil",
   "reason": "the recoil family (Double-Edge/Take Down/Submission); Rock Head negates, draw-free"
  },
  "move:doublekick:multihit": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:doubleslap:multihit": {
   "disposition": "implemented",
   "fp": "val:[2,5]",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:doubleteam:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"evasion\":1}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:doubleteam:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:doubleteam:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:dragonbreath:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"status\":\"par\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:dragondance:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"atk\":1,\"spe\":1}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:dragondance:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:dragondance:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:dragonrage:damage": {
   "disposition": "implemented",
   "fp": "val:40",
   "anchor": "turn.rs::fixed_damage_amount",
   "reason": "the fixed/level damage declarative (Seismic Toss/Night Shade level; Sonic Boom 20; Dragon Rage 40) — accuracy-only draw, no crit/damage roll (gen3_fixeddamage_v1; surfaced by the batch-5 blocklist un-shadowing)"
  },
  "move:dynamicpunch:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":100,\"volatileStatus\":\"confusion\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:ember:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"brn\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:encore:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the encore arm applies the volatile; Status-default ignoreImmunity (no type gate)"
  },
  "move:encore:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"encore\"",
   "anchor": "turn.rs::run_status_move",
   "reason": "the encore arm applies the volatile; Status-default ignoreImmunity (no type gate)"
  },
  "move:endeavor:onTry": {
   "disposition": "implemented",
   "fp": "fn:7439038e:onTry(pokemon, target) { if (pokemon.hp >= target.hp) { this.add(\"-fail\", pokemon); return null; } }",
   "anchor": "turn.rs::run_fixed_damage_move",
   "reason": "the ZERO-DRAW `hp >= target.hp` fail (EQUALITY INCLUDED — probed 50v50 fails): target-form announce + |-fail|<user>, BEFORE the accuracy roll"
  },
  "move:endeavor:onTryImmunity": {
   "disposition": "implemented",
   "fp": "fn:9e8a4a8b:onTryImmunity(target, pokemon) { return pokemon.hp < target.hp; }",
   "anchor": "turn.rs::run_fixed_damage_move",
   "reason": "pokemon.hp < target.hp — the same compare as the onTry (the port gates once, before accuracy); the Normal→Ghost type immunity rides move_is_immune after the accuracy draw"
  },
  "move:endure:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:endure:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:endure:onHit": {
   "disposition": "implemented",
   "fp": "fn:fd8966cf:onHit(pokemon) { pokemon.addVolatile(\"stall\"); }",
   "anchor": "turn.rs::run_protect",
   "reason": "the endure arm rides the SHARED protect stallingMove machinery (willAct gate + randomChance(1,counter) 2→4→8 no-delete-on-fail; priority 4); success sets MonState::endure"
  },
  "move:endure:onPrepareHit": {
   "disposition": "implemented",
   "fp": "fn:90156567:onPrepareHit(pokemon) { return !!this.queue.willAct() && this.runEvent(\"StallMove\", pokemon); }",
   "anchor": "turn.rs::run_protect",
   "reason": "the endure arm rides the SHARED protect stallingMove machinery (willAct gate + randomChance(1,counter) 2→4→8 no-delete-on-fail; priority 4); success sets MonState::endure"
  },
  "move:endure:priority": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::run_protect",
   "reason": "the endure arm rides the SHARED protect stallingMove machinery (willAct gate + randomChance(1,counter) 2→4→8 no-delete-on-fail; priority 4); success sets MonState::endure"
  },
  "move:endure:stallingMove": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_protect",
   "reason": "the endure arm rides the SHARED protect stallingMove machinery (willAct gate + randomChance(1,counter) 2→4→8 no-delete-on-fail; priority 4); success sets MonState::endure"
  },
  "move:endure:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"endure\"",
   "anchor": "turn.rs::run_protect",
   "reason": "the endure arm rides the SHARED protect stallingMove machinery (willAct gate + randomChance(1,counter) 2→4→8 no-delete-on-fail; priority 4); success sets MonState::endure"
  },
  "move:explosion:selfdestruct": {
   "disposition": "implemented",
   "fp": "val:\"always\"",
   "anchor": "turn.rs::pending_explosion_self_ko",
   "reason": "the pre-hit unconditional self-KO (gen3_explosion_v1)"
  },
  "move:extrasensory:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"volatileStatus\":\"flinch\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:extremespeed:priority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::move_priority",
   "reason": "the action-order priority sort (draws only on a priority+speed tie)"
  },
  "move:facade:onBasePower": {
   "disposition": "implemented",
   "fp": "fn:3f28aa69:onBasePower(basePower, pokemon) { if (pokemon.status && pokemon.status !== \"slp\") { return this.chainModify(2); } }",
   "anchor": "turn.rs::run_move",
   "reason": "the ×2-when-statused BP chain member (gen3_facade_v1 — id-gated at the facade site)"
  },
  "move:fakeout:onTry": {
   "disposition": "implemented",
   "fp": "fn:f516a5b4:onTry(source) { if (source.activeMoveActions > 1) { this.hint(\"Fake Out only works on your first turn out.\"); return fal",
   "anchor": "turn/moves.rs::run_move",
   "reason": "the first-turn gate: `active_move_actions > 1` short-circuits the move DRAW-FREE (before the accuracy roll) with the announce plus |-hint|Fake Out only works on your first turn out. — NO [still] attr, NO -fail, landed=false, and `once=false` so a repeat emits the hint EVERY time. PP is still paid (the sim deducts before onTry). The counter is MOVE ACTIONS, not turns: a CANT-ed turn increments it (the sim bumps activeMoveActions at the top of runMove, before BeforeMove) while a turn whose action was CANCELLED by gen-3 faint-cancels-all does not."
  },
  "move:fakeout:priority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn/moves.rs::run_move",
   "reason": "gen-3 Fake Out is priority +1 (NOT the modern +3) — read from the dex row by the ordinary action sort, no special case."
  },
  "move:fakeout:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":100,\"volatileStatus\":\"flinch\"}]",
   "anchor": "turn/secondaries.rs::apply_secondaries",
   "reason": "the chance-100 flinch secondary STILL rolls its random(100) — the ordinary secondary path, no special case."
  },
  "move:faketears:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"spd\":-2}",
   "anchor": "turn.rs::stat_drop_boosts",
   "reason": "the standalone foe stat-drop arm (Screech/Charm/Metal Sound/… — gen3_moves.json statDropBoosts; accuracy roll + a draw-free boost, gen3_move_coverage_batch2_v1)"
  },
  "move:faketears:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:featherdance:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"atk\":-2}",
   "anchor": "turn.rs::stat_drop_boosts",
   "reason": "the standalone foe stat-drop arm (Screech/Charm/Metal Sound/… — gen3_moves.json statDropBoosts; accuracy roll + a draw-free boost, gen3_move_coverage_batch2_v1)"
  },
  "move:featherdance:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:feintattack:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:fireblast:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"brn\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:firepunch:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"brn\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:firespin:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"partiallytrapped\"",
   "anchor": "turn.rs::is_partial_trap_move",
   "reason": "the post-hit partial-trap arm in run_move: gated on !absorbed (a Substitute intercepts before runMoveEffects — no volatile, NO duration draw) + a LIVE target (addVolatile early-returns on hp 0) + no trap already present (addVolatile returns false, no -fail); sets MonState::partial_trap and emits |-activate|<t>|move: <M>|[of] <u> after the -damage"
  },
  "move:flamethrower:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"brn\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:flamewheel:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"brn\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:flash:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"accuracy\":-1}",
   "anchor": "turn.rs::stat_drop_boosts",
   "reason": "the standalone foe stat-drop arm (Screech/Charm/Metal Sound/… — gen3_moves.json statDropBoosts; accuracy roll + a draw-free boost, gen3_move_coverage_batch2_v1)"
  },
  "move:flash:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:focuspunch:onTry": {
   "disposition": "implemented",
   "fp": "fn:dbac830a:onTry(pokemon) { if (pokemon.volatiles[\"focuspunch\"]?.lostFocus) { this.attrLastMove(\"[still]\"); this.add(\"cant\", pokemo",
   "anchor": "turn.rs::run_move",
   "reason": "the onTry cancel BEFORE accuracy iff the focuspunch volatile is lostFocus (|move|…||[still] + |cant|…Focus Punch); PP/lastMove already deducted"
  },
  "move:focuspunch:priority": {
   "disposition": "implemented",
   "fp": "val:-3",
   "anchor": "turn.rs::move_priority",
   "reason": "the action-order priority sort (draws only on a priority+speed tie)"
  },
  "move:furyattack:multihit": {
   "disposition": "implemented",
   "fp": "val:[2,5]",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:furyswipes:multihit": {
   "disposition": "implemented",
   "fp": "val:[2,5]",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:futuresight:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_future_move_cast",
   "reason": "the strike is TYPELESS ??? (move_type None — no chart row, never immune), so the declarative ignoreImmunity is subsumed"
  },
  "move:futuresight:onTry": {
   "disposition": "implemented",
   "fp": "fn:8bb2f909:onTry(source, target) { if (!target.side.addSlotCondition(target, \"futuremove\")) return false; const moveData = { name: ",
   "anchor": "turn.rs::run_future_move_cast",
   "reason": "the cast: addSlotCondition futuremove (a double-cast fails with a bare |move|, zero draws, PP still deducted) + ONE random(16) getDamage snapshot (typeless, cast-time stats, willCrit false) + |-start|<caster>|<Name>; BEFORE the protect block (a cast-turn Protect does not block)"
  },
  "move:gigadrain:drain": {
   "disposition": "implemented",
   "fp": "val:[1,2]",
   "anchor": "turn.rs::apply_drain",
   "reason": "the drain family (Absorb/Mega/Giga Drain/Leech Life); floor non-sub, ceil behind a sub, draw-free"
  },
  "move:glare:status": {
   "disposition": "implemented",
   "fp": "val:\"par\"",
   "anchor": "turn.rs::modeled_status_move",
   "reason": "the standalone status-inflicting move arm (accuracy + try_set_status)"
  },
  "move:grasswhistle:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:grasswhistle:status": {
   "disposition": "implemented",
   "fp": "val:\"slp\"",
   "anchor": "turn.rs::modeled_status_move",
   "reason": "the standalone status-inflicting move arm (accuracy + try_set_status)"
  },
  "move:growth:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"spa\":1}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:growth:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:growth:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:hail:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:hail:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:hail:weather": {
   "disposition": "implemented",
   "fp": "val:\"hail\"",
   "anchor": "turn.rs::modeled_weather_set_move",
   "reason": "the 5-turn timed weather-set arm (Rain Dance/Sunny Day; setWeather fails into the same weather, overwrites a different one)"
  },
  "move:harden:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"def\":1}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:harden:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:harden:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:haze:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:haze:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:haze:onHitField": {
   "disposition": "implemented",
   "fp": "fn:8eede83c:onHitField() { this.add(\"-clearallboost\"); for (const pokemon of this.getAllActive()) { pokemon.clearBoosts(); } }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the haze arm (gen3_haze_v1): ONE |-clearallboost line + getAllActive().clearBoosts() zeroes BOTH actives' 7 boost stages incl. the user's own; DRAW-FREE, landed FALSE"
  },
  "move:headbutt:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"volatileStatus\":\"flinch\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:healbell:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:healbell:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:healbell:onHit": {
   "disposition": "implemented",
   "fp": "fn:193a3f47:onHit(target, source) { this.add(\"-activate\", source, \"move: Heal Bell\"); const allies = [...target.side.pokemon, ...tar",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Heal Bell whole-team cure (active + bench; SKIPS a Soundproof ally, draw-free)"
  },
  "move:heatwave:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"brn\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:highjumpkick:onMoveFail": {
   "disposition": "implemented",
   "fp": "fn:5859cdde:onMoveFail(target, source, move) { if (target.runImmunity(\"Fighting\")) { const damage = this.actions.getDamage(source, t",
   "anchor": "turn.rs::apply_jump_kick_crash",
   "reason": "the crash on miss/protect-block: getDamage-with-crit+roll halved, clamp [1, floor(target.maxhp/2)] (gen3_jump_kick_crash_v1 — the audit's HA1 fix)"
  },
  "move:howl:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"atk\":1}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:howl:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:howl:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:hyperbeam:self": {
   "disposition": "implemented",
   "fp": "val:{\"volatileStatus\":\"mustrecharge\"}",
   "anchor": "turn.rs::run_move",
   "reason": "self.volatileStatus mustrecharge — applied DRAW-FREE on a SUCCESSFUL damaging hit (plain/sub-absorb/sub-break/target-KO; NOT miss/immune/Protect-block), |-mustrecharge| after the damage/sub line"
  },
  "move:hyperfang:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"volatileStatus\":\"flinch\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:hypnosis:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:hypnosis:status": {
   "disposition": "implemented",
   "fp": "val:\"slp\"",
   "anchor": "turn.rs::modeled_status_move",
   "reason": "the standalone status-inflicting move arm (accuracy + try_set_status)"
  },
  "move:icebeam:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"frz\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:icepunch:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"frz\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:iciclespear:multihit": {
   "disposition": "implemented",
   "fp": "val:[2,5]",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:icywind:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":100,\"boosts\":{\"spe\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:irondefense:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"def\":2}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:irondefense:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:irondefense:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:irontail:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"boosts\":{\"def\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:jumpkick:onMoveFail": {
   "disposition": "implemented",
   "fp": "fn:6a0103c4:onMoveFail(target, source, move) { if (target.runImmunity(\"Fighting\")) { const damage = this.actions.getDamage(source, t",
   "anchor": "turn.rs::apply_jump_kick_crash",
   "reason": "the crash on miss/protect-block: getDamage-with-crit+roll halved, clamp [1, floor(target.maxhp/2)] (gen3_jump_kick_crash_v1 — the audit's HA1 fix)"
  },
  "move:karatechop:critRatio": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::CRIT_MULT",
   "reason": "the high-crit denominator table (critRatio 2 => 1/8)"
  },
  "move:kinesis:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"accuracy\":-1}",
   "anchor": "turn.rs::stat_drop_boosts",
   "reason": "the standalone foe stat-drop arm (Screech/Charm/Metal Sound/… — gen3_moves.json statDropBoosts; accuracy roll + a draw-free boost, gen3_move_coverage_batch2_v1)"
  },
  "move:kinesis:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:knockoff:onAfterHit": {
   "disposition": "implemented",
   "fp": "fn:45f5ce6c:onAfterHit(target, source, move) { if (!target.item) return; const item = target.getItem(); if (this.runEvent(\"TakeItem\"",
   "anchor": "turn.rs::apply_item_removal",
   "reason": "the Knock Off/Thief/Covet item removal (Sticky Hold block + the gen3 itemKnockedOff gate)"
  },
  "move:leafblade:critRatio": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::CRIT_MULT",
   "reason": "the high-crit denominator table (critRatio 2 => 1/8)"
  },
  "move:leechlife:drain": {
   "disposition": "implemented",
   "fp": "val:[1,2]",
   "anchor": "turn.rs::apply_drain",
   "reason": "the drain family (Absorb/Mega/Giga Drain/Leech Life); floor non-sub, ceil behind a sub, draw-free"
  },
  "move:leechseed:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:leechseed:onTryImmunity": {
   "disposition": "implemented",
   "fp": "fn:ee9691fd:onTryImmunity(target) { return !target.hasType(\"Grass\"); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Grass-type immunity (accuracy drawn, then -immune)"
  },
  "move:leechseed:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"leechseed\"",
   "anchor": "turn.rs::apply_leech_seed"
  },
  "move:lick:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"status\":\"par\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:lightscreen:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:lightscreen:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:lightscreen:sideCondition": {
   "disposition": "implemented",
   "fp": "val:\"lightscreen\"",
   "anchor": "turn.rs::modeled_screen_move",
   "reason": "the 5-turn screen side-condition arm (Light Screen/Reflect set + the side-residual countdown + the damage-calc halving)"
  },
  "move:lovelykiss:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:lovelykiss:status": {
   "disposition": "implemented",
   "fp": "val:\"slp\"",
   "anchor": "turn.rs::modeled_status_move",
   "reason": "the standalone status-inflicting move arm (accuracy + try_set_status)"
  },
  "move:lusterpurge:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":50,\"boosts\":{\"spd\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:machpunch:priority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::move_priority",
   "reason": "the action-order priority sort (draws only on a priority+speed tie)"
  },
  "move:magicalleaf:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:meanlook:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:meanlook:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:meanlook:onHit": {
   "disposition": "implemented",
   "fp": "fn:87e84bff:onHit(target, source, move) { return target.addVolatile(\"trapped\", source, move, \"trapper\"); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the trap-move arm: protect block → substitute block ([still]+-fail) → already-trapped fail → the linked trapped_by = Some(trapper uid); ZERO draws every branch (MC90/MC91)"
  },
  "move:meditate:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"atk\":1}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:meditate:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:meditate:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:megadrain:drain": {
   "disposition": "implemented",
   "fp": "val:[1,2]",
   "anchor": "turn.rs::apply_drain",
   "reason": "the drain family (Absorb/Mega/Giga Drain/Leech Life); floor non-sub, ceil behind a sub, draw-free"
  },
  "move:memento:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"atk\":-2,\"spa\":-2}",
   "anchor": "turn.rs::run_status_move",
   "reason": "the memento arm: protect/sub block (NO faint — ifHit); the −2/−2 drops via apply_secondary_boost (Clear-Body gated; the user faints even when blocked/floored) then the self-faint through the deferred-faint protocol (gen3 faint-cancels-all + no QC → the ZERO-draw landed turn, MC94)"
  },
  "move:memento:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:memento:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:memento:selfdestruct": {
   "disposition": "implemented",
   "fp": "val:\"ifHit\"",
   "anchor": "turn.rs::run_status_move",
   "reason": "the memento arm: protect/sub block (NO faint — ifHit); the −2/−2 drops via apply_secondary_boost (Clear-Body gated; the user faints even when blocked/floored) then the self-faint through the deferred-faint protocol (gen3 faint-cancels-all + no QC → the ZERO-draw landed turn, MC94)"
  },
  "move:metalclaw:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"self\":{\"boosts\":{\"atk\":1}}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:metalsound:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"spd\":-2}",
   "anchor": "turn.rs::stat_drop_boosts",
   "reason": "the standalone foe stat-drop arm (Screech/Charm/Metal Sound/… — gen3_moves.json statDropBoosts; accuracy roll + a draw-free boost, gen3_move_coverage_batch2_v1)"
  },
  "move:metalsound:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:meteormash:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":20,\"self\":{\"boosts\":{\"atk\":1}}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:milkdrink:heal": {
   "disposition": "implemented",
   "fp": "val:[1,2]",
   "anchor": "turn.rs::recovery_heal_amount",
   "reason": "the flat floor(maxhp/2) recovery arm"
  },
  "move:milkdrink:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:milkdrink:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:mimic:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:mimic:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:mimic:onHit": {
   "disposition": "implemented",
   "fp": "fn:1688b09e:onHit(target, source) { if (source.transformed || !target.lastMove || target.volatiles[\"substitute\"]) { return false; } ",
   "anchor": "turn.rs::run_status_move",
   "reason": "the mimic arm: sub / no-lastMove / failmimic (data-driven MoveData::fail_mimic) / already-known fails ([still]+-fail, draw-free); the copy overlays the slot {pp: min(5, base), maxpp: calculatePP(copied,3)} via MonState::mimic_overlay; restore_mimic_overlay reverts on switch-out/faint (MC95)"
  },
  "move:mirrorcoat:onTry": {
   "disposition": "implemented",
   "fp": "fn:bdcb8380:onTry(source) { if (!source.volatiles[\"mirrorcoat\"]) return false; if (source.volatiles[\"mirrorcoat\"].slot === null) ret",
   "anchor": "turn.rs::run_fixed_damage_move",
   "reason": "the ZERO-DRAW un-armed fail (no volatile / slot===null): a bare |move| line, no -fail, PP already deducted — BEFORE the accuracy roll"
  },
  "move:mirrorcoat:priority": {
   "disposition": "implemented",
   "fp": "val:-5",
   "anchor": "turn.rs::move_priority",
   "reason": "the action-order priority sort (draws only on a priority+speed tie)"
  },
  "move:mistball:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":50,\"boosts\":{\"spa\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:moonlight:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:moonlight:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:moonlight:onHit": {
   "disposition": "implemented",
   "fp": "fn:fc491693:onHit(pokemon) { if (this.field.isWeather([\"sunnyday\", \"desolateland\"])) { this.heal(pokemon.maxhp * 2 / 3); } else if (",
   "anchor": "turn.rs::recovery_heal_amount",
   "reason": "the weather-conditional heal fractions"
  },
  "move:morningsun:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:morningsun:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:morningsun:onHit": {
   "disposition": "implemented",
   "fp": "fn:fc491693:onHit(pokemon) { if (this.field.isWeather([\"sunnyday\", \"desolateland\"])) { this.heal(pokemon.maxhp * 2 / 3); } else if (",
   "anchor": "turn.rs::recovery_heal_amount",
   "reason": "the weather-conditional heal fractions"
  },
  "move:muddywater:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"boosts\":{\"accuracy\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:mudshot:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":100,\"boosts\":{\"spe\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:mudslap:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":100,\"boosts\":{\"accuracy\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:needlearm:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"volatileStatus\":\"flinch\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:nightshade:damage": {
   "disposition": "implemented",
   "fp": "val:\"level\"",
   "anchor": "turn.rs::fixed_damage_amount",
   "reason": "the fixed/level damage declarative (Seismic Toss/Night Shade level; Sonic Boom 20; Dragon Rage 40) — accuracy-only draw, no crit/damage roll (gen3_fixeddamage_v1; surfaced by the batch-5 blocklist un-shadowing)"
  },
  "move:octazooka:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":50,\"boosts\":{\"accuracy\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:overheat:self": {
   "disposition": "implemented",
   "fp": "val:{\"boosts\":{\"spa\":-2}}",
   "anchor": "turn.rs::apply_self_drops",
   "reason": "the self stat-drop (Overheat/Superpower) + the gen3 selfDrops random(100)"
  },
  "move:painsplit:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:painsplit:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:painsplit:onHit": {
   "disposition": "implemented",
   "fp": "fn:c5f50f19:onHit(target, pokemon) { const targetHP = target.getUndynamaxedHP(); const averagehp = Math.floor((targetHP + pokemon.hp",
   "anchor": "turn.rs::run_status_move",
   "reason": "the painsplit arm: protect/sub block; avg = floor((u+t)/2), EACH side clamped at its OWN maxhp (MC96); -sethp target [silent] then user"
  },
  "move:payday:onHit": {
   "disposition": "implemented",
   "fp": "fn:ade5e10c:onHit() { this.add(\"-fieldactivate\", \"move: Pay Day\"); }",
   "anchor": "protocol.rs::fieldactivate_move",
   "reason": "the |-fieldactivate|move: Pay Day line — EMITTED after the -damage on a landed direct hit (gen3_protocol_phase3_v1; the coin scatter itself has no battle effect; the sub-absorbed form stays uncaptured/un-emitted)"
  },
  "move:perishsong:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:perishsong:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:perishsong:onHitField": {
   "disposition": "implemented",
   "fp": "fn:cc6a0b5f:onHitField(target, source, move) { let result = false; let message = false; for (const pokemon of this.getAllActive()) {",
   "anchor": "turn.rs::run_status_move",
   "reason": "the perishsong arm: getAllActive in SIDE order — Soundproof -immune (counted as a result), fresh counters Some(4), ONE -fieldactivate iff >=1 newly applied, the all-counted [still]+-fail, the >=1-immune SILENT re-cast; ZERO draws every branch"
  },
  "move:pinmissile:multihit": {
   "disposition": "implemented",
   "fp": "val:[2,5]",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:poisonfang:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"status\":\"tox\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:poisongas:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:poisongas:status": {
   "disposition": "implemented",
   "fp": "val:\"psn\"",
   "anchor": "turn.rs::modeled_status_move",
   "reason": "the standalone status-inflicting move arm (accuracy + try_set_status)"
  },
  "move:poisonpowder:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:poisonpowder:status": {
   "disposition": "implemented",
   "fp": "val:\"psn\"",
   "anchor": "turn.rs::modeled_status_move",
   "reason": "the standalone status-inflicting move arm (accuracy + try_set_status)"
  },
  "move:poisonsting:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"status\":\"psn\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:poisontail:critRatio": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::CRIT_MULT",
   "reason": "the high-crit denominator table (critRatio 2 => 1/8)"
  },
  "move:poisontail:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"psn\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:powdersnow:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"frz\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:protect:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:protect:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:protect:onHit": {
   "disposition": "implemented",
   "fp": "fn:fd8966cf:onHit(pokemon) { pokemon.addVolatile(\"stall\"); }",
   "anchor": "state.rs::protect_counter",
   "reason": "the stall volatile (re)add on success"
  },
  "move:protect:onPrepareHit": {
   "disposition": "implemented",
   "fp": "fn:90156567:onPrepareHit(pokemon) { return !!this.queue.willAct() && this.runEvent(\"StallMove\", pokemon); }",
   "anchor": "turn.rs::run_protect",
   "reason": "the willAct + StallMove gate (first protect short-circuits draw-free)"
  },
  "move:protect:priority": {
   "disposition": "implemented",
   "fp": "val:3",
   "anchor": "turn.rs::move_priority",
   "reason": "the action-order priority sort (draws only on a priority+speed tie)"
  },
  "move:protect:stallingMove": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_protect",
   "reason": "the protect/detect stall machinery"
  },
  "move:protect:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"protect\"",
   "anchor": "turn.rs::run_protect"
  },
  "move:psybeam:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"volatileStatus\":\"confusion\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:psychic:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"boosts\":{\"spd\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:psychoboost:self": {
   "disposition": "implemented",
   "fp": "val:{\"boosts\":{\"spa\":-2}}",
   "anchor": "turn.rs::apply_self_drops",
   "reason": "the self stat-drop (Overheat/Superpower) + the gen3 selfDrops random(100)"
  },
  "move:psychup:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:psychup:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:psychup:onHit": {
   "disposition": "implemented",
   "fp": "fn:06997a4d:onHit(target, source) { let i; for (i in target.boosts) { source.boosts[i] = target.boosts[i]; } this.add(\"-copyboost\", ",
   "anchor": "turn.rs::run_status_move",
   "reason": "the psychup arm: copies ALL 7 stages VERBATIM (zeros overwrite — MC97); NO protect flag (copies through a Protect); bypasssub"
  },
  "move:pursuit:onModifyMove": {
   "disposition": "implemented",
   "fp": "fn:5a6e39c2:onModifyMove(move, source, target) { if (target?.beingCalledBack || target?.switchFlag) move.accuracy = true; }",
   "anchor": "turn.rs::run_move",
   "reason": "accuracy=true (never-miss) on the interrupt condition (beingCalledBack/switchFlag) — the pursuit_strike flag drives the never-miss + ×2 BP"
  },
  "move:quickattack:priority": {
   "disposition": "implemented",
   "fp": "val:1",
   "anchor": "turn.rs::move_priority",
   "reason": "the action-order priority sort (draws only on a priority+speed tie)"
  },
  "move:raindance:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:raindance:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:raindance:weather": {
   "disposition": "implemented",
   "fp": "val:\"RainDance\"",
   "anchor": "turn.rs::modeled_weather_set_move",
   "reason": "the 5-turn timed weather-set arm (Rain Dance/Sunny Day; setWeather fails into the same weather, overwrites a different one)"
  },
  "move:rapidspin:onAfterHit": {
   "disposition": "implemented",
   "fp": "fn:d70eb3be:onAfterHit(target, pokemon, move) { if (!move.hasSheerForce) { if (pokemon.removeVolatile(\"leechseed\")) { this.add(\"-end",
   "anchor": "turn.rs::apply_rapid_spin",
   "reason": "the Rapid Spin hazard/leech clear (onAfterHit + onAfterSubDamage — clears behind a sub too)"
  },
  "move:rapidspin:onAfterSubDamage": {
   "disposition": "implemented",
   "fp": "fn:2c919874:onAfterSubDamage(damage, target, pokemon, move) { if (!move.hasSheerForce) { if (pokemon.hp && pokemon.removeVolatile(\"l",
   "anchor": "turn.rs::apply_rapid_spin",
   "reason": "the Rapid Spin hazard/leech clear (onAfterHit + onAfterSubDamage — clears behind a sub too)"
  },
  "move:rapidspin:self": {
   "disposition": "noop_justified",
   "fp": "val:{}",
   "reason": "an empty `self: {}` marker (Rapid Spin) — its effect is the onAfterHit/onAfterSubDamage clear"
  },
  "move:razorleaf:critRatio": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::CRIT_MULT",
   "reason": "the high-crit denominator table (critRatio 2 => 1/8)"
  },
  "move:recover:heal": {
   "disposition": "implemented",
   "fp": "val:[1,2]",
   "anchor": "turn.rs::recovery_heal_amount",
   "reason": "the flat floor(maxhp/2) recovery arm"
  },
  "move:recover:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:recover:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:recycle:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:recycle:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:recycle:onHit": {
   "disposition": "implemented",
   "fp": "fn:62485b40:onHit(pokemon, source, move) { if (pokemon.item || !pokemon.lastItem) return false; const item = pokemon.lastItem; pokem",
   "anchor": "turn.rs::run_status_move",
   "reason": "the recycle arm: FAIL ([still]+bare -fail on the USER) if the mon already holds an item OR has no last_item; else clear last_item BEFORE restoring it and emit |-item|<u>|<Item>|[from] move: Recycle. NEVER-MISS so zero draws on both paths, landed=false. `last_item` is set ONLY by the eatItem/useItem sites (items.rs::eat_item, white_herb_restore) — never by takeItem (Knock Off / Thief / Trick), which is why a knocked-off item is not recyclable."
  },
  "move:reflect:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:reflect:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:reflect:sideCondition": {
   "disposition": "implemented",
   "fp": "val:\"reflect\"",
   "anchor": "turn.rs::modeled_screen_move",
   "reason": "the 5-turn screen side-condition arm (Light Screen/Reflect set + the side-residual countdown + the damage-calc halving)"
  },
  "move:refresh:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:refresh:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:refresh:onHit": {
   "disposition": "implemented",
   "fp": "fn:6e5af1cf:onHit(pokemon) { if ([\"\", \"slp\", \"frz\"].includes(pokemon.status)) return false; pokemon.cureStatus(); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Refresh self-cure arm (par/psn/brn cleared; none/slp/frz fail — draw-free)"
  },
  "move:rest:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:rest:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:rest:onHit": {
   "disposition": "implemented",
   "fp": "fn:db425e8f:onHit(target, source, move) { const result = target.setStatus(\"slp\", source, move); if (!result) return result; target.s",
   "anchor": "turn.rs::run_rest",
   "reason": "the full heal + FIXED Sleep(3) self-sleep (draws-then-discards the slp random(2,6))"
  },
  "move:rest:onTry": {
   "disposition": "implemented",
   "fp": "fn:3ab29105:onTry(source) { if (source.status === \"slp\" || source.hasAbility(\"comatose\")) return false; if (source.hp === source.max",
   "anchor": "turn.rs::run_rest",
   "reason": "the already-asleep / full-HP fail gates"
  },
  "move:roar:forceSwitch": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::drag_in",
   "reason": "the phaze arm (accuracy + protect/soundproof/suctioncups gates + ONE sample)"
  },
  "move:roar:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:roar:priority": {
   "disposition": "implemented",
   "fp": "val:-6",
   "anchor": "turn.rs::move_priority",
   "reason": "the action-order priority sort (draws only on a priority+speed tie)"
  },
  "move:rockblast:multihit": {
   "disposition": "implemented",
   "fp": "val:[2,5]",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:rockslide:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"volatileStatus\":\"flinch\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:rocksmash:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":50,\"boosts\":{\"def\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:rocktomb:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":100,\"boosts\":{\"spe\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:rollingkick:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"volatileStatus\":\"flinch\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:sacredfire:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":50,\"status\":\"brn\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:safeguard:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn/status_moves.rs::run_status_move",
   "reason": "the safeguard arm: never-miss so NO accuracy roll, and the FIXED duration 5 (gen-3 durationCallback returns a constant; the Persistent +2 is gen5+) means the cast is entirely DRAW-FREE. Sets SideState::safeguard = 5 and emits |-sidestart|<side>|Safeguard (the BARE name — Light Screen prefixes `move: `, this does not). An already-up re-cast is addSideCondition-false with no onSideRestart, so it takes the [still] did-nothing form plus a bare -fail on the USER, also draw-free. landed=false."
  },
  "move:safeguard:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn/status_moves.rs::run_status_move",
   "reason": "the safeguard arm: never-miss so NO accuracy roll, and the FIXED duration 5 (gen-3 durationCallback returns a constant; the Persistent +2 is gen5+) means the cast is entirely DRAW-FREE. Sets SideState::safeguard = 5 and emits |-sidestart|<side>|Safeguard (the BARE name — Light Screen prefixes `move: `, this does not). An already-up re-cast is addSideCondition-false with no onSideRestart, so it takes the [still] did-nothing form plus a bare -fail on the USER, also draw-free. landed=false."
  },
  "move:safeguard:sideCondition": {
   "disposition": "implemented",
   "fp": "val:\"safeguard\"",
   "anchor": "turn/status_moves.rs::run_status_move",
   "reason": "the safeguard arm: never-miss so NO accuracy roll, and the FIXED duration 5 (gen-3 durationCallback returns a constant; the Persistent +2 is gen5+) means the cast is entirely DRAW-FREE. Sets SideState::safeguard = 5 and emits |-sidestart|<side>|Safeguard (the BARE name — Light Screen prefixes `move: `, this does not). An already-up re-cast is addSideCondition-false with no onSideRestart, so it takes the [still] did-nothing form plus a bare -fail on the USER, also draw-free. landed=false."
  },
  "move:sandattack:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"accuracy\":-1}",
   "anchor": "turn.rs::stat_drop_boosts",
   "reason": "the standalone foe stat-drop arm (Screech/Charm/Metal Sound/… — gen3_moves.json statDropBoosts; accuracy roll + a draw-free boost, gen3_move_coverage_batch2_v1)"
  },
  "move:sandattack:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:sandstorm:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:sandstorm:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:sandstorm:weather": {
   "disposition": "implemented",
   "fp": "val:\"Sandstorm\"",
   "anchor": "turn.rs::modeled_weather_set_move",
   "reason": "the 5-turn timed weather-set arm (Rain Dance/Sunny Day; setWeather fails into the same weather, overwrites a different one)"
  },
  "move:sandtomb:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"partiallytrapped\"",
   "anchor": "turn.rs::is_partial_trap_move",
   "reason": "the post-hit partial-trap arm in run_move: gated on !absorbed (a Substitute intercepts before runMoveEffects — no volatile, NO duration draw) + a LIVE target (addVolatile early-returns on hp 0) + no trap already present (addVolatile returns false, no -fail); sets MonState::partial_trap and emits |-activate|<t>|move: <M>|[of] <u> after the -damage"
  },
  "move:scaryface:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"spe\":-2}",
   "anchor": "turn.rs::stat_drop_boosts",
   "reason": "the standalone foe stat-drop arm (Screech/Charm/Metal Sound/… — gen3_moves.json statDropBoosts; accuracy roll + a draw-free boost, gen3_move_coverage_batch2_v1)"
  },
  "move:scaryface:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:screech:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"def\":-2}",
   "anchor": "turn.rs::stat_drop_boosts",
   "reason": "the standalone foe stat-drop arm (Screech/Charm/Metal Sound/… — gen3_moves.json statDropBoosts; accuracy roll + a draw-free boost, gen3_move_coverage_batch2_v1)"
  },
  "move:screech:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:seismictoss:damage": {
   "disposition": "implemented",
   "fp": "val:\"level\"",
   "anchor": "turn.rs::fixed_damage_amount",
   "reason": "the fixed/level damage declarative (Seismic Toss/Night Shade level; Sonic Boom 20; Dragon Rage 40) — accuracy-only draw, no crit/damage roll (gen3_fixeddamage_v1; surfaced by the batch-5 blocklist un-shadowing)"
  },
  "move:selfdestruct:selfdestruct": {
   "disposition": "implemented",
   "fp": "val:\"always\"",
   "anchor": "turn.rs::pending_explosion_self_ko",
   "reason": "the pre-hit unconditional self-KO (gen3_explosion_v1)"
  },
  "move:shadowball:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":20,\"boosts\":{\"spd\":-1}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:shadowpunch:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:sharpen:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"atk\":1}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:sharpen:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:sharpen:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:shockwave:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:signalbeam:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"volatileStatus\":\"confusion\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:silverwind:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"self\":{\"boosts\":{\"atk\":1,\"def\":1,\"spa\":1,\"spd\":1,\"spe\":1}}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:sing:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:sing:status": {
   "disposition": "implemented",
   "fp": "val:\"slp\"",
   "anchor": "turn.rs::modeled_status_move",
   "reason": "the standalone status-inflicting move arm (accuracy + try_set_status)"
  },
  "move:skillswap:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:skillswap:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:skillswap:onHit": {
   "disposition": "implemented",
   "fp": "fn:d6688131:onHit(target, source, move) { return this.skillSwap(source, target); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the skillswap arm: never-miss (zero draws), Protect blocks / Substitute does not (bypasssub), FAIL on Wonder Guard (gen-3's only failskillswap) or an identical ability pair. Emits ONE gen<=4 line `|-activate|<u>|Skill Swap|||[of] <t>` (two EMPTY fields, no -endability/-ability). The swapped-in abilities do NOT re-fire onStart (the sim gates those on gen>3), but BOTH outgoing abilities DO fire onEnd — the weather_negate WeatherChange (a random(0,2) at a cached-speed tie) and the armed flash_fire silent -end, which is the ONLY draw this move can create. The switch-out revert is free: execute_switch already restores set.ability."
  },
  "move:slackoff:heal": {
   "disposition": "implemented",
   "fp": "val:[1,2]",
   "anchor": "turn.rs::recovery_heal_amount",
   "reason": "the flat floor(maxhp/2) recovery arm"
  },
  "move:slackoff:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:slackoff:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:slash:critRatio": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::CRIT_MULT",
   "reason": "the high-crit denominator table (critRatio 2 => 1/8)"
  },
  "move:sleeppowder:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:sleeppowder:status": {
   "disposition": "implemented",
   "fp": "val:\"slp\"",
   "anchor": "turn.rs::modeled_status_move",
   "reason": "the standalone status-inflicting move arm (accuracy + try_set_status)"
  },
  "move:sleeptalk:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "accuracy true (no draw) + the Status-default ignoreImmunity — the sleeptalk arm draws nothing but the sample"
  },
  "move:sleeptalk:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "accuracy true (no draw) + the Status-default ignoreImmunity — the sleeptalk arm draws nothing but the sample"
  },
  "move:sleeptalk:onHit": {
   "disposition": "implemented",
   "fp": "fn:32945850:onHit(pokemon) { const moves = []; for (const moveSlot of pokemon.moveSlots) { const moveid = moveSlot.id; const pp = mo",
   "anchor": "turn.rs::run_status_move",
   "reason": "the pool build (slot order, !nosleeptalk && !charge — data-driven MoveData flags; NO pp/disabled filter) + ONE sample = random(n) even at n=1 + the 0-PP |cant|nopp| stop + the bare-useMove called run ([from] Sleep Talk, no PP, full draw chain)"
  },
  "move:sleeptalk:onTry": {
   "disposition": "implemented",
   "fp": "fn:67cf1dbe:onTry(source) { return source.status === \"slp\" || source.hasAbility(\"comatose\"); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "usable ONLY while asleep — an awake/wake-turn use fails SILENTLY (the normal self-target announce, no [still], no -fail, zero draws); `comatose` is gen7+, unreachable"
  },
  "move:sleeptalk:onTryHit": {
   "disposition": "implemented",
   "fp": "fn:443207c5:onTryHit(pokemon) { return !pokemon.volatiles[\"choicelock\"] && !pokemon.volatiles[\"encore\"]; }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the choicelock gate (a PRIOR-turn lock → [still]+-fail BEFORE the sample; the lock this use just set does not count — the pre-move snapshot); ENCORE is modeled as of batch 6 (`gen3_move_coverage_batch6_v1`) via move_usable + the onOverrideAction execution override"
  },
  "move:sleeptalk:sleepUsable": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::on_before_move",
   "reason": "the slp handler prints |cant|slp then PROCEEDS for Sleep Talk (skippedTime++; a normal blocked cant resets it; the onSwitchIn restore lives in run_switch)"
  },
  "move:sludge:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"status\":\"psn\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:sludgebomb:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"status\":\"psn\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:smog:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":40,\"status\":\"psn\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:smokescreen:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"accuracy\":-1}",
   "anchor": "turn.rs::stat_drop_boosts",
   "reason": "the standalone foe stat-drop arm (Screech/Charm/Metal Sound/… — gen3_moves.json statDropBoosts; accuracy roll + a draw-free boost, gen3_move_coverage_batch2_v1)"
  },
  "move:smokescreen:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:snatch:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:snatch:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:snatch:priority": {
   "disposition": "implemented",
   "fp": "val:4",
   "anchor": "turn.rs::run_status_move",
   "reason": "the snatch-cast arm: priority +4 sets MonState::snatch DRAW-FREE + emits |move|U|Snatch|U + |-singleturn|U|Snatch (the onStart line); landed FALSE"
  },
  "move:snatch:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"snatch\"",
   "anchor": "turn.rs::run_status_move",
   "reason": "the snatch-cast arm: priority +4 sets MonState::snatch DRAW-FREE + emits |move|U|Snatch|U + |-singleturn|U|Snatch (the onStart line); landed FALSE"
  },
  "move:softboiled:heal": {
   "disposition": "implemented",
   "fp": "val:[1,2]",
   "anchor": "turn.rs::recovery_heal_amount",
   "reason": "the flat floor(maxhp/2) recovery arm"
  },
  "move:softboiled:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:softboiled:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:solarbeam:onBasePower": {
   "disposition": "implemented",
   "fp": "fn:4db566f2:onBasePower(basePower, pokemon, target) { const weakWeathers = [\"raindance\", \"primordialsea\", \"sandstorm\", \"hail\", \"snow",
   "anchor": "turn.rs::run_move",
   "reason": "the rain/sand/hail BP-halving chainModify(0.5) — a draw-free BP-chain fold read at damage time, suppression-aware (gen3 DOES have the modern halving, probed rain 54 vs control 105)"
  },
  "move:solarbeam:onTryMove": {
   "disposition": "implemented",
   "fp": "fn:6630f1d5:onTryMove(attacker, defender, move) { if (attacker.removeVolatile(move.id)) { return; } this.add(\"-prepare\", attacker, m",
   "anchor": "turn.rs::run_move",
   "reason": "the two-turn gate: CHARGE ([still]+-prepare, zero move draws, addVolatile twoturnmove) / SUN SKIP (effective_weather-aware: -anim + immediate 3-draw execution) / FIRE (removeVolatile solarbeam => charging=false, [from]lockedmove, acc-100 drawn)"
  },
  "move:sonicboom:damage": {
   "disposition": "implemented",
   "fp": "val:20",
   "anchor": "turn.rs::fixed_damage_amount",
   "reason": "the fixed/level damage declarative (Seismic Toss/Night Shade level; Sonic Boom 20; Dragon Rage 40) — accuracy-only draw, no crit/damage roll (gen3_fixeddamage_v1; surfaced by the batch-5 blocklist un-shadowing)"
  },
  "move:spark:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"status\":\"par\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:spiderweb:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:spiderweb:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:spiderweb:onHit": {
   "disposition": "implemented",
   "fp": "fn:87e84bff:onHit(target, source, move) { return target.addVolatile(\"trapped\", source, move, \"trapper\"); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the trap-move arm: protect block → substitute block ([still]+-fail) → already-trapped fail → the linked trapped_by = Some(trapper uid); ZERO draws every branch (MC90/MC91)"
  },
  "move:spikecannon:multihit": {
   "disposition": "implemented",
   "fp": "val:[2,5]",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:spikes:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:spikes:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:spikes:sideCondition": {
   "disposition": "implemented",
   "fp": "val:\"spikes\"",
   "anchor": "turn.rs::apply_entry_hazards",
   "reason": "the Spikes side-condition arm (layers + grounded entry chip)"
  },
  "move:splash:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:splash:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:splash:onTry": {
   "disposition": "unreachable_justified",
   "fp": "fn:4f14fd46:onTry(source, target, move) { if (this.field.getPseudoWeather(\"Gravity\")) { this.add(\"cant\", source, \"move: Gravity\", mo",
   "reason": "the Gravity gate — Gravity is a gen4 pseudo-weather, not in the gen3 universe"
  },
  "move:splash:onTryHit": {
   "disposition": "implemented",
   "fp": "fn:997bf98c:onTryHit(target, source) { this.add(\"-nothing\"); }",
   "anchor": "protocol.rs::nothing",
   "reason": "the |-nothing marker — EMITTED (gen3_protocol_phase3_v1, byte-verified; Splash stays the modeled draw-free no-op move)"
  },
  "move:spore:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:spore:status": {
   "disposition": "implemented",
   "fp": "val:\"slp\"",
   "anchor": "turn.rs::modeled_status_move",
   "reason": "the standalone status-inflicting move arm (accuracy + try_set_status)"
  },
  "move:steelwing:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"self\":{\"boosts\":{\"def\":1}}}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:stomp:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"volatileStatus\":\"flinch\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:struggle:noPPBoosts": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::must_struggle",
   "reason": "Struggle is not a slot — no PP tracked/deducted (gen3_pp_tracking_v1)"
  },
  "move:struggle:onModifyMove": {
   "disposition": "implemented",
   "fp": "fn:a892c501:onModifyMove(move) { move.type = \"???\"; }",
   "anchor": "turn.rs::run_move",
   "reason": "the typeless ??? override (no STAB, hits Ghosts) — the struggle arm resolves move_type None"
  },
  "move:struggle:recoil": {
   "disposition": "implemented",
   "fp": "val:[1,4]",
   "anchor": "turn.rs::run_move",
   "reason": "STRUGGLE RECOIL: the gen-3 recoil:[1,4] on ACTUAL damage dealt"
  },
  "move:stunspore:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:stunspore:status": {
   "disposition": "implemented",
   "fp": "val:\"par\"",
   "anchor": "turn.rs::modeled_status_move",
   "reason": "the standalone status-inflicting move arm (accuracy + try_set_status)"
  },
  "move:submission:recoil": {
   "disposition": "implemented",
   "fp": "val:[1,4]",
   "anchor": "turn.rs::apply_recoil",
   "reason": "the recoil family (Double-Edge/Take Down/Submission); Rock Head negates, draw-free"
  },
  "move:substitute:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:substitute:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:substitute:onHit": {
   "disposition": "implemented",
   "fp": "fn:f7fb973d:onHit(target) { this.directDamage(target.maxhp / 4); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the maxhp/4 directDamage create cost"
  },
  "move:substitute:onTryHit": {
   "disposition": "implemented",
   "fp": "fn:234c558d:onTryHit(source) { if (source.volatiles[\"substitute\"]) { this.add(\"-fail\", source, \"move: Substitute\"); return this.NOT_",
   "anchor": "turn.rs::run_status_move",
   "reason": "the already-subbed / not-enough-HP fail gates"
  },
  "move:substitute:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"substitute\"",
   "anchor": "turn.rs::absorb_into_sub",
   "reason": "the substitute volatile (create in run_status_move, absorb at onTryPrimaryHit)"
  },
  "move:sunnyday:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:sunnyday:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:sunnyday:weather": {
   "disposition": "implemented",
   "fp": "val:\"sunnyday\"",
   "anchor": "turn.rs::modeled_weather_set_move",
   "reason": "the 5-turn timed weather-set arm (Rain Dance/Sunny Day; setWeather fails into the same weather, overwrites a different one)"
  },
  "move:superpower:self": {
   "disposition": "implemented",
   "fp": "val:{\"boosts\":{\"atk\":-1,\"def\":-1}}",
   "anchor": "turn.rs::apply_self_drops",
   "reason": "the self stat-drop (Overheat/Superpower) + the gen3 selfDrops random(100)"
  },
  "move:swift:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:swordsdance:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"atk\":2}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:swordsdance:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:swordsdance:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:synthesis:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:synthesis:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:synthesis:onHit": {
   "disposition": "implemented",
   "fp": "fn:fc491693:onHit(pokemon) { if (this.field.isWeather([\"sunnyday\", \"desolateland\"])) { this.heal(pokemon.maxhp * 2 / 3); } else if (",
   "anchor": "turn.rs::recovery_heal_amount",
   "reason": "the weather-conditional heal fractions"
  },
  "move:tailglow:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"spa\":2}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:tailglow:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:tailglow:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:takedown:recoil": {
   "disposition": "implemented",
   "fp": "val:[1,4]",
   "anchor": "turn.rs::apply_recoil",
   "reason": "the recoil family (Double-Edge/Take Down/Submission); Rock Head negates, draw-free"
  },
  "move:taunt:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:taunt:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"taunt\"",
   "anchor": "state.rs::taunt",
   "reason": "the taunt volatile (gen3_taunt_disable_v1)"
  },
  "move:thief:onAfterHit": {
   "disposition": "implemented",
   "fp": "fn:bc2f9627:onAfterHit(target, source, move) { if (source.item || source.volatiles[\"gem\"]) { return; } const yourItem = target.takeI",
   "anchor": "turn.rs::apply_item_removal",
   "reason": "the Knock Off/Thief/Covet item removal (Sticky Hold block + the gen3 itemKnockedOff gate)"
  },
  "move:thunder:onModifyMove": {
   "disposition": "implemented",
   "fp": "fn:6a848e05:onModifyMove(move, pokemon, target) { switch (target?.effectiveWeather()) { case \"raindance\": case \"primordialsea\": move",
   "anchor": "turn.rs::run_move",
   "reason": "the id-gated weather-accuracy mutation: effective rain => never-miss (skip the accuracy random(100)), sun => base 50, else base 70 (gen3_move_coverage_batch4b_v1)"
  },
  "move:thunder:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":30,\"status\":\"par\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:thunderbolt:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"par\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:thunderpunch:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"par\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:thundershock:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":10,\"status\":\"par\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:thunderwave:status": {
   "disposition": "implemented",
   "fp": "val:\"par\"",
   "anchor": "turn.rs::modeled_status_move",
   "reason": "the standalone status-inflicting move arm (accuracy + try_set_status)"
  },
  "move:tickle:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"atk\":-1,\"def\":-1}",
   "anchor": "turn.rs::stat_drop_boosts",
   "reason": "the standalone foe stat-drop arm (Screech/Charm/Metal Sound/… — gen3_moves.json statDropBoosts; accuracy roll + a draw-free boost, gen3_move_coverage_batch2_v1)"
  },
  "move:tickle:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:torment:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:torment:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"torment\"",
   "anchor": "state.rs::move_usable",
   "reason": "the torment volatile — blocks the LAST-USED slot, permanent until switch-out (gen3_torment_v1)"
  },
  "move:toxic:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:toxic:status": {
   "disposition": "implemented",
   "fp": "val:\"tox\"",
   "anchor": "turn.rs::modeled_status_move",
   "reason": "the standalone status-inflicting move arm (accuracy + try_set_status)"
  },
  "move:transform:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:transform:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "never-miss (accuracy true — NO accuracy draw; encore is the one acc-100 batch-6 move and draws) + the Status-default ignoreImmunity (painsplit works on a Ghost — probed)"
  },
  "move:transform:onHit": {
   "disposition": "implemented",
   "fp": "fn:bf2d6106:onHit(target, pokemon) { if (!pokemon.transformInto(target)) { return false; } }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the transform arm (pokemon.transformInto): fainted / already-transformed target fails ([still]+-fail, draw-free — the USER being transformed does NOT block, that guard is gen5+); the copy overwrites species/types/5 non-HP stats/ability/all 7 boosts/moveslots {pp: min(5, base), maxpp: calculatePP(copied, ppUps[i] || 0) — the 1-move randbats Ditto gets NO pp-ups on slots 1-3}/hpType+hpPower via MonState::transform, sets cached_speed to the HYBRID spreadModify(TARGET base, OWN set).spe that setSpecies leaves behind, re-keys the slot-keyed choicelock/disable/lastMove references, and emits |-transform|U|T; restore_transform_overlay reverts on switch-out/faint (TF1-TF7)"
  },
  "move:triattack:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":20}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "Tri Attack's ONE random(100) + brn/par/frz sample (special-cased)"
  },
  "move:trick:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:trick:onHit": {
   "disposition": "implemented",
   "fp": "fn:2c132dec:onHit(target, source, move) { const yourItem = target.takeItem(source); const myItem = source.takeItem(); if (yourItem =",
   "anchor": "turn.rs::run_status_move",
   "reason": "the trick arm swap: yourItem=target.takeItem(source), myItem=source.takeItem(); FAIL ([still]+-fail, no swap) if EITHER side item_knocked_off (gen<=4 takeItem→false) OR both itemless OR the foe has a Substitute (no bypasssub); else swap the two items (TARGET new-item line first, then USER; -item/-enditem [silent] [from] move: Trick) + RELEASE both choice_locked_move; DRAW-FREE (TR1-TR5)"
  },
  "move:trick:onTryImmunity": {
   "disposition": "implemented",
   "fp": "fn:f4c83ddf:onTryImmunity(target) { return !target.hasAbility(\"stickyhold\"); }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the trick arm Sticky-Hold onTryImmunity gate (!target.hasAbility(stickyhold)): a Sticky-Hold target draws the accuracy then reports PLAIN |-immune|<foe> (NO [from] ability), NO swap"
  },
  "move:twineedle:multihit": {
   "disposition": "implemented",
   "fp": "val:2",
   "anchor": "turn.rs::run_multihit",
   "reason": "the multi-strike count + per-strike loop (fixed / [2,5] sample); stops at the target faint"
  },
  "move:twineedle:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":20,\"status\":\"psn\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:twister:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":20,\"volatileStatus\":\"flinch\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:vitalthrow:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:vitalthrow:priority": {
   "disposition": "implemented",
   "fp": "val:-1",
   "anchor": "turn.rs::move_priority",
   "reason": "the action-order priority sort (draws only on a priority+speed tie)"
  },
  "move:volttackle:recoil": {
   "disposition": "implemented",
   "fp": "val:[1,3]",
   "anchor": "turn.rs::apply_recoil",
   "reason": "the recoil family (Double-Edge/Take Down/Submission); Rock Head negates, draw-free"
  },
  "move:waterpulse:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":20,\"volatileStatus\":\"confusion\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  },
  "move:weatherball:onModifyMove": {
   "disposition": "implemented",
   "fp": "fn:486ee7e9:onModifyMove(move) { switch (this.field.effectiveWeather()) { case \"sunnyday\": move.type = \"Fire\"; move.category = \"Spec",
   "anchor": "turn/moves.rs::run_move",
   "reason": "the weatherball id-gate: reads effective_weather() and rewrites TYPE + BASE POWER + CATEGORY before the crit/damage draws, so it is DRAW-NEUTRAL. rain->Water, sun->Fire, sandstorm->Rock, hail->Ice, all at bp 100; no effective weather leaves the dex row (Normal / 50 / Physical). The CATEGORY is re-derived through the shared derive_category so the gen-3 type split produces it rather than a second id-list — Rock is PHYSICAL while Water/Fire/Ice are SPECIAL. Because it reads effective_weather, a Cloud Nine / Air Lock holder on either side reverts it fully. Pinned by WB2/WB3 via Counter-vs-Mirror-Coat, which sees the category flip even on a board where Atk == SpA."
  },
  "move:weatherball:onModifyType": {
   "disposition": "implemented",
   "fp": "fn:b82dfbca:onModifyType(move, pokemon) { switch (pokemon.effectiveWeather()) { case \"sunnyday\": case \"desolateland\": move.type = \"F",
   "anchor": "turn/moves.rs::run_move",
   "reason": "the weatherball id-gate: reads effective_weather() and rewrites TYPE + BASE POWER + CATEGORY before the crit/damage draws, so it is DRAW-NEUTRAL. rain->Water, sun->Fire, sandstorm->Rock, hail->Ice, all at bp 100; no effective weather leaves the dex row (Normal / 50 / Physical). The CATEGORY is re-derived through the shared derive_category so the gen-3 type split produces it rather than a second id-list — Rock is PHYSICAL while Water/Fire/Ice are SPECIAL. Because it reads effective_weather, a Cloud Nine / Air Lock holder on either side reverts it fully. Pinned by WB2/WB3 via Counter-vs-Mirror-Coat, which sees the category flip even on a board where Atk == SpA."
  },
  "move:whirlpool:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"partiallytrapped\"",
   "anchor": "turn.rs::is_partial_trap_move",
   "reason": "the post-hit partial-trap arm in run_move: gated on !absorbed (a Substitute intercepts before runMoveEffects — no volatile, NO duration draw) + a LIVE target (addVolatile early-returns on hp 0) + no trap already present (addVolatile returns false, no -fail); sets MonState::partial_trap and emits |-activate|<t>|move: <M>|[of] <u> after the -damage"
  },
  "move:whirlwind:forceSwitch": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::drag_in",
   "reason": "the phaze arm (accuracy + protect/soundproof/suctioncups gates + ONE sample)"
  },
  "move:whirlwind:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:whirlwind:priority": {
   "disposition": "implemented",
   "fp": "val:-6",
   "anchor": "turn.rs::move_priority",
   "reason": "the action-order priority sort (draws only on a priority+speed tie)"
  },
  "move:willowisp:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:willowisp:status": {
   "disposition": "implemented",
   "fp": "val:\"brn\"",
   "anchor": "turn.rs::modeled_status_move",
   "reason": "the standalone status-inflicting move arm (accuracy + try_set_status)"
  },
  "move:wish:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:wish:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:wish:slotCondition": {
   "disposition": "implemented",
   "fp": "val:\"Wish\"",
   "anchor": "state.rs::wish_pending",
   "reason": "the Wish slot condition set at cast (double-Wish fails [still], draw-free)"
  },
  "move:withdraw:boosts": {
   "disposition": "implemented",
   "fp": "val:{\"def\":1}",
   "anchor": "turn.rs::self_boost_spec",
   "reason": "the pure self-boost setup arm (gen3_moves.json selfBoosts, GIGO-proof lockstep)"
  },
  "move:withdraw:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:withdraw:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:wrap:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"partiallytrapped\"",
   "anchor": "turn.rs::is_partial_trap_move",
   "reason": "the post-hit partial-trap arm in run_move: gated on !absorbed (a Substitute intercepts before runMoveEffects — no volatile, NO duration draw) + a LIVE target (addVolatile early-returns on hp 0) + no trap already present (addVolatile returns false, no -fail); sets MonState::partial_trap and emits |-activate|<t>|move: <M>|[of] <u> after the -damage"
  },
  "move:yawn:ignoreImmunity": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::run_status_move",
   "reason": "the Status-category resolved default (true) / explicit false (Thunder Wave respects Ground) — the port's status arm implements the gen3 status immunities from first principles"
  },
  "move:yawn:neverMiss": {
   "disposition": "implemented",
   "fp": "val:true",
   "anchor": "turn.rs::never_miss",
   "reason": "accuracy === true — NO accuracy draw at all"
  },
  "move:yawn:onTryHit": {
   "disposition": "implemented",
   "fp": "fn:df1f2340:onTryHit(target) { if (target.status || !target.runStatusImmunity(\"slp\")) { return false; } }",
   "anchor": "turn.rs::run_status_move",
   "reason": "the yawn arm TryHit gates: Protect blocks (-activate Protect) > already-statused → [still]+-fail > sleep-immune (Insomnia/Vital Spirit via runStatusImmunity(slp)) → -immune|[from] ability > Substitute → [still]+-fail > add the yawn volatile; ALL DRAW-FREE (accuracy true → no accuracy draw)"
  },
  "move:yawn:volatileStatus": {
   "disposition": "implemented",
   "fp": "val:\"yawn\"",
   "anchor": "state.rs::yawn",
   "reason": "the `yawn` delayed-sleep volatile marker laid on the foe (Some((duration, source_uid)))"
  },
  "move:zapcannon:secondaries": {
   "disposition": "implemented",
   "fp": "val:[{\"chance\":100,\"status\":\"par\"}]",
   "anchor": "turn.rs::apply_secondaries",
   "reason": "the <=1-col secondary shape isModeledMove admits (status/flinch/confusion/boosts/self-boosts)"
  }
 }
}
